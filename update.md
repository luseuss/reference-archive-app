# 변경 이력 (PR 단위)

PR이 병합될 때마다 여기에 **무엇을 왜 어떻게 고쳤는지, 어떻게 테스트했는지,
그리고 검증하지 못한 한계**를 적습니다. 새 세션에서 "지금까지 뭘 했지?"를 파악하는 기록입니다.

프로젝트 구조·기술 결정·워크플로우 같은 절차적인 내용은 `CLAUDE.md`에 있습니다.

---

## PR #1 — Flutter 프로젝트 초기화 + 프로젝트 메모리 문서

**무엇을 했나**

기존 HTML/JS 웹앱을 Flutter로 재구축하는 새 저장소의 뼈대를 세웠습니다.

1. **Flutter 프로젝트 생성** — `flutter create`로 Windows / macOS / Linux / Android / iOS
   5개 플랫폼용 스캐폴딩 생성. 패키지명 `reference_archive_app`, org `com.luseuss`.
   웹은 제외했습니다(설치형 앱이 목표라 PWA 개념이 불필요 — `CLAUDE.md`의 플랫폼 차이표 참고).
2. **기본 카운터 데모를 실제 앱 뼈대로 교체** — `flutter create`가 만들어주는 예제
   카운터 앱은 영어 주석이라 이 프로젝트의 "모든 주석은 한국어" 규칙에 어긋납니다.
   PR #1부터 규칙을 실제로 보여주기 위해 처음부터 다시 썼습니다.
3. **Windows 창 제목을 한글로** — 빌드해서 실행해보니 창 제목이
   `reference_archive_app`으로 떴습니다. Windows 네이티브 창 제목은 Dart 쪽
   `MaterialApp(title: ...)`이 아니라 C++ 러너(`windows/runner/main.cpp`)에서
   따로 정해지기 때문입니다. **둘 다 고쳐야 합니다.**

   그런데 한글을 그냥 넣으면 깨집니다. Flutter가 만들어주는 기본 CMake 설정에는
   `/utf-8` 컴파일 옵션이 없어서, MSVC가 소스를 시스템 코드페이지(한국어 Windows는
   949)로 읽어버립니다. `windows/CMakeLists.txt`의 `APPLY_STANDARD_SETTINGS`에
   `/utf-8`을 추가해서 해결했습니다. 앞으로 C++ 쪽에 한글을 더 넣어도 안전합니다.

4. **`CLAUDE.md` / `update.md` 작성** — 다음 세션의 외부 기억 장치.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 앱 전체 색감 | `lib/main.dart`의 `_seedColor` 상수 하나만 바꾸면 밝은/어두운 테마 양쪽에 반영됩니다 |
| 밝은/어두운 모드 동작 | `lib/main.dart`의 `_buildLightTheme()` / `_buildDarkTheme()` / `themeMode` |
| 첫 화면 내용 | `lib/screens/home_screen.dart`의 `build()` 안 `body:` 부분 |
| 앱 이름(Windows 창 제목) | `windows/runner/main.cpp`의 `window.Create(L"...")` |
| 앱 이름(Dart 쪽) | `lib/main.dart`의 `MaterialApp(title: ...)` |
| C++ 코드에 한글 넣기 | `windows/CMakeLists.txt`의 `/utf-8` 옵션이 이미 켜져 있습니다 |
| 테스트가 기대하는 문구 | `test/widget_test.dart` |

**새로 나온 개념 2가지**

- **위젯(Widget)** — Flutter에서는 화면의 모든 것이 위젯입니다. 버튼도, 글자도, 여백도,
  화면 전체도 위젯입니다. 위젯을 레고처럼 안에 넣어 쌓아서 화면을 만듭니다.
  `StatelessWidget`은 스스로 바뀌는 값이 없는 위젯, `StatefulWidget`은 값이 바뀌면
  화면을 다시 그리는 위젯입니다. 지금은 바뀌는 값이 없어서 전부 Stateless입니다.
- **위젯 테스트(`flutter test`)** — 실제 창을 띄우지 않고 메모리 안에서 위젯을 그려본 뒤
  "이 글자가 화면에 있는가"를 자동으로 확인합니다. 기존 웹앱에서는 브라우저를 띄우고
  가짜 클릭 이벤트를 쏘는 번거로운 방식으로 테스트했는데, Flutter에서는 이게 기본 기능입니다.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음 (No issues found)
- `flutter test` — 위젯 테스트 1건 통과. 홈 화면의 제목·안내 문구·아이콘이 실제로
  그려지는지 확인합니다.
- `flutter doctor` — Flutter 3.47.1 / Dart 3.13.1 정상, Visual Studio / Windows 기기 모두 인식
- `flutter build windows` — 릴리스 빌드 성공 (`reference_archive_app.exe`, 25MB)
- **빌드된 exe를 실제로 실행해서 확인** — 창이 정상적으로 뜨고,
  제목 표시줄에 "레퍼런스 아카이브"가 깨짐 없이 출력되는 것까지 확인했습니다.

**한계 / 아직 확인 못 한 것**

- **Android 빌드는 검증하지 못했습니다.** 이 PC에 Android SDK를 아직 설치하지 않았습니다
  (약 10GB 추가 필요). 폰에서 실제로 볼 준비가 됐을 때 설치하고 그때 검증합니다.
  프로젝트에는 `android/` 폴더가 이미 생성되어 있어 SDK만 설치하면 바로 빌드 가능합니다.

---

## PR #2 — 데이터 모델 + Repository + drift 로컬 저장소

**무엇을 했나**

1단계(뼈대와 저장) 중 **저장 계층**을 만들었습니다. 화면은 아직 없습니다.
저장이 단단해야 화면 작업이 편하고, 한 PR에 다 넣으면 너무 커져서 나눴습니다.

1. **drift 데이터베이스 정의** (`lib/data/`)
   - `tables.dart` — 표 3개: `References`(레퍼런스), `TaxonomyItems`(폴더/카테고리/
     태그/프로젝트), `ReferenceTaxonomyLinks`(레퍼런스↔태그·프로젝트 연결)
   - `app_database.dart` — 데이터베이스를 열고 닫는 곳. 파일 위치는 `driftDatabase()`가
     기기별 앱 데이터 폴더를 알아서 찾습니다(설계 원칙 4-4).

2. **폴더·카테고리·태그·프로젝트를 한 표로 통합**
   넷 다 담는 정보가 (이름 + 시각)으로 완전히 같아서 표를 네 개 만들지 않고
   `kind` 칸으로 구분합니다. 기존 웹앱에서 `makeTaxonomy()` 하나로 폴더와 카테고리를
   함께 처리했던 것과 같은 방식입니다. 코드가 1/4로 줄고 고칠 곳도 한 군데뿐입니다.

3. **Repository 패턴** (`lib/repositories/`) — 설계 원칙 4-3의 실물
   - `reference_repository.dart` / `taxonomy_repository.dart` — **약속만** 적은 파일
   - `local_reference_repository.dart` / `local_taxonomy_repository.dart` — 실제 구현
   - **drift 코드는 `local_*.dart` 두 파일 안에만 있습니다.** 나중에 서버를 붙일 때
     `synced_*.dart`를 만들어 갈아끼우면 화면 코드는 안 건드려도 됩니다.

4. **화면용 모델** (`lib/models/`) — `ReferenceItem`, `TaxonomyItem`, `enums.dart`
   drift가 만들어주는 행 클래스(`ReferenceRow`, `TaxonomyItemRow`)와 이름으로 구분합니다.
   **`~Row`는 데이터베이스 한 줄, `~Item`은 화면이 쓰는 모델**입니다.

5. **UUID 생성기** (`lib/utils/id_generator.dart`) — `newId()` 하나뿐입니다.

**잡은 버그: 시각이 UTC로 저장되지 않고 있었음**

테스트를 쓰다가 발견했습니다. `DateTime.utc(2020,1,1)`을 저장했는데
`2020-01-01 09:00:00` **현지 시각(KST)** 으로 돌아왔습니다.
drift는 기본적으로 시각을 정수 타임스탬프로 저장하고 읽을 때 현지 시각으로 바꿔주는데,
그러면 설계 원칙 4-2("UTC로 저장")가 조용히 깨집니다. 시차가 다른 기기끼리 데이터를
합칠 때 "어느 쪽이 최신인가" 판단이 틀어지는, 나중에 찾기 아주 어려운 종류의 버그입니다.

두 겹으로 고쳤습니다.
- `build.yaml`에 `store_date_time_values_as_text: true` — 시각을 ISO 8601 글자로
  저장해서 시간대 정보(끝의 `Z`)와 밀리초 정밀도를 함께 보존합니다.
  **저장 형식 자체를 바꾸는 설정**이라 사용자 데이터가 없는 지금이 바꿀 수 있는
  마지막 시점이었습니다.
- 저장소가 모델로 변환할 때 `.toUtc()` — 위 설정을 누가 실수로 지워도 막히도록.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 레퍼런스에 저장할 항목 추가 | `lib/data/tables.dart`의 `References` → `schemaVersion` 올리고 `migration`에 추가 → 코드 생성 |
| 목록 정렬 순서 | `lib/repositories/local_reference_repository.dart`의 `getAll()` 안 `orderBy` |
| 폴더/태그 이름 중복 규칙 | `lib/repositories/local_taxonomy_repository.dart`의 `existsWithName()` |
| 폴더를 지울 때 딸린 것 처리 | 같은 파일의 `delete()` |
| 태그를 붙이고 떼는 방식 | `local_reference_repository.dart`의 `_replaceLinks()` |
| 데이터베이스 파일 위치 | `lib/data/app_database.dart`의 `_openConnection()` |
| 새 UUID 만드는 방법 | `lib/utils/id_generator.dart` |

**새로 나온 개념 4가지**

- **abstract class(약속)** — "이런 기능이 있어야 한다"는 목록만 적고 내용은 안 적은
  클래스입니다. 화면과 저장소 사이에 이걸 끼워두면, 저장 방식을 바꿔도 화면은 안 고쳐도 됩니다.
- **Future / async / await** — 데이터베이스를 읽는 데는 시간이 걸립니다. 그동안 화면이
  멈추면 안 되니까 "지금은 없지만 잠시 후 준다"는 약속(Future)을 먼저 돌려줍니다.
  받는 쪽에서 `await`를 붙이면 결과가 올 때까지 기다립니다.
- **transaction(거래)** — 여러 작업을 "전부 성공하거나 전부 실패하거나"로 묶는 것입니다.
  레퍼런스 본체는 저장됐는데 태그 연결에서 오류가 나면 반쪽짜리 데이터가 남기 때문입니다.
- **코드 생성(build_runner)** — 표 정의를 고친 뒤 `dart run build_runner build`를 돌리면
  실제 SQL과 Dart 코드가 자동으로 만들어집니다. **안 돌리면 반영되지 않습니다.**

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **31건 전부 통과** (기존 위젯 테스트 1건 + 새 저장소 테스트 30건)
  - 저장/조회/덮어쓰기, 소프트 삭제(데이터베이스에 실제로 남아있는지 직접 확인),
    핀 고정 정렬, 태그 붙이고 떼기, 뗐던 태그 되살리기, 태그와 프로젝트가 섞이지 않는지,
    분류 항목을 지웠을 때 딸린 레퍼런스 처리, 이름 중복 검사(공백·대소문자·종류별),
    createdAt 보존과 updatedAt 갱신, **시각이 UTC인지**
- 테스트는 메모리 안에서만 도는 데이터베이스(`NativeDatabase.memory()`)를 씁니다.
  파일을 만들지 않아 흔적이 남지 않고 테스트끼리 영향을 주지 않습니다.

**한계 / 아직 안 한 것**

- **실제 앱으로 실행해보지는 못했습니다.** drift는 SQLite 네이티브 라이브러리를 쓰는
  플러그인이고, Windows에서 플러그인을 빌드하려면 **개발자 모드**가 켜져 있어야 하는데
  아직 꺼져 있습니다. 다만 테스트는 이 제약과 무관하게 실제 SQLite 엔진으로 돌기 때문에,
  저장소 동작 자체는 31건으로 검증된 상태입니다. 개발자 모드를 켜면 바로 앱에서도 됩니다.
- **무드보드(씬/배치) 표는 아직 안 만들었습니다.** 4단계에서 만듭니다. 특히 메모의
  리치텍스트를 어떤 형식으로 담을지는 5단계에서 정할 문제라, 지금 정하면 섣부릅니다.
  drift에서 표를 나중에 추가하는 것은 `schemaVersion`을 올리고 `migration`에
  `m.createTable(...)` 한 줄을 더하면 되어서 어렵지 않습니다.
- 검색·일괄 선택·정렬 옵션은 2단계 항목이라 아직 없습니다.

---

## PR #3 — 레퍼런스 추가·목록·삭제 화면 + 이미지 자동 리사이즈

**무엇을 했나**

PR #2에서 만든 저장 계층 위에 **실제로 쓸 수 있는 화면**을 올렸습니다.
이걸로 1단계(뼈대와 저장)가 완료됩니다.

1. **이미지 추가** — 오른쪽 아래 버튼으로 파일을 고르면(여러 장 동시 가능)
   크기를 줄여서 앱 폴더에 저장하고 목록에 추가합니다.
2. **목록** — 격자로 표시. 칸 개수를 고정하지 않고 **칸 너비**를 정해서,
   창을 넓히면 칸이 늘고 폰처럼 좁은 화면에서는 저절로 줄어듭니다.
   데스크톱·모바일용 화면을 따로 만들지 않아도 됩니다.
3. **삭제** — 카드의 휴지통 버튼. 소프트 삭제라 **이미지 파일은 남겨둡니다**
   (되살렸을 때 그림 없는 빈 껍데기가 되지 않도록).
4. **이미지 자동 리사이즈** — 긴 변 1600px, JPEG 품질 85 (기존 웹앱과 동일 기준).
   원본이 1600px보다 작으면 억지로 키우지 않습니다.

**구조**

```
lib/services/
  image_resizer.dart        크기 줄이기 계산만 (파일·화면과 무관한 순수 함수)
  image_storage.dart        파일 저장 "약속"
  local_image_storage.dart  실제 파일 저장 구현
lib/widgets/
  reference_card.dart       카드 한 장의 생김새
lib/screens/
  home_screen.dart          목록 화면
```

**설계하다 막혀서 구조를 바꾼 부분 (중요)**

처음에는 `ImageStorageService` 하나에 전부 넣었는데, 위젯 테스트가
**`pumpAndSettle timed out`으로 멈춰버렸습니다.**

원인: `getApplicationSupportDirectory()`는 **플러그인**이라 테스트 환경에는
대답해줄 상대가 없어서 영원히 기다립니다. 화면이 플랫폼 의존 코드에 직접
묶여 있으면 화면을 테스트할 수 없다는 뜻입니다.

그래서 저장소(repositories/)와 **똑같은 방식**으로 나눴습니다.
- `ImageStorage` (약속) ← 화면은 이것만 압니다
- `LocalImageStorage` (실제 구현)
- `FakeImageStorage` (테스트용, 파일을 안 만들고 즉시 대답)

설계 원칙 4-3이 "나중에 서버 붙이기"뿐 아니라 **지금 당장 테스트를 가능하게
하는 것**에도 쓰인 사례입니다.

**잡은 버그 2개**

1. **그림이 아닌 파일을 고르면 앱이 터졌음** — `img.decodeImage()`는 문서상
   "못 읽으면 null"이지만 실제로는 **오류를 내며 터집니다**(내부적으로 여러 형식을
   하나씩 시험해보다가 데이터 범위를 벗어나 읽음). 사용자가 실수로 텍스트 파일을
   고르면 앱이 꺼졌을 것입니다. `image_resizer.dart`의 `_tryDecode()`로 감쌌습니다.

2. **데이터베이스와 이미지가 사용자 "문서" 폴더에 저장되고 있었음** — drift와
   path_provider의 기본값이 `getApplicationDocumentsDirectory()`인데, Windows에서
   그곳은 사용자의 문서 폴더입니다. 실제로 실행해보니
   `C:\Users\...\Documents\reference_archive.sqlite`가 생겼고, 이미지는
   `Documents\images\`에 들어갈 참이었습니다. **사용자가 직접 만든 images 폴더와
   충돌할 수 있는 위험**입니다. 양쪽 다 `getApplicationSupportDirectory()`
   (`%APPDATA%\com.luseuss\reference_archive_app`)로 바꿨습니다.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 리사이즈 기준 (1600px / 품질 85) | `lib/services/image_resizer.dart`의 `maxImageLongEdge`, `jpegQuality` |
| 저장 형식(JPEG 말고 다른 것) | 같은 파일의 `resizeImageBytes()` 안 `encodeJpg` |
| 이미지 저장 위치 | `lib/services/local_image_storage.dart`의 `_getImagesDirectory()` |
| 데이터베이스 파일 위치 | `lib/data/app_database.dart`의 `_openConnection()` |
| 카드 생김새 (제목·버튼·썸네일) | `lib/widgets/reference_card.dart` |
| 격자 칸 크기·간격 | `lib/screens/home_screen.dart`의 `_buildGrid()` |
| 비어 있을 때 안내 문구 | 같은 파일의 `_buildEmptyState()` |
| 추가 후 뜨는 메시지 | 같은 파일의 `_showResultMessage()` |
| 새 레퍼런스의 기본 제목 | 같은 파일의 `_saveOneImage()` |

**새로 나온 개념 3가지**

- **StatefulWidget과 setState** — 화면에 보이는 내용이 변하는 화면은
  StatefulWidget으로 만듭니다. 값을 바꾼 뒤 `setState(...)`를 부르면 Flutter가
  "바뀌었으니 다시 그려라"를 알아듣습니다. setState 없이 값만 바꾸면 화면은 그대로입니다.
- **compute (별도 작업 공간)** — 큰 사진의 크기를 줄이는 건 무거운 일이라,
  그냥 하면 그동안 화면이 얼어붙습니다. `compute()`는 그 일을 옆 작업 공간에서
  처리해줍니다. 단, 넘기는 함수는 **클래스 밖 최상위 함수**여야 합니다.
- **가짜(Fake)로 갈아끼우기** — 테스트에서 진짜 파일 저장 대신 가짜를 넣습니다.
  화면이 구현체가 아니라 약속에 의존하기 때문에 가능한 일입니다.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **50건 전부 통과** (기존 30건 + 새로 20건)
  - 화면 4건: 빈 목록 안내, 목록에 제목 표시, 제목 없을 때 "(제목 없음)",
    **휴지통 버튼을 실제로 눌러서** 목록과 데이터베이스 양쪽에서 사라지는지
  - 리사이즈 8건: 가로/세로/정사각형, 작은 이미지는 안 키움, 정확히 1600px,
    PNG→JPEG 변환, 픽셀 수 감소, 그림 아닌 파일
  - 실제 파일 저장 8건: 파일 생성, .jpg 확장자, 저장 후 크기 확인,
    이름 중복 없음, 그림 아닌 파일은 쓰레기 안 남김, 삭제, 없는 파일 삭제,
    앱 폴더 안에 저장되는지
- **실제 앱 실행 확인** — `flutter run -d windows`로 띄워서 데이터베이스가
  올바른 위치(`%APPDATA%\com.luseuss\reference_archive_app`)에 만들어지고,
  사용자 문서 폴더는 깨끗한 것까지 확인했습니다.

**한계 / 아직 확인 못 한 것**

- **파일 고르기 창을 눌러보는 것은 자동으로 못 합니다.** 운영체제가 띄우는
  창이라 테스트에서 조작할 수 없습니다. 다만 창이 닫힌 뒤의 처리(크기 줄이기,
  파일 저장, 목록 추가)는 전부 테스트로 검증했습니다.
  **실제로 이미지를 넣어보는 것은 직접 해보셔야 합니다.**
- **릴리스 exe는 Smart App Control이 막습니다.** 개발 중에는
  `flutter run -d windows`로 실행하면 됩니다.
- 지운 이미지 파일은 디스크에 남습니다. 안 쓰는 파일을 정리하는 기능은
  나중에 따로 만듭니다.
- 제목 고치기, 검색, 폴더 지정 등은 2단계 항목입니다.

---

## PR #4 — 레퍼런스 상세/편집 화면 (2단계 시작)

**무엇을 했나**

목록에서 **카드를 누르면 편집 화면이 열립니다.** 2단계(정리 체계)의 첫 번째 조각입니다.

1. **제목·메모 고치기** — 메모는 여러 줄 입력 가능
2. **폴더·카테고리 지정** — 드롭다운에서 하나만 고름. "없음"으로 빼낼 수도 있음
3. **태그·프로젝트 지정** — 칩을 눌러 켜고 끔. 여러 개 가능
4. **그 자리에서 새로 만들기** — 폴더/카테고리는 옆의 + 버튼,
   태그/프로젝트는 맨 끝의 "새 ○○" 칩. **만들면 바로 골라진 상태가 됩니다.**
   "먼저 폴더를 만들어주세요" 같은 안내 없이 흐름이 끊기지 않게 하는 것이 목적입니다.
   (기존 웹앱에서도 같은 이유로 + 버튼을 붙였습니다)
5. **즐겨찾기 / 맨 위 고정** 스위치. 목록 카드에도 작은 표시가 뜹니다.

**저장 시점**: 고칠 때마다 바로 저장하지 않고 오른쪽 위 "저장"을 눌렀을 때 한 번에
저장합니다. 타이핑마다 저장하면 데이터베이스를 계속 건드리게 되고, 사용자가
"역시 그만둘래" 하고 나갈 방법도 없어집니다.

**새 파일**

```
lib/screens/reference_detail_screen.dart   편집 화면
lib/widgets/taxonomy_single_field.dart     폴더·카테고리 (하나만 선택 + 만들기)
lib/widgets/taxonomy_multi_field.dart      태그·프로젝트 (여러 개 선택 + 만들기)
lib/widgets/create_taxonomy_dialog.dart    새로 만들기 대화상자 (이름 중복 검사)
```

폴더·카테고리·태그·프로젝트가 전부 같은 모양이라 위젯을 **종류별로 만들지 않고**
`TaxonomyKind`를 넘겨 돌려씁니다. 안내 문구도 그 종류의 한국어 이름에서
자동으로 만들어집니다. ("새 폴더 만들기", "새 태그 만들기" ...)

**잡은 버그: 메모를 비워도 지워지지 않았음**

테스트를 쓰다 발견했습니다. `copyWith(memo: null)`은 `?? this.memo` 때문에
**"안 바꿈"으로 취급**되어 원래 메모가 그대로 남습니다. 폴더·카테고리에서
겪었던 것과 정확히 같은 함정입니다(그건 `clearFolder()`로 따로 막아뒀었습니다).

`clearMemo()`를 또 만들 수도 있었지만, 이런 함수가 늘어날수록 "이 값도 비우는
처리를 했던가?"를 매번 신경 써야 하고 언젠가 반드시 하나를 빠뜨리게 됩니다.

그래서 **편집 화면은 copyWith를 쓰지 않고 모든 값을 하나하나 적는 생성자로**
새 객체를 만듭니다. 길지만 빠뜨린 값이 있으면 컴파일이 안 되므로 실수할 여지가
없습니다. `copyWith`는 값 하나만 바꾸는 곳에서는 그대로 쓸모가 있어 남겨뒀습니다.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 편집 화면에 항목 추가 | `lib/screens/reference_detail_screen.dart`의 `_buildForm()` |
| 저장할 때 담기는 값 | 같은 파일 `_save()` — **여기 빠뜨리면 저장이 안 됩니다** |
| 미리보기 크기·모양 | 같은 파일 `_buildPreview()` |
| 폴더/카테고리 고르는 칸 | `lib/widgets/taxonomy_single_field.dart` |
| 태그/프로젝트 칩 | `lib/widgets/taxonomy_multi_field.dart` |
| 이름 중복 검사 문구 | `lib/widgets/create_taxonomy_dialog.dart`의 `_save()` |
| 카드의 고정·즐겨찾기 표시 | `lib/widgets/reference_card.dart` |

**새로 나온 개념 3가지**

- **화면 이동(Navigator)** — `Navigator.push()`로 새 화면을 띄우고, 그 화면이
  닫힐 때까지 기다렸다가 **닫으면서 돌려준 값**을 받습니다. 편집 화면은 저장했을 때만
  `true`를 돌려주고, 목록은 그 값을 보고 다시 불러올지 정합니다.
  그냥 뒤로 나왔으면 다시 불러올 필요가 없기 때문입니다.
- **TextEditingController와 dispose** — 입력창의 글자를 읽고 관리하는 도구입니다.
  **만들었으면 반드시 `dispose()`로 정리해야 합니다.** 안 하면 화면을 닫아도
  메모리에 남습니다(메모리 누수). Flutter에서 가장 흔한 실수 중 하나입니다.
- **위젯 돌려쓰기** — 폴더·카테고리·태그·프로젝트용 위젯을 네 개 만들지 않고,
  "어떤 종류인지"를 값으로 넘겨 하나를 돌려씁니다. 고칠 일이 생겨도 한 군데만 보면 됩니다.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **61건 전부 통과** (기존 50건 + 편집 화면 11건)
  - 기존 값이 입력창에 채워지는지, 제목 수정 저장, **메모 비우기**,
    즐겨찾기·고정 저장, 태그 붙이기/떼기, 폴더 고르기, **폴더를 "없음"으로 되돌리기**,
    새 태그 만들면 바로 선택됨, 이름 중복 거부, 빈 이름 거부
  - 화면만 보는 게 아니라 **데이터베이스에 실제로 저장됐는지**까지 확인합니다.
    화면만 보면 "화면은 바뀌었는데 저장은 안 되는" 문제를 놓칩니다.
- 실제 앱을 `flutter run -d windows`로 띄워 오류 없이 실행되는 것 확인

**테스트하다 배운 것**: 위젯 테스트의 기본 화면은 800x600으로 작아서, 편집 화면
아래쪽(태그·프로젝트·스위치)이 화면 밖으로 나갔습니다. **화면 밖 위젯은 아예
만들어지지 않아서** 테스트가 "그런 버튼 없다"며 실패합니다. `useTallScreen()`으로
테스트 창을 크게 만들어 해결했습니다.

**한계 / 아직 안 한 것**

- **드롭다운·칩을 실제로 눌러보는 것은 테스트로 했지만, 눈으로 본 것은 아닙니다.**
  실제로 써보시고 어색한 부분 알려주세요.
- 분류 항목의 **이름 바꾸기와 삭제**는 아직 없습니다. 저장소에는 이미 있으니
  관리 화면만 붙이면 됩니다. 다음 PR 또는 그다음에 넣습니다.
- 검색·정렬·필터는 다음 PR(2단계 두 번째)입니다.

---

## PR #5 — 검색 + 정렬 + 필터

**무엇을 했나**

목록 위에 검색창과 필터 줄이 생겼습니다. 2단계(정리 체계)의 두 번째 조각입니다.

1. **검색** — 제목과 메모에서 찾습니다. 대소문자를 구분하지 않고, 가운데 글자로도 찾힙니다.
2. **정렬** — 최근 수정순 / 최근 추가순 / 오래된 순 / 제목순.
   **고정한 항목은 어떤 정렬에서도 항상 맨 위**에 옵니다.
3. **필터** — 폴더·카테고리·태그·프로젝트 각각 하나씩 + 즐겨찾기만 보기.
   여러 조건을 함께 걸 수 있고, 전부 만족하는 것만 나옵니다.
4. **조건 지우기** — 걸린 조건을 한 번에 해제.

**거르는 일은 전부 데이터베이스(SQL)가 합니다.** 전부 가져와서 화면에서 걸러내면
레퍼런스가 수천 장이 됐을 때 검색할 때마다 전부 읽어야 해서 느려집니다.
나중에 서버용 구현체를 만들 때도 같은 방식(서버에 조건을 넘김)이어야 합니다.

**새 파일**

```
lib/models/reference_query.dart          검색·필터·정렬 조건을 담는 클래스
lib/widgets/reference_filter_bar.dart    검색창 + 정렬 메뉴 + 필터 버튼들
```

**조건을 클래스로 묶은 이유**: `getAll(검색어, 폴더, 태그, 즐겨찾기, 정렬, ...)`처럼
인자를 늘어놓으면 조건이 하나 늘 때마다 저장소 함수의 모양이 바뀌고, 그걸 쓰는 곳을
전부 찾아 고쳐야 합니다. 클래스로 묶으면 값 하나만 추가하면 되고 기존 코드는
그대로 돌아갑니다.

**잡은 버그: 검색어에 % 나 _ 가 있으면 엉뚱한 결과가 나옴**

처음에 흔한 방식대로 `LIKE '%검색어%'`로 만들었습니다. 그런데 LIKE에서 `%`는
"아무 글자나 몇 개든", `_`는 "아무 글자 한 개"라는 **특수한 뜻**입니다.
그래서 "50%"나 "IMG_1234"를 검색하면 그 글자가 와일드카드로 해석되어
엉뚱한 것까지 나옵니다.

막으려고 `\`를 붙였는데, `ESCAPE '\'`를 함께 지정하지 않으면 오히려 `\`를 글자
그대로 찾게 되어 **더 나빠집니다.** 테스트가 이걸 잡아냈습니다.

결국 LIKE를 버리고 **`instr()`** 로 바꿨습니다. instr은 와일드카드 개념 자체가
없어서 언제나 글자 그대로 찾습니다. 양쪽을 `lower()`로 바꿔 대소문자도 무시합니다.
이스케이프를 신경 쓸 일이 아예 사라졌습니다.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 검색 조건 추가 (예: 태그 여러 개) | `lib/models/reference_query.dart`에 값 추가 → `local_reference_repository.dart`의 `search()`에 조건 추가 |
| 검색 대상 (제목·메모 말고 더) | `local_reference_repository.dart`의 `search()` 안 검색어 부분 |
| 정렬 방식 추가 | `reference_query.dart`의 `ReferenceSortOrder` → `search()`의 `_orderingFor()` |
| 검색 반응 속도 (디바운스 시간) | `lib/screens/home_screen.dart`의 `_onSearchTextChanged()` 안 300밀리초 |
| 필터 버튼 생김새·배치 | `lib/widgets/reference_filter_bar.dart` |
| 결과 없을 때 안내 문구 | `lib/screens/home_screen.dart`의 `_buildEmptyState()` |

**새로 나온 개념 2가지**

- **디바운스(debounce)** — "노을"을 치면 글자가 다섯 번 바뀝니다. 그때마다
  검색하면 쓸데없는 검색을 네 번 더 하게 되고 입력이 버벅입니다. 그래서 글자가
  바뀌면 바로 검색하지 않고 300밀리초를 기다리고, 그 사이에 또 입력하면 타이머를
  취소하고 다시 셉니다. 결과적으로 **타이핑을 멈췄을 때 한 번만** 검색합니다.
- **Timer와 dispose** — 타이머를 만들었으면 화면이 사라질 때 반드시 꺼야 합니다.
  안 끄면 화면을 닫은 뒤 타이머가 깨어나서 **이미 없어진 화면을 고치려다 오류**를
  냅니다. TextEditingController와 같은 이야기입니다 — 만들었으면 정리하세요.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **96건 전부 통과** (기존 61건 + 새로 35건)
  - 검색 로직 26건 (저장소만 놓고 SQL 검증): 제목·메모 검색, 가운데 글자,
    **% 와 _ 특수문자**, 지운 항목 제외, 폴더·카테고리·태그·프로젝트 필터,
    **뗀 태그로는 안 걸러지는지**, 즐겨찾기, 조건 여러 개 조합, 정렬 4종,
    고정 우선, `ReferenceQuery`의 `hasAnyFilter`/`clearFilter`/`clearAll`
  - 화면 9건: 검색해서 목록이 줄어드는지, 지우면 돌아오는지,
    **"조건에 맞는 게 없음"과 "아직 아무것도 없음"을 구분하는지**,
    즐겨찾기 칩, 폴더 필터 메뉴, 만들어둔 분류가 없으면 메뉴를 숨기는지,
    조건 지우기, 정렬 바꾸면 실제 순서가 바뀌는지
- 실제 앱을 `flutter run -d windows`로 띄워 오류 없이 실행되는 것 확인

**테스트하다 배운 것**: 격자에서 순서를 확인할 때 **세로 위치(dy)만 비교하면 안 됩니다.**
카드 두 개가 같은 줄에 나란히 놓이면 dy가 똑같기 때문입니다. "읽는 순서"
(위→아래, 같은 줄이면 왼→오른쪽)로 바꿔서 비교하도록 고쳤습니다.

**한계 / 아직 안 한 것**

- **분류 항목의 이름 바꾸기·삭제는 여전히 없습니다.** 오타로 만든 태그를 고칠 수
  없습니다. 다음 PR에 넣습니다.
- 태그는 **한 번에 하나만** 걸 수 있습니다. "태그 A와 B 둘 다"는 아직 안 됩니다.
  `ReferenceQuery`에 값만 추가하면 되도록 구조는 준비돼 있습니다.
- 일괄 선택은 다음다음 PR입니다.

---

## PR #6 — 분류 관리 화면 (이름 바꾸기·삭제 + 삭제 확인)

**무엇을 했나**

목록 화면 오른쪽 위 버튼으로 여는 **분류 관리 화면**을 만들었습니다.
지금까지는 폴더·태그를 만들 수만 있고 고칠 수 없어서, 오타로 만든 태그가
영영 남았습니다.

1. **탭으로 종류별 목록** — 폴더 / 카테고리 / 태그 / 프로젝트
2. **각 항목이 몇 개의 레퍼런스에서 쓰이는지 함께 표시**
3. **이름 바꾸기** — 중복 검사에서 자기 자신은 빼고 봅니다.
   안 그러면 이름을 안 바꾸고 저장할 때 "이미 있다"며 막히는 황당한 일이 생깁니다.
4. **삭제 + 확인 절차**
5. **새로 만들기** — 지금 보고 있는 탭의 종류로 만듭니다.

**삭제 확인 절차 (이번 PR의 핵심)**

분류 항목을 지우면 그걸 쓰던 레퍼런스에서 **조용히 연결이 끊기고 되돌릴 수 없습니다.**
"인물" 폴더를 지우면 그 안에 있던 사진 50장이 폴더 없음이 됩니다.
레퍼런스 자체는 살아있지만 정리해둔 게 날아가는 셈입니다.

그래서 지우기 전에 몇 개가 영향을 받는지 보여주고 확인받습니다.

- 쓰는 곳이 있으면: "이 태그를 쓰는 레퍼런스가 3개 있습니다. 레퍼런스 자체는
  지워지지 않지만, 그 태그 연결이 사라지며 되돌릴 수 없습니다."
- 쓰는 곳이 없으면: "이 태그를 쓰는 레퍼런스는 없습니다."
  ("0개가 영향을 받습니다"는 읽는 사람을 불필요하게 긴장시킵니다)

삭제 버튼은 빨간색이고, 바깥을 눌러 대화상자를 닫으면 지우지 않습니다.

**개수를 세는 로직에서 조심한 것**

연결 표만 세면 **틀린 숫자가 나옵니다.** 레퍼런스를 지워도 연결 줄은 소프트
삭제로 남아 있어서, "이미 지운 사진 3개가 이 태그를 쓴다"는 엉뚱한 안내가
나갑니다. 살아있는 레퍼런스와 이어붙여(join) 확인하도록 만들었고,
테스트로 이 경우를 따로 확인합니다.

**만든 도구: 한글 조사 자동 선택**

처음에 문구를 `'만들어둔 ${kind.displayName}이 없습니다'`처럼 썼더니
**"만들어둔 폴더이 없습니다"** 가 됐습니다. 조사는 앞 글자에 받침이 있느냐에
따라 달라지는데(폴더**가** / 앨범**이**), 문구에 하나로 박아두면 틀립니다.

`lib/utils/korean_particle.dart`를 만들어 받침을 보고 자동으로 고르게 했습니다.
한글은 유니코드에서 규칙적으로 늘어서 있어서, 글자 코드를 28로 나눈 나머지가
0이면 받침이 없습니다.

지금 분류 이름 네 개는 다 받침이 없지만, 나중에 "앨범" 같은 이름이 추가돼도
자동으로 맞습니다. `"폴더을(를)"` 같은 어색한 표기를 피하려는 것이기도 합니다.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 삭제 확인 문구 | `lib/screens/taxonomy_manage_screen.dart`의 `_confirmDelete()` |
| 사용 개수 세는 방식 | `lib/repositories/local_taxonomy_repository.dart`의 `countReferencesUsing()` |
| 목록에 표시할 정보 추가 | `taxonomy_manage_screen.dart`의 `_buildList()` |
| 이름 바꾸기 대화상자 | `lib/widgets/rename_taxonomy_dialog.dart` |
| 한글 조사 규칙 | `lib/utils/korean_particle.dart` |
| 관리 화면 여는 버튼 | `lib/screens/home_screen.dart`의 `AppBar` actions |

**새로 나온 개념 2가지**

- **탭 (TabController / TabBar / TabBarView)** — 화면 하나에 여러 목록을 담을 때
  씁니다. `TabController`도 만들었으면 `dispose()`로 정리해야 합니다.
  (`TextEditingController`, `Timer`와 같은 규칙입니다)
- **되돌릴 수 없는 일에는 확인을 받는다** — 기능이 아니라 태도에 가깝습니다.
  삭제·덮어쓰기처럼 되돌릴 수 없는 동작은 **무슨 일이 일어나는지 구체적으로**
  알려주고 확인받아야 합니다. "정말 삭제하시겠습니까?"보다
  "레퍼런스 3개에서 연결이 사라집니다"가 훨씬 나은 안내입니다.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **127건 전부 통과** (기존 96건 + 새로 31건)
  - 개수 세기 10건: 폴더·카테고리·태그·프로젝트 각각,
    **지운 레퍼런스는 안 세는지**, **뗀 태그는 안 세는지**, 분류를 지운 뒤 0이 되는지
  - 관리 화면 12건: 목록 표시, 사용 개수 표시, 빈 상태,
    이름 바꾸기(반영·중복 거부·**이름 안 바꾸고 저장**),
    삭제 확인(개수 안내·취소·삭제·안 쓰이는 경우 문구·**레퍼런스는 살아남는지**),
    새로 만들기
  - 한글 조사 9건: 받침 있음/없음, 영어, 빈 글자
- 실제 앱을 `flutter run -d windows`로 띄워 오류 없이 실행되는 것 확인

**부수 처리**: 관리 화면에서 분류를 지우고 돌아오면, 목록 화면에 걸려 있던
필터가 없어진 항목을 가리킬 수 있습니다. 그대로 두면 아무것도 안 나오는데
이유를 알 수 없으므로 조건을 자동으로 지웁니다.

**한계 / 아직 안 한 것**

- **일괄 선택**(체크박스 → 폴더 이동 / 태그 추가 / 일괄 삭제)이 2단계의 마지막
  남은 항목입니다.
- 분류 항목을 **합치기**(태그 A를 태그 B로 통합)는 없습니다. 오타로 태그를 두 개
  만들었을 때 유용하지만, 지금은 이름을 바꾸고 하나를 지우는 것으로 대신할 수 있습니다.

---

## PR #7 — 웹에서 이미지 가져오기 (끌어다 놓기 / 붙여넣기 / 주소)

**무엇을 했나**

의뢰인이 물어봤던 "웹에서 이미지를 끌어서 바로 추가하는 기능"입니다.
인수인계 문서가 "별도 검토"로 미뤄뒀던 항목이라 개발 단계에 없었습니다.

1. **앱 창에 끌어다 놓기** — 창 어디에 놓아도 됩니다. 끄는 중에는
   "여기에 놓으면 레퍼런스로 추가됩니다" 안내가 덧그려집니다.
2. **붙여넣기 (Ctrl+V / macOS는 Cmd+V)** — 클립보드의 이미지를 바로 추가합니다.
3. **이미지 주소 붙여넣기** — 클립보드에 이미지가 없으면 글자를 보고,
   주소면 내려받습니다. 브라우저에서 "이미지 주소 복사"를 한 경우입니다.

**왜 셋을 함께 만들었나 (핵심)**

"끌어다 놓기"만 만들면 **어떤 경우엔 되고 어떤 경우엔 안 되는** 기능이 됩니다.
브라우저에서 이미지를 끌면 브라우저가 무엇을 건네주는지가 상황마다 다릅니다.

- 이미지 데이터 자체 (드물게)
- 임시 파일 경로
- **이미지 주소(URL)만** ← 브라우저에서 끌 때 가장 흔합니다

그래서 떨어진 것을 그대로 파일로 읽으면 조용히 실패합니다.
`DroppedItemReader`가 종류를 가려내고, 주소면 내려받아서
**무엇이 들어오든 결국 이미지 데이터가 나오게** 만들었습니다.

**새 파일**

```
lib/services/image_source.dart          가져오기 "약속" + 결과 타입
lib/services/network_image_source.dart  인터넷·클립보드 실제 구현
lib/services/dropped_item_reader.dart   떨어진 게 파일인지 주소인지 판별
```

세 가지 경로가 전부 `_saveImageBytes()` 하나로 모입니다.
가져오는 방법은 셋이지만 저장 방식은 하나여야, 어느 쪽으로 넣든
똑같이 리사이즈되고 똑같이 기록됩니다.

**의존성 충돌을 만나서 패키지를 바꿈**

처음에 `super_clipboard`를 넣었더니 최신 0.9.1이 아니라 **0.1.7**이 잡혔습니다.
확인해보니 진짜 충돌이었습니다.

- `file_picker 12` → `win32 ^6.3.0`
- `super_clipboard 0.9.1` → `device_info_plus` → `win32 <6.0.0`

Windows에서 둘이 양립할 수 없습니다. 옛 API를 쓰는 대신 `pasteboard`로
바꿨고 최신 0.5.0이 깨끗하게 들어갔습니다.
**"버전이 낮게 잡혔다"는 그냥 넘길 신호가 아닙니다.**

**잡은 버그 2개**

1. **주소에서 제목을 뽑을 때 이중 디코딩** — `Uri.pathSegments`가 퍼센트 인코딩을
   **이미 풀어서** 주는데 `Uri.decodeComponent()`를 한 번 더 불렀습니다.
   한글 주소에서 `Illegal percent encoding` 오류가 났습니다.

2. **실패 이유가 엉뚱한 안내로 덮여 나감** — 주소 내려받기가 실패하면
   구체적인 이유("그 사이트가 이미지 가져오기를 막고 있습니다")를
   "클립보드에 이미지가 없습니다"로 덮어쓰고 있었습니다. 사용자는 멀쩡한
   클립보드를 다시 복사하러 가게 됩니다. 주소를 실제로 시도했는지를
   구분해서 그쪽 이유를 그대로 전하도록 고쳤습니다.

**안전장치**

- 내려받기 **20MB 제한** — 주소가 이미지가 아니라 큰 동영상을 가리킬 수 있습니다.
- **20초 제한** — 응답 없는 서버를 무한정 기다리면 앱이 멈춘 줄 압니다.
- **http/https만 허용** — `file:` 같은 주소를 받아들이면 엉뚱한 곳을 읽으려 합니다.
- 오류 문구는 **상태 코드 대신 할 일**을 알려줍니다.
  403이면 "그 사이트가 막고 있습니다. 이미지를 복사해서 붙여넣어 보세요."

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 내려받기 크기·시간 제한 | `lib/services/network_image_source.dart`의 `maxDownloadBytes`, `downloadTimeout` |
| 실패 안내 문구 | 같은 파일 `fetchFromUrl()` 안 |
| 주소에서 제목 뽑는 규칙 | 같은 파일 `titleFromUrl()` |
| 떨어진 게 주소인지 판별 | `lib/services/dropped_item_reader.dart`의 `looksLikeUrl()` |
| 끄는 중 안내 모양 | `lib/screens/home_screen.dart`의 `_buildDropArea()` |
| 붙여넣기 단축키 | 같은 파일 `build()` 안 `CallbackShortcuts` |
| 붙여넣기 우선순위(이미지 먼저) | 같은 파일 `_pasteFromClipboard()` |

**새로 나온 개념 3가지**

- **DropTarget** — 창에 무언가 끌어다 놓는 것을 받는 위젯입니다.
  들어옴/나감/놓음 세 시점을 각각 알려줍니다.
- **CallbackShortcuts + Focus** — 키 조합에 동작을 붙입니다.
  `Focus(autofocus: true)`로 감싸야 화면이 키 입력을 받습니다.
  안 감싸면 초점이 없어서 Ctrl+V가 그냥 무시됩니다.
- **가짜로 갈아끼워 네트워크 테스트하기** — 진짜 인터넷을 쓰는 테스트는 느리고,
  연결이 없으면 실패하고, 남의 서버 사정에 좌우됩니다. `http.Client` 자리에
  정해둔 답을 주는 가짜를 넣으면 "404일 때", "사이트가 막을 때" 같은
  실제로 만들기 어려운 상황도 마음대로 시험할 수 있습니다.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **159건 전부 통과** (기존 127건 + 새로 32건)
  - 내려받기 15건: 주소 형식 거절(http 아님·file:·빈 글자), 정상 내려받기,
    제목 뽑기, **404 / 403 / 20MB 초과 / 빈 응답 / 연결 실패**, 한글 주소
  - 드롭 판별 10건: http 판별, **Windows 경로를 주소로 착각하지 않는지**,
    파일 읽기, **주소면 내려받는지**, 실패 이유 전달, 빈 파일, 없는 파일
  - 붙여넣기 7건: 클립보드 이미지, 클립보드 주소, **이미지가 글자보다 우선**,
    빈 클립보드 안내, 주소가 아닌 글자, 내려받기 실패 이유 표시, 그림 아닌 데이터
- `flutter build windows` 성공 — `desktop_drop_plugin.dll`, `pasteboard_plugin.dll`
  정상 등록·번들 확인
- 실제 앱을 띄워 오류 없이 실행되는 것 확인

**한계 / 직접 확인이 필요한 것**

- **끌어다 놓기는 자동으로 테스트할 수 없습니다.** 운영체제가 만드는 동작이라
  테스트에서 흉내낼 수 없습니다. 떨어진 것을 읽는 부분은 따로 테스트했고,
  저장은 붙여넣기와 같은 함수를 공유하므로 경로 자체는 검증된 상태입니다.
  **브라우저에서 실제로 끌어보는 것은 의뢰인이 해봐야 합니다.**
- **사이트에 따라 안 될 수 있습니다.** 이미지 가져오기를 막는 사이트가 있습니다
  (403). 그럴 때는 "이미지 복사 → 붙여넣기"로 안내합니다.
- **모바일 공유 시트는 아직 없습니다.** 폰에서 "공유 → 레퍼런스 아카이브"는
  완전히 다른 코드 경로라 별도 작업입니다.

---

## PR #7 — 웹에서 이미지 가져오기 (끌어다 놓기 / 붙여넣기 / 주소)

**무엇을 했나**

의뢰인이 물어봤던 "웹에서 이미지를 끌어서 바로 추가하는 기능"입니다.
인수인계 문서가 "별도 검토"로 미뤄뒀던 항목이라 개발 단계에 없었습니다.

1. **앱 창에 끌어다 놓기** — 창 어디에 놓아도 됩니다. 끄는 중에는 안내가 뜹니다.
2. **붙여넣기 (Ctrl+V / macOS Cmd+V)** — 클립보드의 이미지를 바로 추가
3. **이미지 주소 붙여넣기** — 클립보드가 글자면 주소인지 보고 내려받음

**읽는 순서** (앞쪽이 빠르고 확실하므로 먼저 시도):

```
1. 이미지 데이터를 직접 줄 수 있으면 → 그대로 받음
2. HTML 조각에서 <img src> 찾기      → 링크에 감싸인 이미지 대응
3. 주소를 그대로 받기                → 평범한 이미지 드래그
4. 사이트가 자체 형식에 넣은 주소 찾기 → 핀터레스트 상세 페이지 대응
```

세 경로가 전부 `_saveImageBytes()` 하나로 모입니다. 가져오는 방법은 여럿이지만
저장 방식은 하나여야, 어느 쪽으로 넣든 똑같이 리사이즈되고 똑같이 기록됩니다.

**패키지를 두 번 바꿨습니다**

처음에 `desktop_drop`을 썼는데 **브라우저 드래그가 아예 안 됐습니다.**
소스를 열어보니 Windows에서 `CF_HDROP`(진짜 파일 드롭)만 받고 있었습니다.

```cpp
FORMATETC fmtetc = {CF_HDROP, nullptr, DVASPECT_CONTENT, -1, TYMED_HGLOBAL};
```

브라우저는 파일이 아니라 URL·HTML 형식으로 건네주므로 아예 거부됩니다.
`super_drag_and_drop`으로 바꿨고, 이 과정에서 **`file_picker`를 12 → 11로
낮춰야 했습니다**(win32 버전 충돌). 파일 고르기 코드를 예전 API로 되돌렸습니다.

`super_clipboard`도 처음엔 최신 대신 0.1.7이 잡혀서 이상했는데, 확인해보니
같은 win32 충돌이었습니다. **"버전이 낮게 잡혔다"는 그냥 넘길 신호가 아닙니다.**

**핀터레스트 대응 (의뢰인 주 사용처)**

세 번 시도해서 세 번째에 됐습니다. 앞의 두 번은 추측이었고 둘 다 틀렸습니다.

- ❌ "CDN이 막는다" → 확인해보니 Dart User-Agent로도 HTTP 200. 아님
- ❌ "페이지의 og:image를 쓰자" → 핀 페이지는 JS로 그려져서 서버가 껍데기만 줌. 아님
- ✅ **로그를 찍어 실제 데이터를 봤더니** 답이 나왔습니다

**피드 페이지**: HTML 조각이 오는데 **글자가 깨져서** 왔습니다.

```
받은 글자: 愼愠楲ⵡ慬敢㵬     원래: <a aria-label=
愼 = U+613C → 0x3C 0x61 → "<a"
```

UTF-8 바이트를 UTF-16으로 읽은 것이라, 되돌리는 함수(`repairMangledHtml`)를
만들어 해결했습니다. 정상 HTML을 망가뜨리지 않도록 **못 찾았을 때만** 시도합니다.

**핀 상세 페이지**: 표준 형식이 하나도 안 옵니다. 대신 핀터레스트가 자체 형식에
이미지 주소를 넣어두고 있었습니다.

```
application/x-pinterest-closeup-image
{"pinId":"...","previewImageUrl":"https://i.pinimg.com/736x/...jpg"}
```

사이트마다 키 이름이 다르므로(previewImageUrl, imageUrl, src...) 이름을 찾지 않고
**주소처럼 생긴 글자**를 찾습니다(`findImageUrlInText`). 다른 사이트에도 통합니다.

**원본 화질로 승격**: 핀터레스트가 알려주는 주소는 축소본입니다.
크기 부분만 바꾸면 원본이 나옵니다. 실제로 확인한 차이입니다.

| 주소 | 결과 |
|---|---|
| `/736x/...jpg` | 36KB |
| `/originals/...jpg` | **223KB** |
| `/originals/...png` | 403 (확장자는 바꾸면 안 됨) |

원본이 없는 경우도 있어서 실패하면 조용히 원래 주소로 돌아갑니다.

**PureRef는 어떻게 하나 (의뢰인 질문)**

`DragImageBits`를 읽습니다. 끌 때 커서를 따라다니는 미리보기 비트맵인데,
브라우저가 화면에 그리려면 어차피 만들어야 해서 **항상 존재합니다.**
그래서 페이지가 협조하든 말든 어디서나 동작합니다.

다만 측정해보니 **314x426**이었습니다. 원본이 아니라 화면에 보이던 크기입니다.
우리 방식(자체 데이터 + 원본 승격)이 핀터레스트에서는 화질이 더 낫습니다.
이 방식은 채택하지 않았습니다 — 비트맵 해석 코드가 필요하고 화질도 낮습니다.

**잡은 버그 3개**

1. **주소에서 제목 뽑을 때 이중 디코딩** — `Uri.pathSegments`가 퍼센트 인코딩을
   이미 풀어주는데 `decodeComponent()`를 또 불러서, 한글 주소에서 오류가 났습니다.

2. **실패 이유가 엉뚱한 안내로 덮임** — 주소 내려받기가 실패하면 구체적 이유
   ("그 사이트가 막고 있습니다")를 "클립보드에 이미지가 없습니다"로 덮어썼습니다.
   사용자는 멀쩡한 클립보드를 다시 복사하러 가게 됩니다.

3. **0 바이트를 안 걸러서 주소를 못 찾음** ← 이게 가장 배울 점이 많았습니다.
   자체 형식은 글자 하나에 2바이트를 써서 영어·숫자 사이에 `0x00`이 낍니다.
   진단 로그에서는 그게 **공백처럼 보였고**, 저는 로그의 겉모습을 보고
   테스트를 공백으로 만들었습니다. **테스트는 통과하는데 실제로는 동작하지
   않는** 상태였습니다. 바이트 처리를 `textFromCustomData()`로 꺼내서
   실제 UTF-16 바이트로 테스트하도록 고쳤습니다.

**나중에 이 부분을 고치려면 어디를 보면 되나**

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 내려받기 크기·시간 제한 | `lib/services/network_image_source.dart`의 `maxDownloadBytes`, `downloadTimeout` |
| 실패 안내 문구 | 같은 파일 `_fetch()` 안 |
| 핀터레스트 원본 승격 규칙 | `lib/services/image_source.dart`의 `upgradePinterestUrl()` |
| 자체 데이터에서 주소 찾는 규칙 | 같은 파일 `findImageUrlInText()` |
| 깨진 글자 되돌리기 | 같은 파일 `repairMangledHtml()` |
| 드롭에서 읽는 순서 | `lib/services/dropped_item_reader.dart`의 `read()` **(PR #8에서 화면에서 이리로 옮김)** |
| 받을 이미지 형식 목록 | 같은 파일 맨 위 `droppableImageFormats` / `dropRegionFormats` **(PR #8에서 옮김)** |
| 붙여넣기 단축키 | `lib/screens/home_screen.dart`의 `build()` 안 `CallbackShortcuts` |

**안 되는 사이트가 생겼을 때**: 디버그로 실행하면
`[드롭] 실패 — 넘어온 형식: [...]` 이 찍힙니다. 그 목록을 보면 무엇이
넘어오는지 알 수 있고, 대응 방법을 정할 수 있습니다.

**새로 나온 개념 3가지**

- **DropRegion** — 창에 끌어다 놓는 것을 받는 위젯. **받겠다고 선언한 형식만**
  들어옵니다. 목록에 없으면 놓아도 아무 일이 안 일어납니다.
- **CallbackShortcuts + Focus** — 키 조합에 동작을 붙입니다.
  `Focus(autofocus: true)`로 감싸야 화면이 키 입력을 받습니다.
- **가짜로 갈아끼워 네트워크 테스트하기** — `http.Client` 자리에 정해둔 답을 주는
  가짜를 넣으면 "404일 때", "사이트가 막을 때" 같은 상황을 마음대로 시험할 수 있습니다.

**어떻게 테스트했나**

- `flutter analyze` — 문제 없음
- `flutter test` — **178건 전부 통과** (기존 127건 + 새로 51건)
  - 내려받기 15건: 주소 형식 거절, 404/403/20MB 초과/빈 응답/연결 실패, 한글 주소
  - HTML에서 주소 뽑기 10건: 링크에 감싸인 경우, 따옴표, 대문자, `&amp;`, data: 제외
  - 깨진 글자 되돌리기 5건: **실제 로그 데이터 그대로** 사용
  - 자체 데이터 처리 4건: **실제 UTF-16 바이트로** 시험 (그냥 읽으면 실패하는 것까지 확인)
  - 핀터레스트 대응 11건: 실제 로그의 JSON, 원본 승격, 남의 사이트는 안 건드리는지
- `flutter build windows` 성공, 실제 앱에서 **피드·상세 페이지 둘 다 동작 확인**

**한계**

- **끌어다 놓기 자체는 자동 테스트가 안 됩니다.** 운영체제 동작이라
  테스트에서 흉내낼 수 없습니다. 읽고 저장하는 부분은 전부 테스트했습니다.
- **사이트에 따라 안 될 수 있습니다.** 아무것도 안 넘겨주는 사이트가 있습니다.
  그때는 "이미지 복사 → 붙여넣기"로 안내합니다.
- **모바일 공유 시트는 아직 없습니다.** 완전히 다른 코드 경로라 별도 작업입니다.

---

## PR #8 — 일괄 선택 (폴더 이동 / 태그 추가 / 삭제) + 드롭 코드 분리

**2단계(정리 체계)의 마지막 항목입니다. 이걸로 2단계가 전부 끝났습니다.**

커밋을 둘로 나눴습니다. 앞의 것은 정리만 하고 동작은 안 바꿉니다.

| 커밋 | 내용 |
|---|---|
| `5b7355b` | 끌어다 놓기 코드를 화면에서 떼어냄 (동작 변화 없음) |
| `c9f3d71` | 일괄 선택 기능 |

### 1. 정리 — 끌어다 놓기 코드 분리 (`5b7355b`)

`home_screen.dart`가 975줄까지 커졌습니다. `CLAUDE.md`의 "300줄 넘으면 나누기" 기준을
한참 넘었는데, **늘어난 대부분이 PR #7에서 붙인 사이트별 예외 처리**였습니다.
목록 화면 코드를 찾으려면 핀터레스트 우회법을 헤치고 지나가야 하는 상태였습니다.

그 부분을 `lib/services/dropped_item_reader.dart`로 옮겼습니다. **위치만 옮겼고
로직은 한 줄도 안 바꿨습니다.** 기존 테스트 178개가 그대로 통과하는 것으로 확인했습니다.

- `_droppableImageFormats` → `droppableImageFormats` (공개)
- `_readDroppedItem()` → `DroppedItemReader.read()`
- `_findUrlInCustomData()` / `_readValue()` / `_readFileBytes()` → 같은 클래스 안으로

덤으로 `dropRegionFormats`를 새로 만들었습니다. `DropRegion`이 "이 형식들을 받겠다"고
선언하는 목록인데, 원래는 화면에 있고 그걸 **읽는** 코드는 다른 데 있었습니다.
따로 있으면 한쪽만 고쳤을 때 **받아는 놓고 읽지 못하는 형식**이 생깁니다.
이제 둘이 같은 파일에 붙어 있습니다.

**결과**: `home_screen.dart` 975줄 → 771줄.

### 2. 일괄 선택 (`c9f3d71`)

**쓰는 법**

1. 오른쪽 위 ✓ 버튼, 또는 카드를 **길게 누르기**
2. 카드를 눌러 고르면 위쪽에 "3장 선택"이 뜸
3. 아래 막대에서 **폴더 이동 / 태그 추가 / 삭제**
4. X 버튼으로 고르기 끝내기

**새 파일**

- `lib/widgets/bulk_action_bar.dart` — 아래쪽 작업 막대 (버튼만 그리고 실제 일은 안 함)
- `lib/widgets/pick_taxonomy_dialog.dart` — "어느 폴더로?" / "어느 태그를?" 고르는 대화상자

**저장소에 함수 3개 추가** (`reference_repository.dart` + `local_reference_repository.dart`)

화면에서 `for` 문을 돌며 `save()`를 여러 번 부르지 않고 따로 만들었습니다. 이유 셋:
① 100장을 옮길 때 데이터베이스를 100번 오가지 않고 한 번에 끝남
② 50장째에서 오류가 나도 절반만 옮겨진 어정쩡한 상태가 안 됨
③ 나중에 서버를 붙일 때 "한 번의 요청"으로 보낼 수 있음 (설계 원칙 3)

| 함수 | 하는 일 |
|---|---|
| `moveManyToFolder(ids, folderId)` | 폴더로 옮김. **`null`을 넘기면 폴더에서 빼냄** |
| `addTaxonomyItemToMany(ids, taxonomyItemId)` | 태그를 **더함**. 갈아치우지 않음 |
| `deleteMany(ids)` | 소프트 삭제 |

### 왜 이렇게 했는지 (겉으로 안 보이는 판단들)

**"고른 게 하나라도 있으면 고르기 모드"로 하지 않았습니다.**
그렇게 하면 마지막 한 장의 선택을 풀었을 때 체크박스와 아래 막대가 통째로 사라집니다.
사용자는 잘못 눌러서 모드가 꺼졌다고 느낍니다. 그래서 `_isSelecting`(모드)과
`_selectedIds`(무엇을 골랐는지)를 따로 뒀습니다.

**안 보이게 된 것은 선택에서 뺍니다.**
세 장을 골라둔 채 검색어를 바꾸면 그중 두 장이 목록에서 사라질 수 있습니다.
그대로 두면 화면에는 "1장 골랐다"고 보이는데 **실제로는 3장이 지워집니다.**
`_loadItems()`의 `_selectedIds.removeWhere(...)`가 이걸 막습니다.

**이미 붙어있는 태그는 다시 안 붙입니다.**
그냥 `insertOnConflictUpdate`로 덮어쓰면 붙인 시각(`createdAt`)이 새로 찍힙니다.
그러면 아무것도 안 바뀌었는데 **나중에 기기 간 동기화가 "방금 새로 붙은 태그"로 착각합니다.**
`_isLinked()`로 먼저 확인하고 건너뜁니다. `_replaceLinks()`가 같은 이유로 이미
"실제로 달라진 것만 고치는" 방식이었는데, 그 원칙을 그대로 지켰습니다.

**태그를 붙이면 레퍼런스 본체의 `updatedAt`도 갱신합니다.**
태그는 다른 표에 있어서 본체는 글자 하나 안 바뀝니다. 하지만 여기서 갱신을 안 하면
"마지막 동기화 이후 바뀐 항목"을 고를 때 이 레퍼런스가 빠져서, **다른 기기에는 태그가
안 붙은 채로 남습니다.**

**일괄 삭제에만 확인을 받습니다.**
낱장 삭제에는 확인이 없습니다. 한 장을 잘못 지우는 것과 50장을 잘못 지우는 것은
되돌리는 수고가 완전히 다르기 때문입니다.

**"폴더 없음"과 "취소"를 구분해야 했습니다.**
대화상자를 그냥 닫은 것도 `null`, "폴더에서 빼줘"도 `null`이면 화면이 둘을 구분할 수
없습니다. 그래서 `PickedTaxonomy`라는 작은 클래스로 감쌌습니다 —
**취소는 결과 자체가 `null`, "없음"은 결과 안의 `item`이 `null`.**

**태그는 "붙이기"만 하고 "갈아치우기"는 안 합니다.**
여러 장에 같은 태그를 다는 일은 흔하지만, "고른 것들의 태그를 통째로 바꾸자"는
실수하기 쉽습니다. 통째로 바꾸는 건 편집 화면에서 한 장씩 할 수 있습니다.

**작업이 끝나면 고르기 모드를 끕니다.**
선택이 그대로 남아 있으면, 다음 작업이 방금 그 장들에 또 적용되는 줄 모르고
두 번 실행하기 쉽습니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 고르기 모드 전체 동작 | `lib/screens/home_screen.dart`의 `_isSelecting`, `_selectedIds`, `_toggleSelected()` |
| 위쪽 막대(선택 중) 모양 | 같은 파일 `_buildSelectionAppBar()` |
| 아래 작업 막대 버튼 | `lib/widgets/bulk_action_bar.dart` |
| 폴더·태그 고르는 대화상자 | `lib/widgets/pick_taxonomy_dialog.dart` |
| 삭제 확인 문구 | `home_screen.dart`의 `_confirmBulkDelete()` |
| 실제로 데이터를 고치는 부분 | `lib/repositories/local_reference_repository.dart`의 `moveManyToFolder()` / `addTaxonomyItemToMany()` / `deleteMany()` |
| 카드의 체크박스·선택 테두리 | `lib/widgets/reference_card.dart`의 `_buildThumbnailArea()`, `build()`의 `shape` |
| 길게 누르기 동작 | 같은 파일 `InkWell`의 `onLongPress` |

### 새로 나온 개념

- **`Set`** — `List`와 달리 ① 같은 값이 두 번 안 들어가고 ② "이게 들어있나?" 확인이 빠릅니다.
  카드를 그릴 때마다 확인하는 값이라 이 차이가 실제로 체감됩니다. (`_selectedIds`)
- **`late`** — "지금 말고 처음 쓸 때 만들어라". `DroppedItemReader`를 만들려면
  `widget.imageSource`가 필요한데, 값을 적어두는 시점에는 아직 `widget`이 준비되기 전입니다.
- **`bottomNavigationBar`** — 화면 맨 아래 자리. 떠 있는 버튼(FAB)과 달리
  **목록 영역을 그만큼 줄여주기 때문에** 마지막 줄 카드를 안 가립니다.
- **`Stack` / `Positioned`** — 위젯을 겹쳐 쌓기. 사진 위에 체크박스를 올리는 데 썼습니다.

### 어떻게 테스트했나

- `flutter analyze` — 문제 없음
- `flutter test` — **205건 전부 통과** (기존 178건 + 새로 27건)
- `flutter build windows` 성공
- **의뢰인이 실제 앱에서 직접 확인 완료** (`flutter run -d windows`)

새 테스트 27건:

`test/repositories/reference_bulk_test.dart` (12건) — 데이터가 맞게 고쳐지는지
- 고른 것들만 바뀌고 안 고른 건 그대로인지
- `null`을 넘기면 폴더에서 빠지는지 (`copyWith`로는 안 되는 경로)
- 원래 있던 태그를 지우지 않고 더하는지
- **이미 붙어있으면 붙인 시각을 새로 안 찍는지** (동기화 관련, 겉으로 안 보임)
- 예전에 뗐던 태그를 다시 붙일 수 있는지 (`deletedAt`만 찍힌 줄이 남아 있는 경우)
- 태그를 붙이면 레퍼런스의 `updatedAt`도 갱신되는지
- 소프트 삭제가 맞는지 (`deletedAt`만 찍히고 줄은 남는지)
- 시각이 UTC인지

`test/screens/home_bulk_select_test.dart` (15건) — 화면과 제대로 연결됐는지
- 고르기 버튼 / 길게 누르기로 모드 진입
- **고르기 모드에서 카드를 눌러도 편집 화면이 안 열리는지** — 이게 안 되면
  여러 장 고르기가 아예 불가능합니다
- **체크박스를 직접 눌러도 한 번만 골라지는지** — 체크박스는 카드(`InkWell`) 안에
  있습니다. 둘 다 반응하면 두 번 뒤집혀서 **아무 일도 안 일어난 것처럼 보입니다.**
  실제로는 `Checkbox`가 탭을 흡수해서 괜찮았지만, 나중에 카드 구조를 바꿀 때
  깨지기 쉬운 부분이라 테스트로 못 박아뒀습니다
- 전체 선택 / 전체 해제
- 폴더 이동 · 태그 추가 · 삭제가 실제 데이터에 반영되는지
- **취소했을 때 정말 아무것도 안 바뀌는지** (폴더 이동 취소, 삭제 취소)
- "폴더 없음"과 "취소"가 구분되는지
- 폴더/태그가 하나도 없을 때 무엇을 하면 되는지 알려주는지
- 아무것도 안 골랐을 때 버튼이 잠기는지
- 검색으로 안 보이게 된 것이 선택에서 빠지는지

### 한계 / 남은 것

- **`home_screen.dart`가 다시 1066줄이 됐습니다.** 정리로 200줄을 뺐지만 기능이
  290줄 붙었습니다. 다음에 나눌 후보는 **이미지 가져오기 부분**
  (`_addImages` / `_handleDrop` / `_pasteFromClipboard` / `_saveImageBytes` / `_finishAdding`)
  으로, `lib/services/reference_importer.dart`로 묶으면 200줄쯤 빠집니다.
  이번 PR에 같이 넣으면 봐야 할 것이 너무 많아져서 미뤘습니다.
- **지운 것을 앱에서 되살리는 방법이 아직 없습니다.** 데이터는 `deletedAt`만 찍혀
  남아 있으므로, 나중에 휴지통 화면을 붙이면 복구할 수 있습니다.
- **카테고리·프로젝트 일괄 지정은 없습니다.** 저장소의 `addTaxonomyItemToMany()`는
  종류를 안 가리므로 프로젝트에도 그대로 쓸 수 있습니다. 화면에 버튼만 붙이면 됩니다.
  버튼이 넷이 되면 아래 막대가 좁은 화면에서 답답해져서 일단 둘만 뒀습니다.
- **길게 누르기의 손맛**은 자동 테스트로 확인할 수 없습니다. 동작만 검증했습니다.

---

## PR #9 — 3단계 유튜브 (링크 저장 / 썸네일 / 앱 안에서 재생)

**3단계의 본체입니다.** 영상 주소를 저장하고, 제목·썸네일을 자동으로 가져오고,
앱을 나가지 않고 재생합니다.

### 시작 전에 정한 것: 재생 방식

기존 웹앱은 `iframe`으로 앱 안에서 바로 재생했는데, Flutter 데스크톱에는 그 방법이
그대로 없습니다. 의뢰인에게 두 갈래를 설명하고 **"앱 안에서 재생"** 을 선택받았습니다.

- 브라우저에서 열기 — 쉽고 안 깨지지만 앱 밖으로 나가야 함
- **앱 안에서 재생** ← 선택. 웹뷰 패키지가 필요하고 무거움

`flutter_inappwebview` 하나로 Windows·안드로이드·iOS·macOS를 전부 덮었습니다.
(Flutter 공식 `webview_flutter`는 **Windows를 지원하지 않습니다.**)

### 새 파일

| 파일 | 하는 일 |
|---|---|
| `lib/services/youtube_url.dart` | 주소에서 영상 번호 뽑기, 각종 주소 만들기 (인터넷·화면 안 건드림) |
| `lib/services/youtube_info_source.dart` | 제목(oEmbed)·썸네일 가져오기 |
| `lib/services/local_player_server.dart` | 재생기 페이지를 띄우는 임시 서버 (아래 "오류 153" 참고) |
| `lib/screens/youtube_player_screen.dart` | 재생 화면 |
| `lib/widgets/add_youtube_dialog.dart` | 주소 입력 대화상자 |

### 넣는 방법 3가지 — 전부 `_saveYoutubeReference()`로 모임

1. 오른쪽 아래 작은 ▶ 버튼 (클립보드에 유튜브 주소가 있으면 미리 채워줌)
2. Ctrl+V
3. 끌어다 놓기

**붙여넣기·끌어다 놓기는 유튜브인지를 이미지 처리보다 *먼저* 가려냅니다.**
안 그러면 유튜브 페이지를 이미지로 내려받으려다 "그림이 아닙니다"로 실패합니다.
사용자는 링크를 넣었을 뿐인데 이유를 알 수 없습니다.
단, **클립보드에 이미지가 있으면 이미지가 우선**입니다 — 이미지를 복사했는데
예전 유튜브 주소가 클립보드에 남아 있을 수 있어서입니다.

### 판단한 것들

**주소가 아니라 영상 번호만 저장합니다.** 같은 영상이라도 주소는 대여섯 가지
모양이지만 영상 번호는 하나입니다. 들어올 때 한 번 바꿔두면 앱 안에서는 그것만
다루면 됩니다.

**주소를 정규식이 아니라 `Uri`로 뜯습니다.** 정규식으로 "youtube.com이 들어있나"를
보면 `youtube.com.evil.net` 같은 주소가 통과합니다. `Uri`로 뜯어서 **사이트 이름
칸만** 비교하면 그런 일이 없습니다. 테스트에 이 경우가 들어 있습니다.

**제목은 oEmbed로 가져옵니다 — API 키가 필요 없습니다.** YouTube Data API를 쓰면
구글 계정을 만들고 키를 발급받아 앱에 넣어야 하는데, 키가 앱 안에 노출되고 하루
사용량 제한도 걸립니다. 의뢰인이 관리할 것이 하나 느는 셈입니다. 제목만 필요한
우리에겐 oEmbed로 충분합니다.

**제목을 `bodyBytes`에서 UTF-8로 직접 읽습니다.** `response.body`를 쓰면 서버가
인코딩을 안 알려줬을 때 **latin-1로 읽혀서 한글 제목이 깨집니다.**

**썸네일은 내려받아 파일로 저장합니다.** 매번 유튜브에서 가져오게 하면 인터넷이
없을 때 목록이 텅 빈 회색 칸이 됩니다. 이 앱은 "내 컴퓨터에 모아두는" 것이 핵심이라
이미지와 똑같이 파일로 갖고 있습니다. `maxresdefault`를 먼저 받아보고 없으면
`hqdefault`로 내려갑니다 — 큰 썸네일은 **모든 영상에 있지는 않습니다.**

**제목과 썸네일은 서로 독립적으로 실패하고, 둘 다 실패해도 저장합니다.**
비공개 영상이거나 인터넷이 끊겼다고 저장을 막으면 멀쩡한 영상을 못 넣습니다.
영상 번호만 있으면 나중에 재생할 수 있고 제목은 직접 적으면 됩니다.

**운영체제 이름으로 재생 가능 여부를 판단하지 않습니다.**
`Platform.isWindows` 같은 걸로 적어두면 웹뷰 부품이 없는 환경에서 **빨간 오류
화면**이 뜹니다. 대신 `InAppWebViewPlatform.instance != null`로 **부품이 실제로
준비됐는지**를 물어보고, 아니면 조용히 "브라우저에서 열기"로 넘어갑니다.
이 버튼은 실패했을 때만이 아니라 **항상** 있습니다.

**기존 웹앱의 임베드 우회법(PR #26~#31)은 안 가져왔습니다.** (인수인계 문서 지시)
그건 브라우저 iframe 제약을 피하려던 것인데 앱에는 그 제약이 애초에 없습니다.

### 겪은 문제 1 — 빌드에 `nuget`이 필요해졌다

`flutter_inappwebview_windows`는 빌드 중에 WebView2 SDK와 nlohmann.json을
**nuget으로 받아옵니다.** 없으면 이렇게 실패합니다.

```
error MSB3073: NUGET-NOTFOUND install nlohmann.json ...
```

`winget install Microsoft.NuGet`으로 해결. **설치 후 새 터미널을 열어야** PATH가
잡히고, 이미 한 번 실패했다면 `build` 폴더에 `NUGET:FILEPATH=NUGET-NOTFOUND`가
캐시돼 있어서 `flutter clean`을 해야 합니다.

### 겪은 문제 2 — 앱이 켜져 있으면 다시 빌드가 안 된다

```
error MSB3027: WebView2Loader.dll 을(를) 복사할 수 없습니다.
파일이 "reference_archive_app (19372)"에 의해 잠겨 있습니다.
```

실행 중인 앱이 `WebView2Loader.dll`을 붙잡고 있어서입니다.
**웹뷰가 들어오면서 새로 생긴 제약입니다** — 전에는 복사할 DLL 자체가 없었습니다.
다시 빌드하기 전에 앱을 완전히 닫아야 합니다(`q`).

### 겪은 문제 3 — 유튜브 오류 153 (가장 오래 걸림)

의뢰인이 실제로 실행해보니 영상 자리에 이렇게 떴습니다.

> 오류 153 — 플레이어 구성 오류

유튜브 재생기는 **"어느 페이지에 끼워져 있는지"** 를 확인하고, 그게 없으면 거부합니다.
**세 번 만에 고쳤습니다.**

1. **처음**: embed 주소를 웹뷰의 맨 위 페이지로 그대로 열었음 → 페이지가 어디에도
   속해 있지 않아 거부.
2. **두 번째 (틀림)**: iframe이 든 HTML을 만들고 `baseUrl`로 "youtube.com에 있는
   것으로 쳐달라"고 알림 → **Windows에서는 그대로 실패.**
3. **세 번째 (성공)**: 추측을 멈추고 **플러그인 소스를 직접 읽었습니다.**

   ```
   flutter_inappwebview_windows/windows/in_app_webview/in_app_webview.cpp:1057
       failedLog(webView->NavigateToString(utf8_to_wide(data).c_str()));
   ```

   Windows 구현은 HTML만 받고 **`baseUrl`을 아예 읽지 않습니다.** 흉내는 통하지
   않는다는 뜻이라, **진짜 주소를 만들기로** 했습니다. 내 컴퓨터 안에만 열리는
   작은 서버(`LocalPlayerServer`)를 잠깐 띄우고 웹뷰가 그 주소를 엽니다.
   그러면 브라우저에서 웹사이트를 보는 것과 똑같은 상태가 됩니다 —
   기존 웹앱이 아무 문제 없던 바로 그 조건입니다.

**서버에서 지킨 것 두 가지**
- `loopbackIPv4`에만 묶습니다. 같은 와이파이의 다른 사람은 접근할 수 없습니다.
  아무 데나 열어두면 남이 들여다볼 수 있게 됩니다.
- 포트는 0(운영체제가 빈 번호를 고름). 8080처럼 고정하면 다른 프로그램과 부딪힙니다.
- `dispose()`에서 반드시 끕니다. 안 끄면 영상 볼 때마다 서버가 쌓입니다.

**이번에도 배운 것**: PR #7의 핀터레스트 때와 똑같았습니다.
**추측 두 번 → 소스/데이터 직접 확인 → 해결.** 그래서 이번엔 고치면서
**진단 로그도 함께 넣었습니다.** 오류 153은 웹뷰 오류가 아니라 **유튜브 페이지
안에서** 나기 때문에 `onReceivedError`로는 안 잡히고, `onConsoleMessage`로만 보입니다.

```
[재생기] 임시 서버 켜짐: http://127.0.0.1:52341/
[재생기] 페이지 열림: http://127.0.0.1:52341/
[재생기/유튜브] (유튜브가 직접 남기는 말)
[재생기] 오류(주소): 설명
```

**부수 수정**: 곁다리 요청(광고·통계)이 실패해도 재생기를 오류 화면으로 덮지 않게
`request.isForMainFrame == false`면 넘어가도록 했습니다. 하나 막혔다고 볼 수 있는
영상을 못 보게 하면 안 됩니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| **새 유튜브 주소 모양이 안 먹힐 때** | `lib/services/youtube_url.dart`의 `youtubeVideoIdFrom()` |
| 제목 가져오는 방식 | `lib/services/youtube_info_source.dart`의 `_fetchTitle()` |
| 썸네일 화질 | 같은 파일 `fetchThumbnail()` |
| **재생이 안 될 때** | `lib/services/local_player_server.dart` 맨 위 주석 → 그다음 `youtube_player_screen.dart` |
| 자동재생·전체화면 옵션 | `youtube_url.dart`의 `youtubeEmbedUrl()`, `youtubePlayerHtml()` |
| 주소 입력 대화상자 | `lib/widgets/add_youtube_dialog.dart` |
| 추가 버튼 위치 | `lib/screens/home_screen.dart`의 `_buildAddButtons()` |
| 카드의 재생 버튼 | `lib/widgets/reference_card.dart`의 `_buildThumbnailArea()` |
| 끌어온 게 유튜브인지 가리는 곳 | `lib/services/dropped_item_reader.dart`의 `youtubeVideoIdOf()` |

### 새로 나온 개념

- **웹뷰(WebView)** — 앱 안에 띄우는 작은 브라우저. 유튜브 영상은 파일로 갖고 있는
  게 아니라 유튜브 서버에서 흘러오므로, 재생하려면 결국 브라우저가 하나 필요합니다.
- **oEmbed** — 유튜브가 영상 제목 정도는 **키 없이** 알려주는 공개 창구.
- **`Uri.parse` vs 정규식** — 주소는 정규식으로 "포함되어 있나"를 보면 안 됩니다.
  `youtube.com.evil.net`이 통과합니다.
- **로컬(loopback) 서버** — 내 컴퓨터 안에서만 접속되는 임시 웹서버.
  `HttpServer.bind(InternetAddress.loopbackIPv4, 0)`.

### 어떻게 테스트했나

- `flutter analyze` 문제 없음
- `flutter test` — **258건 전부 통과** (기존 205건 + 새로 53건)
  - 주소 뜯어보기 17건: 되는 주소 14가지, 안 되는 주소(흉내 주소 포함), 재생기 HTML 구조
  - 제목·썸네일 9건: 한글 제목, 401/JSON 아님/인터넷 없음, 큰 썸네일 없을 때 재시도
  - 화면 16건: 추가 3경로, 취소, 잘못된 주소, 썸네일·제목 없어도 저장, 재생 버튼
- `flutter build windows` 성공
- **의뢰인이 실제 앱에서 재생 확인 완료** (오류 153 수정 후)

### 한계

- **영상이 실제로 재생되는지는 자동 테스트로 확인 불가.** 테스트 환경에서는 웹뷰
  부품이 안 켜집니다(`InAppWebViewPlatform.instance == null`). 테스트가 보장하는
  것은 "재생 화면으로 간다"와 "웹뷰를 못 쓰는 환경에서도 안 깨진다"까지입니다.
- **호버 미리보기 없음** — 별도 작업으로 미뤘습니다. 카드마다 웹뷰를 두면 감당이
  안 되므로 "웹뷰 하나를 호버한 카드 위로 옮기는" 방식이 필요합니다.
- **같은 영상을 두 번 넣어도 막지 않습니다.** 기존 웹앱도 그랬습니다.
- **안드로이드 미확인** — 이 컴퓨터에 Android SDK가 없습니다.

---

## PR #10 — 호버 미리보기 (유튜브 카드에 마우스를 올리면 소리 없이 재생)

**이걸로 3단계가 전부 완료됐습니다.** 기존 웹앱에 있던 마지막 유튜브 기능입니다.

유튜브 카드에 마우스를 **0.2초쯤** 올려두면 썸네일 자리에서 소리 없이 재생됩니다.
벗어나면 바로 멈추고 썸네일로 돌아갑니다. 데스크톱에서만 동작합니다.

### 판단한 것들

**웹뷰를 카드마다 두지 않고 딱 하나만 둡니다.**
기존 웹앱은 카드마다 `iframe`을 둬도 괜찮았지만, 웹뷰는 훨씬 무거워서 영상이
서른 개면 웹뷰 서른 개가 됩니다. 마우스는 어차피 한 곳에만 있으므로 화면이
`_previewingItemId` 하나만 기억하고 **그 카드만** 재생기를 만듭니다.

**바로 켜지 않고 기다립니다.**
목록을 훑을 때 마우스는 카드 여러 장을 스쳐 지나갑니다. 올라오자마자 틀면
지나가는 길의 영상이 줄줄이 켜졌다 꺼지고, 볼 생각도 없던 영상을 계속 받아옵니다.
**처음엔 0.4초로 뒀는데 의뢰인이 써보고 "답답하다"고 해서 0.2초로 줄였습니다.**
(`home_screen.dart` 맨 위 `hoverPreviewDelay`)

**미리보기를 `IgnorePointer`로 감쌌습니다.**
웹뷰는 그 위의 클릭을 자기가 가져갑니다. 그냥 두면 미리보기가 도는 동안
**카드를 눌러도 편집 화면이 안 열리고** 재생 버튼도 길게 누르기도 먹통이 됩니다.
사용자 입장에서는 카드가 갑자기 고장난 것처럼 보입니다. `IgnorePointer`로 감싸면
미리보기는 "움직이는 썸네일"처럼만 동작하고 기존 조작은 하나도 안 바뀝니다.
호버를 살피는 일은 바깥의 `MouseRegion`이 하므로 영향받지 않습니다.

**재생 버튼이 미리보기를 가리던 문제.**
처음에는 재생 버튼이 언제나 가운데 크게 있었습니다. 의뢰인이 써보니
**영상 한가운데를 가려서 정작 보려던 것을 못 봤습니다.** 미리보기 중에는
작게 줄여 오른쪽 아래로 옮겼습니다. 아예 숨기면 크게 보러 갈 길이 사라지고,
왼쪽 위는 체크박스 자리라 비워뒀습니다.

**서버가 켜지는 사이에 마우스가 떠날 수 있습니다.**
미리보기를 켜려면 임시 서버를 띄워야 하는데(오류 153 대응), 그 사이 사용자는
이미 마우스를 옮겼을 수 있습니다. 그대로 진행하면 **마우스가 없는 카드에서
영상이 재생됩니다.** `_pendingPreviewId`로 한 번 더 확인해 막습니다.

**고르기 모드에서는 안 틉니다.** 체크박스를 누르려는데 뒤에서 영상이 돌면 산만합니다.

**재생 화면을 열기 전에 미리보기를 끕니다.**
▶를 누르는 시점에는 마우스가 카드 위에 있으므로 미리보기가 돌고 있습니다.
그대로 두면 **재생 화면 뒤에서 같은 영상이 하나 더 돌아갑니다.**

### 겪은 문제 — 재생 화면에서 뒤로 나오면 앱이 통째로 꺼짐

의뢰인이 실제로 써보다 발견했습니다. Dart 오류가 아니라 **네이티브 쪽에서 죽는**
것이라 아무 메시지 없이 창이 사라졌습니다.

**추측으로 고치지 않기 위해 `integration_test/`를 새로 만들었습니다.**
`test/` 폴더의 일반 테스트는 웹뷰가 아예 안 켜져서 이 영역을 건드릴 수 없습니다.
통합 테스트는 진짜 앱을 실행하므로 진짜 웹뷰가 만들어집니다.

```
flutter test integration_test -d windows
```

네 가지 시나리오를 만들어 쫓았습니다.

1. 재생 화면 한 번 열었다 닫기
2. 세 번 연속 열었다 닫기 (정리 쪽 문제는 대개 2~3번째에 터짐)
3. 미리보기가 켜진 채로 열었다 닫기 (웹뷰 2개)
4. 웹뷰가 사라지는 순간 새 웹뷰가 생기게 하기

**넷 다 안 죽었습니다 — 끝내 재현하지 못했습니다.**

그래서 확실한 것부터 고쳤고(위의 "재생 화면을 열기 전에 미리보기를 끕니다"),
**의뢰인 쪽에서는 그 뒤로 증상이 사라졌습니다.** 다만 재현을 못 했으므로
**어느 변경이 고쳤는지는 확정하지 못했습니다.** 웹뷰가 둘이던 상황이 없어진 것이
가장 유력합니다.

**다시 나타나면 볼 것**: `integration_test/youtube_player_test.dart`에 도구가
있으므로 시나리오만 바꿔가며 쫓으면 됩니다. 그리고 `flutter run` 터미널의
마지막 줄(`Lost connection to device` 등)이 결정적인 단서입니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| **기다리는 시간** | `lib/screens/home_screen.dart` 맨 위 `hoverPreviewDelay` |
| 어느 기기에서 켜지는지 | 같은 파일 `supportsHoverPreview` |
| 언제 켜고 끄는지 | 같은 파일 `_onCardHoverChanged()` / `_startPreview()` / `_stopPreview()` |
| 미리보기 생김새·클릭 처리 | `lib/widgets/reference_card.dart`의 `_buildPreviewPlayer()` |
| 재생 버튼 크기·위치 | 같은 파일 `_buildPlayButton()` |
| 음소거·조작버튼 숨김 | `lib/services/youtube_url.dart`의 `youtubeEmbedUrl(muted: true)` |
| **웹뷰가 얽힌 문제를 쫓을 때** | `integration_test/youtube_player_test.dart` |

### 새로 나온 개념

- **`MouseRegion`** — 마우스가 영역에 들어오고 나가는 것을 알려줍니다.
  손가락 터치로는 아무 일도 안 일어나서 폰에서는 저절로 조용해집니다.
- **`IgnorePointer`** — "이 안쪽은 클릭 대상으로 치지 말라". 보이기는 하지만
  마우스·터치는 통과해서 뒤에 있는 것이 받습니다.
- **`defaultTargetPlatform`** — 지금 어느 기기에서 도는지 알려줍니다.
  테스트에서는 `debugDefaultTargetPlatformOverride`로 **"폰인 척"** 하게 만들 수
  있어서, 폰이 없어도 폰 동작을 확인할 수 있습니다. 실제로 그렇게 했습니다.
- **통합 테스트(integration_test)** — 진짜 앱을 띄워놓고 하는 테스트.
  느리지만 웹뷰처럼 일반 테스트에서 안 켜지는 부품을 확인할 수 있습니다.

### 어떻게 테스트했나

- `flutter analyze` 문제 없음
- `flutter test` — **272건 전부 통과** (기존 258건 + 새로 14건)
  - 임시 서버 7건: **`127.0.0.1`로만 열리는지**(보안), 포트를 운영체제가 고르는지,
    끄면 정말 꺼지는지, 두 번 켜면 앞의 것이 저절로 꺼지는지
  - 음소거 주소 5건: 미리보기엔 `mute=1`·`controls=0`, **재생 화면은 음소거 아님**
  - 플랫폼 게이트 4건: 데스크톱 3종 켜짐, 안드로이드·iOS 꺼짐, 대기 시간 범위
- `flutter test integration_test -d windows` — 4건 통과 (진짜 웹뷰)
- `flutter build windows` 성공
- **의뢰인이 실제 앱에서 확인 완료**

### 한계

- **미리보기 영상이 실제로 재생되는지는 일반 테스트로 확인 불가.**
  그래서 웹뷰 부품이 없으면 **아무것도 안 그리도록** 방어했습니다 —
  확인 없이 만들면 목록 전체가 빨간 오류 화면이 됩니다.
- **호버할 때마다 웹뷰를 새로 만들어 0.2~0.3초쯤 걸립니다.** 웹앱의 `iframe`보다
  살짝 굼뜹니다. 데이터도 그때그때 받아옵니다.
- **꺼지던 문제의 원인은 확정하지 못했습니다.** (위 참고)

---

## PR #11 — 기존 웹앱 디자인 맞추기 (색 / 카드 / 메이슨리 격자)

의뢰인이 **"지금 만드는 게 원래 앱이랑 디자인이 많이 다르다"** 고 짚어서 시작한 작업입니다.
원본(`app.html`)의 CSS를 직접 확인해보니, 차이의 대부분을 만드는 것이 세 가지였습니다.

### 확인하다 찾은 실수 — 강조색이 틀렸다

`main.dart`에 **"기존 웹앱에서 쓰던 accent 색"** 이라고 적어두고 `#719F89`를 넣었는데,
그건 **랜딩페이지(`index.html`)** 색이었습니다.
실제 앱의 강조색은 **`#3D5A4C`** — 훨씬 진한 숲 초록입니다.
PR #1에서 잘못 가져온 것이 여기까지 왔습니다.

### 1. 색을 자동 생성하지 않고 원본 값을 그대로 적음

Flutter에는 대표색 하나로 나머지를 만들어주는 `ColorScheme.fromSeed`가 있고 처음엔
그걸 썼습니다. 그런데 **자동 생성 색이 원본과 달랐습니다.**

- 배경이 **푸른기 도는 회색** → 원본은 **따뜻한 오프화이트** `#F6F5F2`
- **테두리 색이 아예 없음** → 원본 카드는 그림자만이 아니라 **1px 테두리**로 형태가 잡힘

맞춰야 할 원본이 있을 때는 자동 생성이 오히려 방해가 됩니다.
`app.html`의 CSS 변수를 밝은/어두운 모드 둘 다 옮겨 적었습니다.

- `lib/theme/app_palette.dart` — 색 값 (원본 CSS 그대로)
- `lib/theme/app_theme.dart` — 그 색으로 테마 조립 (main.dart에서 분리)

### 2. 카드를 `Card` 위젯 대신 직접 그림

원본 카드는 **그림자가 두 겹**입니다. 하나는 바짝 붙어 옅게(윤곽), 하나는 멀리 넓게(뜬 느낌).
Flutter의 `Card`는 그림자를 `elevation` 숫자 하나로만 만들어서 이 모양이 안 나옵니다.
그래서 `Container`에 테두리와 그림자를 직접 그렸습니다.

**마우스를 올리면 2px 떠오르는 것**도 원본 동작이라 살렸습니다.
그것 하나 때문에 카드 전체를 StatefulWidget으로 바꾸긴 아까워서,
호버만 기억하는 작은 `_HoverLift` 위젯을 따로 뒀습니다.

### 3. 메이슨리 격자 — 가장 큰 차이

**지금까지 모든 사진을 4:3으로 잘라내고 있었습니다.**
레퍼런스를 모으는 앱에서 사진을 네모로 자르면 구도가 사라집니다.

`flutter_staggered_grid_view`의 `MasonryGridView`로 바꿔서, 칸의 **너비만** 정하고
높이는 사진이 정하게 했습니다. 원본 웹앱의 `column-count`와 같은 모양입니다.
유튜브는 어차피 전부 16:9라 고정했습니다 — 고정하면 썸네일이 아직 없을 때도
카드 크기가 안 흔들립니다.

### 겪은 문제 — 그림을 읽는 동안 카드 높이가 0

격자를 바꾸자 **기존 테스트 하나가 바로 실패**했습니다.
(`체크박스를 직접 눌러도 한 번만 골라진다`)

원인: 카드 높이를 그림이 정하게 했는데, **파일을 읽기 전에는 그림 높이가 0**입니다.
그러면 카드가 납작해지고, 그 위에 얹은 **체크박스와 재생 버튼이 카드 밖으로 밀려나
잘려서 안 눌립니다.** (`Stack`은 기본적으로 밖으로 나간 것을 잘라냅니다)

눈으로는 *"가끔 클릭이 안 먹네"* 정도로만 보였을 문제입니다.
`Image`의 `frameBuilder`로 읽는 동안 4:3 자리를 잡아두게 고쳤습니다.

**이번에 테스트를 새로 안 늘렸는데도** 기존 285개가 그대로 통과하는지가 중요했고,
실제로 그 과정에서 이 버그가 잡혔습니다.

### 어떻게 테스트했나
- `flutter analyze` 문제 없음, `flutter test` **272건 통과**, `flutter build windows` 성공
- 의뢰인이 실제 앱에서 확인 완료

---

## PR #12 — 카드에 폴더·카테고리·태그·메모·날짜 표시

PR #11에서 미뤄뒀던 "카드 안 내용"입니다. 이걸로 원본 카드와 같아졌습니다.

지금까지 카드에는 **제목만** 있었습니다. 어디에 넣었는지, 무슨 태그를 달았는지,
무슨 메모를 남겼는지 보려면 **카드를 하나씩 열어야** 했습니다.
정리해서 모으는 앱인데 정작 정리한 내용이 안 보이던 셈입니다.

원본과 같은 순서: 제목 → `📁` 폴더(옅게) → `🏷` 카테고리(강조색) → `#` 태그 알약 →
메모(최대 3줄) → 실선 → 날짜 · 삭제.

**삭제 버튼**이 제목 옆 휴지통 아이콘에서 **맨 아래 날짜 옆 "삭제" 글자**로 옮겨졌습니다.
원본과 같은 자리입니다. (`widget_test.dart`도 아이콘 대신 글자를 누르도록 고침)

**없는 항목은 아예 자리를 차지하지 않습니다.** 폴더도 태그도 메모도 없는 레퍼런스가
흔한데, 빈 줄이 남으면 카드마다 높이가 들쭉날쭉해집니다.

### 판단한 것 — 이름을 카드가 직접 찾지 않음

레퍼런스에는 폴더·카테고리·태그가 **id로만** 들어있습니다. 카드가 이름을 찾으러
데이터베이스를 뒤지면 **카드를 그릴 때마다 조회**가 일어나 목록이 버벅입니다.
그래서 화면이 "id → 이름" 표를 **한 번만** 만들어 모든 카드에 나눠줍니다.

**대신 위험이 하나 생겼습니다.** 표를 한 번 만들어두고 쓰니, 이름을 바꿔도
표를 다시 안 만들면 **카드에 옛 이름이 그대로 남습니다.** 사용자는 이름을
바꿨는데 안 바뀌었다고 느낍니다.

눈으로 놓치기 쉬운 종류라 **실제 UI를 거치는 테스트**로 못 박았습니다
(분류 관리 → 태그 탭 → 이름 바꾸기 → 뒤로 나오기 → 카드 확인).

### 날짜 — UTC로 저장, 보여줄 때만 현지 시각

이 앱은 시각을 언제나 UTC로 저장합니다(설계 원칙 2). 그래서 **보여줄 때는 반드시
현지 시각으로 바꿔야** 합니다. 안 바꾸면 한국에서는 9시간 어긋나서
**밤에 넣은 것이 하루 전 날짜로 보입니다.**

눈으로 알아채기 어려운 오류라 변환을 `lib/utils/date_format.dart` 한 곳에 모으고
테스트를 붙였습니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| **앱 색 전부** | `lib/theme/app_palette.dart` — 원본 CSS 값이 그대로 적혀 있음 |
| 모서리 둥글기 | 같은 파일 `appCornerRadius` |
| 앱바·입력창·대화상자 기본 모양 | `lib/theme/app_theme.dart` |
| 카드 테두리·그림자·호버 | `lib/widgets/reference_card.dart`의 `_buildCard()` |
| **카드에 보여줄 항목·순서** | 같은 파일 `_buildBody()` |
| 태그 알약 모양 | 같은 파일 `_buildTags()` |
| 날짜·삭제 줄 | 같은 파일 `_buildFoot()` |
| 그림 비율 처리 | 같은 파일 `_buildThumbnail()` |
| 격자 칸 너비·간격 | `lib/screens/home_screen.dart`의 `_buildGrid()` |
| 날짜 표기 | `lib/utils/date_format.dart` |
| 이름 표 만드는 곳 | `home_screen.dart`의 `_loadTaxonomyOptions()` |

### 새로 나온 개념
- **`ColorScheme` 직접 적기 vs `fromSeed`** — 맞춰야 할 원본이 있으면 자동 생성이 방해가 됩니다.
- **메이슨리 격자** — 벽돌 쌓듯 높이가 제각각인 칸을 쌓는 배치. 핀터레스트가 쓰는 방식.
- **`frameBuilder`** — 그림이 아직 안 읽혔을 때 무엇을 보여줄지. `errorBuilder`(실패)와 짝.
- **`Wrap`** — 한 줄에 다 못 들어가면 다음 줄로 넘겨주는 배치. 태그처럼 개수가 들쭉날쭉한 것에 씀.

### 어떻게 테스트했나
- `flutter analyze` 문제 없음, `flutter test` **285건 통과**(새로 13건), `flutter build windows` 성공
- `test/utils/date_format_test.dart` (4건): 한국식 표기, 0 붙이기,
  **밤 11시 30분에 넣은 것이 하루 전으로 안 밀리는지**
- `test/screens/home_card_content_test.dart` (9건): 폴더·카테고리·태그·메모·날짜 표시,
  **없는 항목이 자리를 안 차지하는지**, 공백뿐인 메모, **이름을 못 찾는 태그 건너뛰기**,
  **이름 바꾸면 카드에 반영되는지**

### 아직 원본과 다른 점
- **타입 배지** — 원본은 썸네일 왼쪽 위에 "이미지"/"유튜브" 배지가 있음.
  유튜브는 재생 버튼으로 구분되고 모든 이미지에 "이미지"를 붙이면 소음 같아 뺐습니다.
- **폰트** — 원본은 Pretendard 우선, 없으면 시스템 폰트. Windows에서는 대개 시스템
  폰트가 쓰이므로 지금과 큰 차이 없음. 꼭 맞추려면 폰트 파일(약 2MB)을 넣어야 합니다.
- **사진 크기를 미리 저장해두기** — 지금은 파일을 읽어야 비율을 알 수 있어서 처음 뜰 때
  자리가 한 번 잡힙니다. DB에 가로·세로를 저장하면 없앨 수 있지만
  **저장 구조 변경(마이그레이션)** 이라 따로 다뤄야 합니다.
- **편집 화면·분류 관리 화면 등 나머지 화면**은 아직 머티리얼 기본에 가깝습니다.

---

## PR #13 — 디자인 기반 잡기 (글자·둥글기·여백을 한곳으로)

색은 PR #11에서 한곳에 모았는데 **크기·굵기·여백은 여전히 화면마다 흩어져** 있었습니다.
같은 성격의 글자가 어떤 화면은 `12.5px`, 어떤 화면은 `13px`이었고,
"메모 글자를 키우자"고 하면 어디를 다 고쳐야 하는지 알 수 없는 상태였습니다.

### 새 파일

| 파일 | 담는 것 |
|---|---|
| `lib/theme/app_metrics.dart` | 모서리 둥글기, 여백, 격자 크기 |
| `lib/theme/app_text.dart` | 글자 크기·굵기 |

둘 다 기존 웹앱 `app.html`의 CSS에서 그대로 가져왔습니다.
(색은 이미 있던 `app_palette.dart`)

### 판단한 것들

**이름을 크기가 아니라 "쓰는 곳"으로 붙였습니다.**
`fontSize14_5` 같은 이름은 크기를 바꾸는 순간 이름과 실제가 어긋납니다.
그래서 `AppText.cardTitle`, `AppText.sectionLabel`처럼 붙였습니다.

**글자 스타일에 색을 넣지 않았습니다.**
색은 밝은 모드와 어두운 모드가 다릅니다. 스타일에 박아두면 한쪽에서만 맞게 됩니다.
크기·굵기만 정해두고 색은 쓰는 곳에서 `copyWith`로 붙입니다.
번거롭지만 **어두운 모드에서 안 보이는 글자가 생길 수 없습니다.**

**버튼을 한 가지 모양으로 통일했습니다.**
웹앱은 모든 버튼이 `둥글기 9px, 여백 9/14px, 굵기 600`으로 같고 배경색만 다릅니다.
Flutter는 버튼 종류마다 기본값이 달라서, 그냥 두면 화면마다 버튼이 조금씩 어긋납니다.

### 적어두면서 바로 잡힌 실수

칩(필터 버튼)에 **카드 태그용 둥글기(6px)를 쓰고 있었습니다.**
원본에서 둘은 다릅니다 — `.chip`은 `border-radius: 99px`인 **알약**,
`.tag`는 `6px`인 **각진 사각형**.

값을 한곳에 모아 이름을 붙이는 순간 드러났습니다. **이런 걸 막으려고 하는 작업입니다.**

칩은 `StadiumBorder`로 바꿨습니다. 웹은 "아주 큰 숫자를 넣어 알약처럼 보이게" 하지만,
Flutter에는 높이에 맞춰 알아서 둥글게 해주는 것이 있습니다. 글자 크기가 바뀌어도 따라옵니다.

**덤**: 빈 화면 안내의 64px 아이콘이 강조색이라 정작 눌러야 할 버튼보다 눈에 띄었습니다.
안내는 거들 뿐이라 옅은 색으로 내렸습니다.

### 어떻게 테스트했나

- `flutter analyze` 문제 없음, `flutter test` **285건 통과**, `flutter build windows` 성공
- **테스트를 새로 안 늘렸습니다.** 이번 작업은 "값을 옮겨 이름 붙이기"라
  **동작이 안 바뀌는 것**이 핵심이고, 기존 285건이 그대로 통과하는 것이 그 확인입니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 글자 크기·굵기 | `lib/theme/app_text.dart` |
| 모서리 둥글기·여백 | `lib/theme/app_metrics.dart` |
| 색 | `lib/theme/app_palette.dart` |
| 버튼·입력창·칩 기본 모양 | `lib/theme/app_theme.dart` |
| 격자 칸 너비·간격 | `app_metrics.dart`의 `gridMaxCrossAxisExtent`, `gridSpacing` |

---

## PR #14 — 화면 구조 껍데기 (사이드바 + 본문, 설정 화면)

의뢰인이 **목업 이미지로 앞으로의 화면 구조를 정해줬습니다.** 왼쪽 사이드바 +
오른쪽 본문 2단이며, 지금까지의 "위쪽 막대 + 격자"에서 크게 바뀝니다.
(구조 자체는 `CLAUDE.md`에 그림과 함께 적어뒀습니다)

"껍데기 먼저 → 파트 도입" 순서로 하기로 했고, 이건 그 1단계입니다.

| 자리 | 들어간 것 |
|---|---|
| ① 사이드바 위 | 사용자 이름 |
| ② 사이드바 가운데 | 파트 목록 — **"전체 레퍼런스" 하나뿐** |
| ③ 사이드바 아래 | 설정 · 로그인 |
| ④ 머리줄 | 제목 · 개수 · **검색창** · **추가 버튼** |
| ⑤ 필터 줄 | 폴더 / 카테고리 / 프로젝트 · 정렬 · 즐겨찾기 · 고르기 · 분류 관리 |
| ⑥ 격자 | 그대로 |

**②(파트)를 일부러 안 넣었습니다.** "어느 파트인지"를 저장하려면 데이터베이스
구조를 바꿔야 하는데(마이그레이션), 화면 변경과 섞으면 문제가 생겼을 때 원인을
가리기 어렵습니다. 지금은 모든 레퍼런스가 한곳에 있으므로 "전체"만 있는 것이
**사실 그대로**이기도 합니다.

### 옮긴 것
- **검색창** — 필터 줄 안 → 위쪽 머리줄(④). `reference_search_field.dart`로 분리
- **추가 버튼** — 떠 있던 버튼(FAB) → 머리줄 오른쪽. 의뢰인이 정한 자리
- **고르기 / 분류 관리** — 위쪽 막대 → 필터 줄(⑤)

### 폰에서 쓸 수 있게 (놓치기 쉬운 부분)

사이드바는 **232px**입니다. 폰 화면이 400~500px이니 **절반 가까이** 잡아먹습니다.
그대로 두면 정작 볼 목록이 손바닥만 해집니다.

그래서 **창이 900px보다 좁으면 사이드바를 서랍(Drawer)에 넣고**, 머리줄의 메뉴
버튼으로 꺼내 씁니다. 넓은 창에서는 늘 펼쳐져 있고 메뉴 버튼은 안 나옵니다.

**개발하는 컴퓨터 화면은 넓어서 늘 괜찮아 보이는** 종류의 문제라, 넓은 창·좁은 창
양쪽을 테스트로 못 박아뒀습니다.

### 설정 화면을 함께 만든 이유

③에 버튼만 있고 눌러도 아무 일이 없으면 고장난 줄 압니다. 의뢰인이 정한 세 가지만
담았습니다 — 밝기 모드 / 사용자 이름 / 만든 사람·버전.

**설정을 drift 데이터베이스가 아니라 `shared_preferences`에 담았습니다.**
- 밝기 모드는 레퍼런스 데이터가 아니고, 기기마다 달라도 됩니다
  (컴퓨터는 어둡게, 폰은 밝게)
- 나중에 기기 간 동기화를 붙일 때도 같이 넘길 값이 아닙니다
- 데이터베이스에 넣었다면 **설정 하나 추가할 때마다 마이그레이션**을 해야 했습니다

`ChangeNotifier`로 알려서 고르는 즉시 앱 전체가 바뀝니다. 껐다 켜야 반영되면
고장난 줄 알게 됩니다.

**로그인은 "아직 만들지 않았습니다"라고 알려줍니다.** 버튼을 빼지 않은 이유는
의뢰인이 정한 구조에 자리가 있어서입니다. 사이드바에도 가짜 아이디를 지어내지 않고
**"로그인 안 함"** 이라고 그대로 적었습니다.

### 만들면서 잡힌 것

Flutter가 경고를 줬습니다 — **배경색을 칠한 `Container`가 그 안 항목의 "누른
표시(물결)"를 덮는다**는 것이었습니다. 그대로 뒀으면 설정에서 항목을 눌러도 반응이
없는 것처럼 보였을 겁니다. `Material`로 바꿔서 모양은 그대로 두고 물결을 살렸습니다.

### 함께 한 정리 — 가져오기 코드 분리

`home_screen.dart`가 **1438줄**이라 여기에 사이드바를 더 얹으면 감당이 안 됩니다.
`CLAUDE.md`에 "밀린 정리거리"로 적어뒀던 작업을 먼저 했습니다.

가져오기 네 경로(파일 고르기·끌어다 놓기·붙여넣기·유튜브)를
`lib/services/reference_importer.dart`로 옮겼습니다. **동작은 그대로**고
테스트 285개가 그대로 통과했습니다. (1438 → 1126줄)

덤으로, 네 경로가 **앞뒤로 똑같은 일**(중복 실행 막기 → "추가하는 중" 켜기 → 끄기 →
목록 갱신 → 안내)을 각자 적어두고 있었는데 `_runImport` 하나로 합쳤습니다.
네 벌로 두면 언젠가 한 군데를 빠뜨려 **"추가하는 중"이 안 꺼지는** 일이 생깁니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| 사이드바 생김새 | `lib/widgets/app_sidebar.dart` |
| **사이드바가 서랍으로 바뀌는 창 너비** | 같은 파일 `sidebarBreakpoint` |
| 사이드바 너비 | 같은 파일 `sidebarWidth` |
| 머리줄(제목·검색·추가) | `lib/widgets/main_header.dart` |
| 검색창 | `lib/widgets/reference_search_field.dart` |
| 필터 줄 | `lib/widgets/reference_filter_bar.dart` |
| 설정 항목 | `lib/screens/settings_screen.dart` |
| **앱 버전·만든 사람** | 같은 파일 맨 위 `appVersion`, `appAuthor` |
| 설정 저장 | `lib/services/app_settings.dart` |
| 가져오기(추가) 동작 | `lib/services/reference_importer.dart` |

### 새로 나온 개념
- **`ChangeNotifier` / `ListenableBuilder`** — "값이 바뀌었다"고 화면에 알려주는
  Flutter의 기본 장치. 설정을 바꾸면 앱 전체가 곧바로 반영되는 이유.
- **`shared_preferences`** — 운영체제가 앱마다 마련해주는 작은 저장 공간.
  데이터베이스를 쓸 만큼은 아닌 자잘한 설정용.
- **`Drawer`** — 옆에서 밀려 나오는 서랍. 좁은 화면에서 사이드바를 여기 넣습니다.
- **`MediaQuery.sizeOf(context)`** — 지금 창 크기. 넓은 창/좁은 창을 갈라 그릴 때 씁니다.

### 어떻게 테스트했나
- `flutter analyze` 문제 없음, `flutter test` **302건 통과**(새로 17건),
  `flutter build windows` 성공
- `test/services/app_settings_test.dart` (8건): 기본값, 저장·재읽기, 공백 떼기,
  **빈 이름이면 기본 이름으로 되돌리기**, **저장된 값이 이상해도 앱이 켜지는지**,
  알림이 가는지 / 안 바뀌었으면 안 가는지
- `test/screens/app_shell_test.dart` (9건): 넓은 창/좁은 창 양쪽, 메뉴 버튼으로
  꺼내기, 설정 화면 열기, **어두운 모드가 곧바로 반영되는지**, 로그인 안내
- 기존 테스트는 버튼 위치가 바뀐 만큼 **찾는 방법만** 고쳤습니다 (툴팁 → 버튼 글자)

---

## PR #15 — 더블클릭으로 앱 켜고 끄기

의뢰인이 **"앱 실행하는 법이 너무 귀찮다"** 고 해서 만들었습니다.
매번 터미널 열고 → `cd` → `flutter run -d windows` → `q` 였고,
게다가 **앱을 안 닫고 다시 실행하면 빌드가 실패**하는 함정까지 있었습니다.

| 파일 | 언제 쓰나 |
|---|---|
| `앱 만들기.bat` | 코드가 바뀐 뒤 한 번. 앱을 만들고 **바탕화면에 바로가기**를 놓습니다 |
| `개발용 실행.bat` | 고친 것을 바로 확인할 때. 터미널이 함께 뜨고 `q`로 끕니다 |
| 바탕화면 바로가기 | **평소 쓸 때.** 그냥 프로그램처럼 열고 창을 닫아 끕니다 |

**둘 다 실행 중인 앱을 먼저 닫고 시작합니다.** `MSB3027`(WebView2Loader.dll을
붙잡고 있어서 나는 실패)을 자동으로 피하려는 것입니다.

### 겪은 문제 — 스크립트가 자기 한글 파일명 때문에 실패

의뢰인이 처음 실행했을 때 앱은 만들어졌는데 **바로가기가 안 생겼습니다.**
그런데 배치 파일은 "다 됐습니다"라고 했습니다. 원인이 **둘**이었습니다.

**① 한글이 깨졌습니다.** 경로가 `?덊띁?곗뒪 아카이브.lnk`로 나왔습니다.
**Windows PowerShell 5.1은 `.ps1` 파일에 BOM이 없으면 한글을 949 코드페이지로
읽습니다.** 파일은 UTF-8인데 다르게 읽혀서, 코드 안의 `'레퍼런스 아카이브.lnk'`가
쓰이기도 전에 깨졌습니다.

오류는 `"저장할 수 없습니다"` + `FileNotFoundException`으로만 나와서
**글자 문제인 줄 알아채기 어렵습니다.** 파일 맨 위에 "반드시 BOM 있는 UTF-8로
저장할 것"이라고 적어뒀습니다. 편집기에서 무심코 다시 저장하면 또 깨집니다.

**② 실패했는데 성공했다고 했습니다.** 배치 파일이 PowerShell의 종료 코드를
확인하지 않았습니다. 이제 나눠서 알려줍니다.
- 앱 만들기 실패 → 자주 있는 원인 안내 (nuget 없음 / 앱 켜져 있음)
- **앱은 됐는데 바로가기만 실패** → exe 경로를 알려줍니다. 앱 자체는 쓸 수 있으므로
  이걸 "전부 실패"로 다루면 안 됩니다.

스크립트 쪽도 `$ErrorActionPreference = 'Stop'` + try/catch를 넣고,
`Save()` 뒤에 **파일이 실제로 생겼는지 확인**합니다. 조용히 실패하는 경우가 있습니다.

### 왜 PowerShell을 파일로 분리했나
배치 파일 안에 PowerShell을 욱여넣으려면 따옴표를 여러 겹 겹쳐야 하는데,
하나만 어긋나도 알아보기 힘든 오류가 납니다. `tools/make_shortcut.ps1`로 나누니
평범한 PowerShell 코드가 되어 읽기도 고치기도 쉬워집니다.

### 어떻게 테스트했나
- 스크립트를 실제로 실행해서 **바탕화면에 바로가기가 생기는 것**과
  그 바로가기가 **빌드된 exe를 가리키는 것**까지 확인
- `앱 만들기.bat`을 처음부터 끝까지 돌려서 세 단계가 다 지나가는 것 확인

---

## PR #16 — 파트 (사이드바에서 큰 갈래로 나눠 보기) + 저장 구조 v2

의뢰인이 목업으로 정해준 **②(파트)** 를 채웠습니다. 디자인/파티클 같은 가장 큰
갈래로 레퍼런스를 나눠 보는 기능이고, 사이드바에서 고릅니다.

이걸로 거르는 단계가 셋이 됐습니다.

| | 무엇으로 | 자리 |
|---|---|---|
| **파트** | 큰 갈래 | 사이드바 |
| 폴더·카테고리·프로젝트·태그 | 그 안에서 좁히기 | 필터 줄 |
| 검색어 | 글자로 찾기 | 머리줄 |

### 파트를 새 개념으로 만들지 않았습니다

의뢰인이 **"사용자가 추가할 수 있게"** 로 정해줘서, 파트를 그냥
`TaxonomyKind`에 하나 더한 것으로 만들었습니다. 폴더·카테고리·태그·프로젝트와
성격이 같기 때문입니다.

덕분에 **만들기·이름 바꾸기·삭제가 공짜로 따라왔습니다.** 분류 관리 화면이
`TaxonomyKind.values`를 돌면서 탭을 만들기 때문에, 파트 탭이 저절로 생겼습니다.
새로 만든 화면이 없습니다.

**단, 위쪽 필터 줄에서는 뺐습니다.** (`filterableTaxonomyKinds`)
같은 것을 사이드바와 필터 줄 두 군데서 고를 수 있으면 "어느 쪽이 진짜지?" 하게 됩니다.

### 저장 구조 v1 → v2 (이 앱의 첫 마이그레이션)

`CLAUDE.md`에 *"기존 사용자의 앱만 안 켜지는 실수"* 로 못 박아둔 그 작업입니다.

업데이트할 때 세 가지가 일어납니다.
1. References에 `partId` 칸 추가
2. **기본 파트** 생성 (이름 "기본" — 의뢰인이 "일단 기본으로 두고 나중에 바꾸자"고 정함)
3. **이미 있던 레퍼런스를 전부 기본 파트에 넣기**

**3번이 특히 중요합니다.** 빼먹으면 예전 레퍼런스가 어느 파트에도 안 속해서
사이드바에서 뭘 골라도 안 보입니다. 실제로는 멀쩡히 있는데 **데이터가 사라진 것처럼**
보이는, 원인 찾기 가장 어려운 종류의 문제입니다.

**`if (from < 2)`로 적었습니다.** `== 1`로 적으면, 한참 업데이트를 안 하다가
1에서 곧장 3·4로 건너뛰는 사용자가 이 단계를 통째로 건너뜁니다.

### 마이그레이션을 테스트로 확인했습니다 (이 PR에서 가장 중요한 부분)

**개발하는 사람은 대개 데이터베이스를 새로 만드는 쪽입니다.** 그래서 마이그레이션이
잘못돼도 **자기 컴퓨터에서는 아무 문제가 없습니다.** 업데이트한 사용자만 겪습니다.
눈으로는 절대 못 잡는 종류라, 앞으로도 저장 구조를 바꿀 때는 반드시 테스트를 붙여야 합니다.

`test/data/migration_v1_to_v2_test.dart`가 **일부러 옛 구조(v1)의 진짜 sqlite
파일을 만들어놓고** 지금 앱으로 엽니다. 실제 업데이트 상황과 같습니다.

확인하는 것 여섯 가지:
- 앱이 **켜지는지** (안 켜지는 게 가장 흔한 실패)
- 예전 레퍼런스가 **그대로 있는지**
- 전부 **기본 파트에 들어갔는지**
- 원래 있던 **폴더는 안 건드려졌는지**
- 앱을 껐다 켜도 **기본 파트가 하나만** 있는지
- **기본 파트 이름을 바꿔뒀으면 되돌아가지 않는지**

마지막 것은 실수하기 쉽습니다. "없으면 만들기"(`insertOrIgnore`)를
"덮어쓰기"(`insertOnConflictUpdate`)로 적으면 앱을 켤 때마다 이름이 "기본"으로
되돌아가서, **고쳐지지 않는 버그**처럼 보입니다.

**겪은 문제**: 테스트에서 v1 표를 만드는 SQL이 `near "references": syntax error`로
실패했습니다. `references`가 **SQLite 예약어**라서 그냥 쓰면 안 되고 `"references"`로
감싸야 합니다. drift는 알아서 감싸주기 때문에 평소에는 드러나지 않습니다.

### 판단한 것들

**기본 파트만 번호를 정해뒀습니다.** 다른 항목은 만들 때마다 새 UUID를 뽑는데
(설계 원칙 1), 기본 파트는 미리 정한 값을 씁니다.
1. 코드가 "기본 파트"를 가리켜야 할 때가 있습니다 (새 레퍼런스를 어디 넣을지 등)
2. **나중에 동기화를 붙일 때 유리합니다.** 기기마다 다른 번호를 뽑았다면
   "컴퓨터의 기본 파트"와 "폰의 기본 파트"가 서로 다른 것이 되어 파트가 두 개로
   늘어납니다. 같은 번호면 저절로 같은 것이 됩니다.

**파트는 "조건"이 아니라 "지금 보고 있는 자리"로 다룹니다.**
- `clearAll()`이 **파트는 안 지웁니다.** "조건 지우기"를 눌렀다고 보던 자리에서
  튕겨 나가면 당황스럽습니다.
- `hasAnyFilter`가 **파트를 안 셉니다.** 빈 파트에서는 "조건에 맞는 게 없습니다"가
  아니라 "아직 아무것도 없습니다"가 맞는 안내입니다. 조건을 지워봐야 소용없으니까요.

**보고 있는 파트에 새 레퍼런스가 들어갑니다.** "파티클"을 보면서 사진을 넣었는데
기본 파트로 들어가면 **방금 넣은 것이 화면에서 곧바로 사라집니다.** 저장이 안 된 줄 압니다.
"전체"를 보는 중이면 기본 파트로 갑니다 — "전체"는 자리가 아니라 보기 방식입니다.

**편집 화면의 파트 항목을 폴더보다 위에 뒀습니다.** 큰 갈래에서 작은 갈래로
좁혀지는 순서가 자연스럽습니다.

### 지난 PR(#14)의 버그도 함께 고침

검색창을 필터 줄에서 머리줄로 옮기면서, **"조건 지우기"가 조건만 지우고 입력창
글자는 안 지우게** 됐습니다. 글자는 남아 있는데 목록은 다 나오는 어긋난 상태라
검색이 고장난 것처럼 보입니다. `_applyQuery()`에서 입력창을 맞춰주도록 고쳤습니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| **저장 구조를 또 바꿀 때** | `lib/data/app_database.dart`의 `onUpgrade` — 이번 것이 본보기 |
| 기본 파트 이름·번호 | `lib/models/taxonomy_item.dart` 맨 아래 |
| 사이드바 파트 목록 | `lib/widgets/app_sidebar.dart`의 `_buildPartList()` |
| 파트를 골랐을 때 | `lib/screens/home_screen.dart`의 `_selectPart()` |
| 새 레퍼런스가 어느 파트로 갈지 | 같은 파일 `_partIdForNewItems` |
| 필터 줄에서 파트를 빼는 곳 | `lib/widgets/reference_filter_bar.dart`의 `filterableTaxonomyKinds` |

### 어떻게 테스트했나
- `flutter analyze` 문제 없음, `flutter test` **317건 통과**(새로 15건),
  `flutter build windows` 성공
- `test/data/migration_v1_to_v2_test.dart` (6건) — 위의 마이그레이션 확인
- `test/screens/home_parts_test.dart` (9건) — 사이드바 표시, 파트별 거르기,
  **파트를 고른 채로 넣으면 그 파트에 들어가는지**, 파트 안에서만 검색되는지,
  **"조건 지우기"에도 파트가 유지되는지**, 파트가 필터 줄에 안 나오는지
- **의뢰인이 실제 앱에서 확인 완료** — 기존 레퍼런스 무사

**기존 테스트 두 개는 찾는 방법만 고쳤습니다.** 편집 화면에 드롭다운이 하나
늘면서 순서(`.first`)로 찾던 것이 어긋났습니다. 이제 **이름표로** 찾습니다
(`find.widgetWithText(..., '폴더')`). 앞으로 항목이 늘어도 안 깨집니다.

---

## PR #17 — 무드보드 1단계: 판 만들고 카드를 끌어서 배치하기 (저장 구조 v3)

4단계 무드보드의 **첫 조각**입니다. 판을 만들고, 레퍼런스를 올리고,
**끌어서 원하는 자리에 놓고**, 다시 내립니다.

목록은 컴퓨터가 정한 순서로 줄을 세웁니다. 무드보드는 반대로 **사람이 직접
늘어놓는 판**입니다. 색감이 비슷한 것끼리 모아두거나, 왼쪽엔 배경 오른쪽엔
인물처럼 의미를 담아 배치해볼 수 있습니다.

여섯 조각 중 1번입니다. 일부러 **이것만으로도 쓸 수 있게** 만들었습니다.
크기 조절·줌·스냅·다중선택·정렬툴바·캡처는 전부 "여기서 더 편해지는 것"입니다.

**들어가는 길**: 왼쪽 사이드바의 **무드보드** (파트 목록 바로 위)

### 저장 구조 v2 → v3

표를 **두 개** 새로 만듭니다. 기존 데이터는 손대지 않습니다.

| 표 | 담는 것 |
|---|---|
| `Boards` | 판 그 자체 (이름) |
| `BoardCards` | "어느 판에, 어느 레퍼런스가, 어디에(x, y) 놓였는가" |

**왜 둘로 나눴나**: 같은 레퍼런스를 여러 판에 올릴 수 있고 판마다 놓인 자리가
다릅니다. References 표에 x, y를 적으면 레퍼런스 하나당 자리도 하나뿐이라
판을 두 개 만들 수 없습니다.

**배치마다 고유 번호를 줍니다.** 태그 연결(`ReferenceTaxonomyLinks`)은
(레퍼런스, 태그) 짝이 하나뿐이라 짝 자체를 열쇠로 썼지만, 무드보드는
**같은 사진을 한 판에 두 번 올리는 것이 말이 됩니다** — 같은 색감을 좌우에
나란히 두고 비교하는 식으로요.

**빈 판을 자동으로 만들지 않습니다.** 파트 때(v2)는 이미 있던 레퍼런스를
기본 파트에 넣어줘야 했지만, 무드보드는 사용자가 만들기 전에는 하나도 없는
것이 맞습니다. 만든 적 없는 것이 목록에 있으면 오히려 헷갈립니다.

### 마이그레이션 테스트 — v1에서 v3으로 건너뛰는 경우

`CLAUDE.md`에 새로 못 박아둔 규칙대로 **옛 구조의 진짜 sqlite 파일을 만들어놓고**
지금 앱으로 엽니다(`test/data/migration_v2_to_v3_test.dart`).

**마지막 케이스가 특히 중요합니다** — **v1에서 v3으로 한 번에 건너뛰는** 경우입니다.
한참 업데이트를 안 한 사용자가 겪는 상황인데, 개발할 땐 늘 바로 전 버전에서만
올려보게 되어 놓치기 쉽습니다. `if (from == 2)`처럼 등호로 적어두면
**그런 사용자만** 앱이 안 켜집니다.

### 판이 스크롤되지 않습니다 (판 전체를 화면에 맞춰 줄여서 보여줍니다)

처음엔 스크롤로 만들었다가 **바꿨습니다.** 테스트가 문제를 잡아줬습니다.

1. **끌기가 스크롤에 졌습니다.** 카드를 끌려는 건지 판을 스크롤하려는 건지
   Flutter가 판단해야 하는데(제스처 아레나), 스크롤이 이겨서 **카드가 아예
   안 잡혔습니다.** 테스트가 없었으면 앱을 켜보고서야 알았을 문제입니다.
2. **카드를 잃어버릴 수 있었습니다.** 스크롤이면 카드가 화면 밖에 있을 수 있어서
   "분명히 올렸는데 안 보인다"가 생깁니다.

지금은 판(1920×1200)을 창 크기에 맞춰 **통째로 줄여서** 보여줍니다.
둘 다 사라졌습니다. 대신 창이 작으면 카드도 작아지는데, 크게 보는 것은
**2단계(줌·팬)**에서 붙입니다.

**좌표를 손으로 환산하지 않습니다.** `FittedBox`로 줄이면 Flutter가 화면의
손가락 위치를 판 좌표로 알아서 되돌려줍니다. 축소 비율을 나눠주는 계산이
코드 어디에도 없습니다.

### 판단한 것들

**저장은 손을 뗐을 때 한 번만.** 끄는 동안 매 순간 저장하면 1초에 수십 번
데이터베이스에 쓰게 되어 버벅입니다. 끄는 중에는 화면에서만 옮기고
`onPanEnd`에서 한 번 저장합니다.

**"저장" 버튼이 없습니다.** 손을 떼면 저장되므로 누를 것이 없습니다.
저장 버튼을 두면 언제 눌러야 하나 신경 쓰게 되고, 안 누르고 나갔다가
배치를 통째로 잃습니다.

**카드를 잡으면 맨 위로 올라옵니다.** 아래 깔린 카드를 꺼내려고 끌었는데
여전히 덮여 있으면 무엇을 잡았는지 안 보입니다.

**판 밖으로는 못 끌어냅니다.** 판 바깥은 그려지지 않는 자리라
**눈에 안 보이는데 지울 수도 없는 상태**가 됩니다.

**내리기는 ✕, 지우기가 아닙니다.** 판에서만 내려가고 레퍼런스는 목록에
그대로 남습니다. 휴지통 아이콘을 쓰면 사진이 영영 지워지는 줄 알고
누르기를 무서워합니다. **확인도 안 받습니다** — 되돌리기 쉬운 일에 매번
확인을 받으면 확인 창을 안 읽고 누르는 버릇이 들어, 정작 위험한 확인도
그냥 넘기게 됩니다.

**판을 지울 때는 확인받고, "레퍼런스는 남는다"고 분명히 적습니다.**
이 말이 없으면 판을 지우면 안에 있던 사진까지 사라지는 줄 알고 못 지웁니다.

**카드 높이는 비워둡니다(null).** "아직 안 정했으니 **그림 비율대로**"라는
뜻입니다. 0으로 두면 "높이가 0"인지 "안 정했다"인지 구분이 안 됩니다.
2단계에서 크기를 조절하면 그때 값이 채워집니다.

**새로 올린 카드는 늘 같은 자리에서 시작합니다.** 빈자리를 찾아 넣으려면
놓인 카드를 전부 살펴봐야 하는데, 많아질수록 느려지고 결과도 예측하기
어렵습니다. 겹치면 끌어서 옮기면 되는 일입니다.

**배치 계산은 화면에서 빼냈습니다.** `lib/utils/board_layout.dart`에 순수한
함수로 두면, 판 밖 막기나 새 카드 자리를 **앱을 띄우지 않고 숫자만으로**
확인할 수 있습니다.

### 나중에 이 부분을 고치려면 어디를 보면 되나

| 고치고 싶은 것 | 봐야 할 곳 |
|---|---|
| **판 크기 · 카드 기본 크기** | `lib/theme/app_metrics.dart` 맨 아래 / `lib/models/board.dart` 맨 아래 |
| 판이 화면에 어떻게 그려지는지 | `lib/widgets/board_canvas.dart`의 `build()` |
| 카드 한 장의 생김새 | `lib/widgets/board_card_view.dart` |
| **끌었을 때 무슨 일이 일어나는지** | `lib/screens/board_screen.dart`의 `_onDragStart` / `_onDragUpdate` / `_onDragEnd` |
| 판 밖으로 못 나가게 막는 곳 | `lib/utils/board_layout.dart`의 `clampToBoard` |
| 새 카드가 놓이는 자리 | 같은 파일 `initialCardPosition` |
| 판 만들기·이름 바꾸기·지우기 | `lib/screens/board_list_screen.dart` |
| 올릴 레퍼런스 고르는 창 | `lib/widgets/pick_references_dialog.dart` |
| **저장 구조를 또 바꿀 때** | `lib/data/app_database.dart`의 `onUpgrade` |
| 사이드바의 무드보드 자리 | `lib/widgets/app_sidebar.dart`의 `_buildBoardsBlock()` |

### 어떻게 테스트했나
- `flutter analyze` 문제 없음, `flutter test` **368건 통과**(317 → 368, 새로 51건),
  `flutter build windows` 성공
- `test/data/migration_v2_to_v3_test.dart` (5건) — v2→v3, **v1→v3 한 번에 건너뛰기**,
  두 번 열어도 판이 남는지
- `test/repositories/local_board_repository_test.dart` (16건) — 판·카드 저장,
  소프트 삭제, 겹침 순서, 장수 세기
- `test/utils/board_layout_test.dart` (11건) — 판 밖 막기, 새 카드 자리 계산
- `test/screens/board_screen_test.dart` (7건) — **끌어서 옮긴 자리가 저장되는지**,
  판 밖으로 못 나가는지, 잡으면 맨 위로 오는지,
  **카드를 내려도 레퍼런스는 남는지**
- `test/screens/board_list_screen_test.dart` (9건) — 만들기·이름 바꾸기·지우기,
  **지우기 전에 "레퍼런스는 남는다"고 알려주는지**
- `test/screens/app_shell_test.dart` (1건 추가) — 사이드바에서 무드보드로 가는지

**기존 테스트 8개는 인자 하나만 추가했습니다.** 앱을 만들 때 무드보드 저장소를
넘겨주게 되어서입니다.

### 알고 있는 한계 (다음 조각에서)
- 창이 작으면 카드도 작아집니다 → **2단계 줌·팬**
- 카드 크기를 못 바꿉니다 → **2단계**
- 여러 장을 한꺼번에 못 옮깁니다 → **4단계 마퀴 다중선택**
- 레퍼런스 고르는 창의 검색은 이미 읽어온 목록에서 거릅니다.
  수천 장이 되면 데이터베이스가 거르도록 바꿔야 합니다.

### 이 작업 중에 발견한 별개 문제 (아직 안 고침)
`LocalTaxonomyRepository.delete()`가 **파트를 지울 때 그 파트의 레퍼런스를
정리하지 않습니다.** 폴더·카테고리는 정리해주는데 PR #16에서 파트만 빠졌습니다.
파트를 지우면 그 안의 레퍼런스가 **사이드바 어느 파트에서도 안 보이게** 됩니다
("전체 레퍼런스"에는 남습니다). 별도 작업으로 처리할 것.

---

## PR #19 — Windows 세팅 문서에 개발자 모드 추가

**코드 변경 없이 `CLAUDE.md`만 고친 PR입니다.** PR #18(무드보드 2단계)보다
번호가 크지만 먼저 병합했습니다. 서로 겹치는 파일이 없습니다.

### 무엇을 왜 고쳤나

의뢰인이 노트북에서 데스크톱으로 작업 환경을 옮기면서, `CLAUDE.md`의
"새 컴퓨터에서 처음 시작할 때"를 그대로 따라갔습니다. 그런데 문서에 없던
곳에서 막혔습니다.

```
Got dependencies!
Building with plugins requires symlink support.
```

`flutter pub get`이 부품을 받는 데까지는 성공해놓고 **그다음 "부품을 이어
붙이는" 단계**에서 멈춥니다. Flutter는 Windows에서 플러그인을 연결할 때
**심볼릭 링크**(폴더 바로가기 같은 것)를 쓰는데, Windows는 보안상 이걸
아무나 못 만들게 막아둡니다. **개발자 모드**를 켜야 풀립니다.

```
start ms-settings:developers
```

**`flutter doctor`는 이 문제를 알려주지 않습니다.** 그래서 "doctor는 다
통과했는데 왜 안 되지"가 됩니다. NuGet의 `NUGET-NOTFOUND`와 성격이 똑같은
**오류 문구만 봐서는 원인을 짐작할 수 없는** 부류라 문서에 남겼습니다.

### 고친 곳

- **"새 컴퓨터에서 처음 시작할 때"에 4번 항목으로 추가.** 뒤 항목들을
  5~8번으로 밀었고, 본문의 "(아래 4번 참고)"도 5번으로 맞췄습니다.
- **"Windows 빌드 관련해서 알아둘 것"에도 한 줄.** 세팅하는 사람은 앞쪽을,
  이미 세팅해두고 빌드하다 막힌 사람은 뒤쪽을 봅니다. **두 군데 다** 있어야
  어느 쪽으로 들어와도 찾습니다.
- **Visual Studio 항목을 "Build Tools 또는 Community"로.** 기존 문서는
  Build Tools만 적어놔서, Community가 이미 깔린 컴퓨터에서도 Build Tools를
  또 까야 하는 줄 알게 됩니다. 실제로는 Community + "C++를 사용한 데스크톱
  개발" 워크로드로 `flutter doctor`를 통과했습니다.
- **로컬 clone 경로를 노트북 `D:\` / 데스크톱 `E:\` 둘 다 명시.**
  기존에는 `D:\`만 박혀 있었습니다.

### 어떻게 확인했나

데스크톱(Windows 11 25H2)에서 문서대로 처음부터 따라갔습니다.

| | 결과 |
|---|---|
| `flutter --version` | Flutter 3.47.1 stable |
| `flutter doctor` — Visual Studio | 통과 (Community 2022 17.14.37) |
| `flutter doctor` — Android | 실패 (문서대로 의도한 것) |
| 개발자 모드 **끄고** `flutter pub get` | **실패** — 이 PR이 다루는 문제 |
| 개발자 모드 **켜고** `flutter pub get` | 통과 |
| `flutter test` | **368개 전부 통과** |

### 알고 있는 한계
- **문서만 바뀌었으므로 앱 동작에는 영향이 없습니다.**
- Android 쪽은 여전히 검증 불가입니다(Android SDK를 일부러 안 깔았음).
- 이 문서는 **Windows 기준**입니다. macOS/Linux로 옮길 때는 또 다른
  걸림돌이 나올 수 있고, 그때 다시 적어야 합니다.

### 이 작업 중에 발견한 별개 문제 (아직 안 고침)
`update.md`에 **PR #7 항목이 두 번 들어가 있습니다** (본 파일에서 `## PR #7`
검색하면 두 군데가 나옵니다). 내용이 겹치므로 나중에 하나로 합쳐야 합니다.

---

## PR #18 — 무드보드 2단계: 확대·축소·이동과 카드 크기 조절

4단계 무드보드의 **두 번째 조각**입니다. 1단계에서는 판이 창에 맞춰 통째로
줄어든 채 고정이었고, 카드 크기도 올릴 때 정해진 그대로였습니다.
이제 원하는 곳을 크게 볼 수 있고, 카드도 원하는 크기로 바꿀 수 있습니다.

### 조작 방법

| 하고 싶은 것 | 방법 |
|---|---|
| 판 옮기기 | **빈 곳**을 끌기 |
| 확대·축소 | **마우스 휠**(커서 자리 기준) 또는 오른쪽 아래 ＋／－ |
| 원래대로 | 오른쪽 아래 **⛶ 판 전체 보기** |
| 카드 크기 | 카드에 마우스를 올리면 나오는 **오른쪽 아래 손잡이**를 끌기 |

### 스크롤을 또 안 썼습니다

PR #17에서 겪은 문제 그대로입니다. 스크롤을 쓰면 **끌기가 스크롤에 져서
카드가 안 잡힙니다.** 이번에는 **판을 옮기는 바닥을 카드보다 아래층에**
깔았습니다. 카드 위에서 시작한 끌기는 카드가, 빈 곳에서 시작한 끌기는
판이 가져갑니다. 겨룰 일 없이 "누구를 눌렀는가"로 갈립니다.

그래서 `board_canvas.dart`는 **판 바탕을 안 그립니다** — 바탕을 깔면
그게 클릭을 가로채서 아래층까지 안 내려갑니다.

### 확대는 가리키는 자리를 기준으로

왼쪽 위 기준으로 커지면 확대할 때마다 보던 곳이 화면 밖으로 밀려납니다.
계산은 `lib/utils/board_layout.dart`에 순수한 함수로 뒀습니다.
붙잡고 있어야 할 식은 하나뿐입니다.

```
화면 위치 = 이동(offset) + 판 위치 × 배율(scale)
```

축소는 "판 전체가 보이는 배율"에서 멈추고, 확대 상한은 3배입니다.

### 크기 조절은 가로세로 비율을 고정

무드보드는 그림을 있는 그대로 보려고 만든 판이라, 찌그러진 그림이 놓이면
판을 만든 이유가 없어집니다. 손잡이를 끌면 **가로로 끈 거리만** 봅니다.

**여기서 카드 높이가 처음으로 채워집니다.** 그 전까지는 비어 있었고
(= 그림 비율대로), 크기를 바꾸는 순간 값이 들어갑니다. 높이 칸은 PR #17에서
미리 만들어뒀으므로 **마이그레이션이 없습니다.**

### 파일 나누기

`board_screen.dart`가 435줄까지 늘어서, `CLAUDE.md`에 적어둔 대로
조작 부분을 빼냈습니다(435 → 340줄).

| 새 파일 | 하는 일 |
|---|---|
| `lib/widgets/board_viewport.dart` | 확대·축소·이동 |
| `lib/widgets/board_zoom_controls.dart` | 확대·축소 버튼 |
| `lib/utils/board_card_actions.dart` | 옮기기·크기 바꾸기·맨 위로 올리기 규칙 |
| `lib/services/reference_lookup.dart` | 레퍼런스를 한 번에 읽어 표로 (판과 고르는 창이 같은 일을 두 번 하고 있었음) |
| `lib/widgets/empty_state_message.dart` | "아직 없습니다" 안내 |

### 검토에서 찾아 고친 것 두 가지

병합 전 검토와 실제 앱 시험에서 나온 것입니다. **둘 다 테스트를 먼저
실패시켜 놓고 고쳤습니다.**

**1. 크기 조절이 판 아래쪽 끝을 안 봤습니다** (커밋 `a890302`)

`clampResizedCardWidth`가 가로는 판 오른쪽 끝으로 붙잡으면서 세로는
아무도 안 봤습니다. 비율 고정이라 가로를 키우면 세로도 따라 커집니다.
3:4 세로 사진을 최대(가로 960)까지 키우면 세로가 1278.5가 되어
판 높이 1200을 넘습니다.

문제는 **되돌릴 수 없다**는 점이었습니다. 크기 조절 손잡이는 카드의
오른쪽 아래에 있어서 카드와 함께 판 밖으로 나가고, 그러면 다시 잡아서
줄일 수가 없습니다. 위젯 테스트로 확인해보니 손잡이가 트리에는 남아
있는데 `hitTestable`에는 안 걸렸습니다. 크기는 손을 뗄 때 저장되므로
앱을 껐다 켜도 그대로입니다.

→ `clampResizedCardWidth`가 아래로 남은 자리도 함께 봅니다. 세로 여유를
비율로 나눠 가로로 환산한 뒤 가장 빡빡한 것을 따릅니다. 인자가 넷이
되어 이름 있는 인자로 바꿨고, **기본값은 일부러 안 줬습니다** — 두면
새로 부르는 곳에서 세로를 빠뜨려도 조용히 지나갑니다.

**2. 끌면 엉뚱한 카드가 따라왔습니다** (커밋 `4de55a3`)

의뢰인이 앱에서 직접 찾았습니다. 카드를 끌면 잡은 카드는 가만히 있고
다른 카드가 커서를 따라다녔습니다.

`board_canvas.dart`가 카드들을 **Key 없이** 그리고 있었습니다. 카드를
잡으면 그 카드가 목록 맨 뒤로 옮겨지는데(맨 위에 그리려고), Key가 없으면
Flutter는 화면 조각을 **순서로만** 짝지어서 목록이 `[가,나,다]` →
`[나,다,가]`로 바뀌는 순간 **끌기를 붙잡고 있던 0번 자리에 "나"가
들어옵니다.** 그 뒤의 움직임이 전부 "나"에게 갑니다.

```
가(잡은 것) x : 100.0     ← 안 움직임
나          x : 905.7     ← 이게 움직임 (700에서)
다          x : 1300.0
```

→ `Positioned`에 `ValueKey(card.id)`를 붙였습니다. 레퍼런스를 못 찾아
빈 상자를 돌려주는 쪽에도 붙였습니다. 안 붙이면 그 빈 상자 때문에 뒤
카드들의 순서가 밀려 같은 문제가 납니다.

**이건 PR #18이 만든 것이 아니라 PR #17부터 있던 문제입니다.** 기존 끌기
테스트가 **카드를 한 장만** 써서 못 잡았습니다 — 한 장짜리 목록은 순서를
바꿔도 그대로입니다.

### 테스트를 쓰면서 알게 된 것 (다음에도 걸릴 곳)

재현 테스트를 카드 세 장으로 썼는데 **통과해버렸습니다.**
`tester.drag`는 누르고-옮기고-떼기를 **화면을 다시 그리지 않고** 한 번에
합니다. 그래서 그 사이에 목록 순서가 바뀌지 않아 문제가 재현되지 않습니다.
실제 앱은 매 순간 다시 그립니다.

**끄는 도중의 상태 변화가 얽힌 문제는 `tester.drag`로 못 잡습니다.**
`tester.startGesture` + `moveBy` 사이사이에 `pump`를 넣어야 합니다.

### 어떻게 확인했나

- `flutter analyze` 문제 없음 · `flutter test` **421건 통과**(368 → 421)
- `flutter build windows` 성공, 실제 앱으로 눈으로 확인
- `test/utils/board_layout_test.dart` — 판 전체 배율, 배율 한계,
  가리킨 지점이 확대해도 같은 자리에 남는지, 판을 화면 밖으로 못 미는지,
  **크기 조절이 판 오른쪽·아래쪽 끝을 넘지 않는지**
- `test/utils/board_card_actions_test.dart` — 맨 위로 올리기, 옮기기,
  비율 고정, 크기 한계, **세로 사진을 키워도 판 밖으로 안 나가는지**
- `test/widgets/board_viewport_test.dart` — 처음엔 판 전체가 보이는지,
  확대·축소, "판 전체 보기"로 되돌아오는지, 빈 곳을 끌면 움직이는지
- `test/screens/board_screen_test.dart` — 손잡이를 끌면 저장되는지,
  **손잡이를 끈 뒤 다시 누를 수 있는지**(hitTestable),
  **여러 장이 놓여 있어도 잡은 카드만 움직이는지**

### 알고 있는 한계

- **폰에서는 카드 크기를 못 바꿉니다.** 손잡이(와 내리기 버튼)가 마우스를
  올렸을 때만 나타나는데, 터치에는 호버가 없습니다. `CLAUDE.md`의 플랫폼
  차이표에 적은 대로 **롱프레스**로 여는 방식이 필요합니다.
  지금은 Android 빌드 자체가 검증 불가라 미뤘습니다.
- **빈 곳을 한 번 끌면 그 뒤로 창 크기를 안 따라갑니다.** `_pan`이
  `_scale`에 값을 넣기 때문인데, `_scale`의 주석은 "**확대·축소**를 하면
  값이 들어간다"입니다. 끌기는 확대가 아닙니다. 게다가 판이 화면에 다
  들어와 있을 때는 끌어도 화면에서 아무 일이 안 일어나므로(가운데 고정),
  사용자는 원인을 짐작할 수 없습니다. ⛶를 누르면 돌아옵니다.
  (`lib/widgets/board_viewport.dart`의 `_pan`)
- 카드를 **줄 맞춰 놓기**는 아직 손으로 → **3단계 엣지·격자 스냅**
- 여러 장을 한꺼번에 못 옮깁니다 → **4단계 마퀴 다중선택**
- **확대한 상태는 기억되지 않습니다.** 판을 나갔다 오면 다시 전체 보기입니다.
  기억하려면 저장 구조를 또 바꿔야 해서 일부러 미뤘습니다.

---

## PR #20 — 무드보드 로드맵을 PureRef 대체 기준으로 다시 짜기

**코드 변경 없이 `CLAUDE.md`만 고친 PR입니다.** 다만 로드맵 순서가 바뀌므로
다음 작업에 바로 영향이 있습니다.

### 왜 고쳤나

의뢰인이 **PureRef 대체를 기준으로** 무드보드 추가 제안을 정리해 보내줬습니다.
그중 두 가지가 로드맵 자체를 바꿔야 하는 것이었습니다.

### 1. 프로젝트 목적에 판단 기준을 넣었습니다

지금까지 "개인용 아카이브"라고만 되어 있어서, 무드보드 기능을 넣을지 말지
고민될 때 **기준이 없었습니다.**

의뢰인이 실제로 하는 작업은 **AE·포토샵을 띄워놓고 그 옆에 레퍼런스를
참고하는** 일입니다. 그래서 무드보드는 "예쁘게 배치해보는 판"이 아니라
**작업 중에 옆에 띄워두는 도구**입니다.

앞으로는 **"PureRef를 안 켜도 되게 만드는가"** 로 판단합니다.

### 2. 무한 캔버스를 3번으로 앞당겼습니다 (스냅은 4번으로)

스냅 설계를 짜다가 드러났습니다. **"판 가장자리에 붙기"가 무한 캔버스로
가면 통째로 버려집니다.** 의뢰인이 스냅 범위를 고를 때 그것도 골랐는데,
설계를 이어가기 전에 판 크기 자체를 정해야 한다는 것이 그때 보였습니다.

4·5·6번(다중선택, 정렬 툴바, 내보내기)도 전부 판 크기를 전제로 쌓이므로
**미룰수록 비싸집니다.**

비용을 미리 세어봤습니다.

| | |
|---|---|
| 판 크기에 묶인 곳 | `board_layout.dart` 7군데(전부 순수한 함수), `board_viewport.dart` 6군데(전부 build 안). **그 외 없음** |
| 저장 구조 | **안 바뀝니다.** x, y가 이미 그냥 숫자라 음수도 10000도 들어갑니다 → **마이그레이션 없음** |
| 경계를 강제하는 것 | `clampBoardOffset`과 `clampToBoard` **둘뿐** |

PR #18에서 스크롤 위젯을 버리고 **이동·배율을 직접 계산하는 방식**으로 간
덕분에 이 정도로 좁아졌습니다. 스크롤 위젯을 썼다면 훨씬 컸을 것입니다.

### 3. 떠 있는 창(항상 위) — 되는 것을 확인했습니다

의뢰인이 짚은 대로 **이게 PureRef 대체 여부를 가르는 기준점**이라, 로드맵에
적기 전에 짧게 시험(spike)해서 되는지부터 봤습니다. **버리는 코드로
별도 브랜치에서 하고 통째로 지웠습니다.**

| 기능 | 메서드 | 결과 |
|---|---|---|
| 항상 위 | `setAlwaysOnTop()` | ✅ 다른 프로그램 위에 계속 떠 있는 것까지 눈으로 확인 |
| 창 테두리 없애기 | `setAsFrameless()` | ✅ API 있음 |
| 반투명 | `setOpacity()` | ✅ |
| **클릭 통과** | `setIgnoreMouseEvents()` | ✅ 기대 안 했던 것 |

`window_manager` 0.5.2, Windows/macOS/Linux 지원(모바일은 대상 아님 —
항상 위라는 개념이 없어서 맞습니다). Windows 빌드도 통과했습니다.

**`setAsFrameless()`는 일부러 실행하지 않았습니다.** 제목 표시줄이 사라지면
창을 닫을 수가 없어서 시험 도중 앱에 갇힙니다. API가 있다는 것만 확인했습니다.
정식으로 만들 때는 **창을 닫는 방법을 반드시 함께** 만들어야 합니다.

4단계 **8번**으로 넣었고, 플랫폼 차이표의 "플로팅 PiP 무드보드 창" 줄도
미지원 → 확인됨으로 고쳤습니다.

### 4. 나머지 제안은 버리지 않고 남겼습니다

새 절 **"무드보드 추가 제안"** 에 우선순위와 **비용**을 함께 적었습니다.

- **불투명도 / 자유 회전** — 둘 다 `BoardCards`에 칸 추가 =
  **저장 구조 v4 + 마이그레이션**. 각각 PR 하나짜리입니다.
  의뢰인은 불투명도를 "4단계 언저리에 끼워 넣기 좋다"고 보셨는데,
  실제로는 그만큼 나갑니다.
- **자유 회전은 스냅과 충돌합니다.** 돌아간 카드의 "왼쪽 가장자리"가
  무엇인지부터 정해야 하므로 **스냅 다음에** 와야 합니다.
- **판 배경색은 작습니다.** 무한 캔버스 작업 때 배경을 어차피 건드리므로
  그때 같이 넣는 것이 자연스럽습니다.

### 5. 확정한 것 둘

- **4단계 7번 "이미지 캡처"의 용도 = 팀 공유**(슬랙·노션에 올리기).
  그래서 화면에 보이는 부분이 아니라 **카드 전체가 들어간 한 장**이어야 합니다.
- **웹 전체 자동 추천은 채택하지 않습니다.** 로컬 우선 원칙과 충돌하고,
  리버스 이미지 검색은 유료 API가 필요하며, **붙여넣은 이미지는 출처 정보가
  없어서** 전제가 성립하지 않습니다. 대안(구글 이미지 검색 열기 버튼,
  6단계 유사도 엔진을 "내 아카이브 안에서 찾기"로)을 기록해뒀습니다.

### 어떻게 확인했나

문서만 바뀌므로 코드 동작에는 영향이 없습니다. `main`에서 `flutter test`
**421건 통과**를 확인했고, 시험 흔적이 `pubspec.yaml` / `pubspec.lock` /
`lib/main.dart`에 남지 않은 것도 확인했습니다.

### 알고 있는 한계
- 항상 위는 **Windows에서만** 확인했습니다. macOS/Linux는 패키지가 지원한다고
  적혀 있을 뿐 직접 보지 않았습니다.
- 클릭 통과는 **API가 있다는 것만** 확인했습니다. 실제로 마우스가 아래
  프로그램으로 넘어가는지는 정식으로 만들 때 확인해야 합니다.

---

## PR #21 — 무드보드 3단계: 판에서 경계 없애기 (무한 캔버스)

4단계 무드보드의 **세 번째 조각**입니다. 판의 크기 제한을 없앴습니다.

전에는 판이 **1920×1200 고정**이었습니다. 카드를 오른쪽으로 끌면 1700쯤에서
벽에 부딪혔고, 레퍼런스가 쌓이면 판을 새로 만드는 수밖에 없었습니다.
PureRef 대체가 목표라면 그건 아트보드지 무드보드가 아닙니다.

**저장 구조는 안 바뀌었습니다.** 카드의 x, y가 이미 그냥 숫자라서 음수든
10000이든 그대로 들어갑니다. **마이그레이션이 없습니다.**

### 왼쪽 위 모서리만 고정입니다 (일부러)

사방으로 무한하게 만들지 **않았습니다.** 카드는 (0, 0)보다 왼쪽·위로 갈 수
없고, 오른쪽·아래로만 끝이 없습니다.

**음수 자리를 허용하면 클릭이 안 닿기 때문입니다.** Flutter는 상자 바깥의
클릭을 자식에게 안 내려보냅니다. PR #18에서 카드가 판 아래로 삐져나갔을 때
손잡이가 화면에 보이는데도 안 눌렸던 것과 똑같은 문제입니다.

의뢰인이 "일부러 그렇게 만든 거냐"고 물어서, 고치는 두 가지 길(원점 옮기기 /
원점을 아주 멀리 두기)과 비용을 **`CLAUDE.md`의 4단계 남겨둔 것**에 적어뒀습니다.
**실제로 왼쪽 위 벽에 부딪히는지 써보고 정하기로** 했습니다.

### ⛶ 버튼의 뜻이 바뀌었습니다

**"판 전체 보기" → "카드 전부 보기"**

경계가 없어져서 "판 전체"라는 게 없습니다. 대신 지금 놓인 카드들을 전부
감싸는 범위를 그때그때 계산해서 거기에 맞춥니다.

**판에 끝이 없어도 카드를 잃어버리지 않는 유일한 이유가 이것입니다.**
없애면 안 됩니다.

축소 한계도 "판 전체가 보이는 배율"에서 **10% 고정**으로 바뀌었습니다.
그 기준이 사라졌기 때문입니다.

### 바뀐 이름들

| 전 | 후 |
|---|---|
| `boardWidth` / `boardHeight` | `minCanvasWidth` / `minCanvasHeight` (이제 최소치) |
| `clampToBoard` | `clampToCanvas` (왼쪽 위만 막음) |
| `clampBoardOffset` | `clampCanvasOffset` (가운데로 안 잡아끔) |
| `fitBoardScale` | `fitAllScale` (카드가 놓인 범위에 맞춤) |

`clampResizedCardWidth`는 판 끝을 안 봅니다. 떨어져 나갈 곳이 없어졌습니다.
판 바탕(테두리 있는 네모)도 안 그립니다. 경계가 없으니 그릴 테두리가 없습니다.

### 곁다리로 고쳐진 것

**"빈 곳을 끌어도 판이 안 움직인다"가 없어졌습니다.** 전에는 판이 화면보다
작으면 강제로 가운데에 두느라 끌기가 무시됐습니다. PR #18 검토에서 찾아
남겨뒀던 항목인데, 규칙이 "가운데 고정"에서 "화면에 조금이라도 걸쳐 있기만
하면 됨"으로 바뀌면서 저절로 풀렸습니다.

### 테스트를 쓰면서 잡은 버그 (다음에도 걸릴 곳)

처음에는 화면을 **매 순간** "카드 전부 보기"로 맞추게 만들었습니다.
그랬더니 **카드를 끌면 화면이 그 카드를 따라가서 제자리처럼 보였습니다.**
카드가 오른쪽으로 가면 범위도 넓어지고 거기에 다시 맞추니, 아무리 끌어도
안 움직였습니다.

**기존 테스트(`카드를 끌면 화면에서 옮겨진다`)가 잡았습니다.** 120픽셀
끌었는데 0이 나왔습니다.

이제 **처음 한 번만** 맞춥니다(`_ensureFitted`). 다시 맞추는 것은 ⛶ 버튼과
레퍼런스를 새로 담았을 때뿐입니다. 그 대가로 **창 크기를 바꿔도 다시 안
맞춰집니다.** 자동으로 맞추는 것과 카드를 끄는 것은 같이 갈 수 없습니다.

### 담은 카드가 안 보이는 문제

멀리 확대해서 보고 있으면 새로 담은 카드가 화면 밖에 생겨 "안 담겼나?"
싶어집니다. 담은 직후 `viewResetCount`를 올려 전체 보기로 되돌립니다.

### 파일을 나눴습니다

`board_layout.dart`가 303줄이 되어 300줄 기준에 걸렸고, 마침 성격이 둘로
갈려서 나눴습니다.

| 파일 | 하는 일 | 줄 수 |
|---|---|---|
| `lib/utils/board_layout.dart` | 카드가 **어디에 놓이는가** | 152 |
| `lib/utils/board_view.dart` (새로) | 그 판을 **어느 배율로 어디를 보는가** | 164 |

`board_view.dart`는 **카드가 뭔지 모릅니다.** 범위(`Rect`)만 받습니다.

### 어떻게 확인했나

`flutter analyze` 문제 없음 · `flutter test` **434건 통과**(421 → 434) ·
`flutter build windows` 성공

- `board_view_test.dart`(새로) — 멀리 떨어진 카드도 ⛶로 담기는지,
  가리킨 지점이 확대해도 남는지, 화면 밖으로 완전히 못 미는지
- `board_layout_test.dart` — 카드 범위, 그릴 자리가 카드를 따라 늘어나는지,
  왼쪽 위로만 막히는지
- `board_card_actions_test.dart` — 오른쪽·아래로 계속 갈 수 있는지,
  여러 번 끌어도 계속 나아가는지
- `board_viewport_test.dart` — **빈 곳을 끌면 진짜 움직이는지**,
  되돌리기 신호로 전체 보기가 되는지
- `board_screen_test.dart` — 실제 화면에서 옛 판 크기를 넘어 끌리는지

세로 사진을 한껏 키우는 테스트는 **⛶로 손잡이를 되찾는** 경로로 바꿨습니다.
판 끝이 없어져서 카드가 화면 밖으로 나갈 수는 있지만 ⛶가 되돌려줍니다.

### 알고 있는 한계
- 왼쪽 위 벽 (위에 적음)
- 확대·이동한 상태가 기억되지 않습니다
- 창 크기를 바꿔도 다시 안 맞춰집니다
- 카드가 아주 많아지면 느려질 수 있습니다.
  화면 밖 이미지 언로드는 **느려지는 것을 확인한 뒤에** 합니다
- 폰에서 카드 크기를 못 바꾸는 것은 그대로입니다

---

## PR #22 — 무드보드: 왼쪽 위 벽 없애기 (사방 무한)

PR #21에서 판에 끝을 없앴지만 **왼쪽 위 (0, 0)만 벽으로 남겨뒀습니다.**
음수 자리에서는 클릭이 안 닿기 때문이었습니다.

의뢰인이 "일부러 그렇게 만든 거냐"고 물었고, 실제로 써보니 **자주
부딪힌다**고 해서 없앴습니다. 이제 사방으로 무한합니다.

### 좌표를 옮기지 않았습니다

`CLAUDE.md`에 적어뒀던 방법은 "카드가 0 밑으로 가려 하면 모든 카드를
그만큼 밀고 저장한다"였습니다. 그러면 왼쪽으로 끌 때마다 **카드 전부를
데이터베이스에 다시 써야** 합니다.

대신 **카드 좌표는 음수여도 그대로 두고, 그리는 상자를 카드를 따라
움직이게** 했습니다(`boardCanvasRect`). 상자 안에서 보면 모든 카드가
0 이상이라 클릭이 정상적으로 닿습니다. 벽을 세웠던 유일한 이유가 그것이었습니다.

화면이 안 흔들리는 것은 계산이 상쇄되기 때문입니다.

```
화면 = 이동 + 원점×배율 + (카드 − 원점)×배율 = 이동 + 카드×배율
```

원점이 얼마든 카드의 화면 위치는 같습니다. **`board_viewport.dart`가
원점을 더하고 `board_canvas.dart`가 뺍니다. 둘 중 하나만 고치면 카드가
어긋납니다.**

저장은 지금과 같습니다. **끈 카드 하나만** 씁니다. 저장 구조도 안 바뀝니다.

### 무엇이 바뀌었나

| | |
|---|---|
| `boardCanvasSize` → `boardCanvasRect` | 크기만이 아니라 **어디서 시작하는지**도 돌려줍니다 |
| `clampToCanvas` | **삭제.** 막을 이유가 없어졌습니다 |
| `BoardCanvas` | `canvasOrigin`을 받아 카드를 (카드 − 원점)에 놓습니다 |
| `BoardViewport` | 상자를 (이동 + 원점×배율)에 놓습니다 |

### 테스트를 먼저 빨갛게 만들었습니다

벽이 실제로 있다는 것을 실패하는 테스트로 못박고 시작했습니다.

```
Expected: a value less than <0>
  Actual: <0.0>                   ← 왼쪽 벽에 막혔습니다

Expected: exactly one matching candidate
  Actual: Found 0 widgets ...     ← 음수 자리 카드가 클릭이 안 닿습니다
```

- 왼쪽·위로도 계속 끌 수 있다 — 저장된 좌표가 음수가 되는지
- **음수 자리로 간 카드도 다시 잡을 수 있다** — `hitTestable`.
  벽을 세웠던 바로 그 이유라 이게 핵심입니다.
- 왼쪽으로 끌어도 다른 카드는 제자리에 있다 — 원점 보정이 맞는지

**세 번째 테스트를 처음에 잘못 썼습니다.** `.last`로 카드를 찾았는데
카드를 잡으면 목록 맨 뒤로 옮겨져서 **엉뚱한 카드를 보고 있었습니다.**
PR #18에서 겪은 함정과 같은 것이라 `ValueKey`로 찾도록 고쳤습니다.
**앞으로도 무드보드 테스트에서 카드를 몇 번째냐로 찾지 마세요.**

`board_viewport_test`의 상자를 일부러 음수(-200, -100)에서 시작하게
바꿨습니다. 원점이 0일 때만 맞는 계산을 걸러내기 위해서입니다.

옛 벽 테스트 두 개(`판 밖으로는 끌어낼 수 없다`, `왼쪽 위로는 0에서
멈춘다`)는 지웠습니다. 이제 반대가 맞습니다.

### 어떻게 확인했나

`flutter analyze` 문제 없음 · `flutter test` **432건 통과** ·
`flutter build windows` 성공 · 의뢰인이 실제 앱에서 확인

### 알고 있는 한계
- 확대·이동한 상태가 기억되지 않습니다
- 창 크기를 바꿔도 다시 안 맞춰집니다 (⛶를 누르면 됨)
- 카드가 아주 많아지면 느려질 수 있습니다.
  화면 밖 이미지 언로드는 **느려지는 것을 확인한 뒤에** 합니다
- 폰에서 카드 크기를 못 바꾸는 것은 그대로입니다

---

## PR #23 — 무드보드 4단계: 카드가 서로 착 붙게 하기 (스냅)

4단계 무드보드의 **네 번째 조각**입니다. 카드를 가까이 가져가면 **착
붙습니다.** 손으로만 맞추면 3픽셀씩 어긋나서 줄이 안 맞고, 그걸 눈으로
잡아내기도 어렵습니다.

### 조작 방법 (2026-08-28 기본값을 뒤집은 뒤)

| 하고 싶은 것 | 방법 |
|---|---|
| **평소** | 자유롭게 움직입니다. 스냅이 안 걸립니다 |
| **줄 맞추기** | **Alt를 누른 채로** 끌면 그때만 착 붙습니다 |
| 격자에 맞추기 | 위쪽 막대 **⊞ 버튼**을 켠 다음, Alt를 누른 채로 |

### 붙는 자리 자체를 먼저 고쳤습니다

의뢰인이 처음 써보고 **"붙는 게 부자연스럽다, 바로 옆에 있는 것에 신경을
못 쓰는 느낌"** 이라고 했습니다. 재현해서 원인을 셋 찾았습니다.

**① 세로 스냅이 유령 선에 붙고 있었습니다.** 카드 높이는 저장돼 있지
않고 그림 비율이 정하는데, 판은 4:3이라고 어림잡고 있었습니다.

| 그림 (폭 220 기준) | 실제 높이 | 어긋남 |
|---|---|---|
| 4:3 | 165 | 0 |
| 3:2 | 147 | 18 |
| 세로(3:4) | 293 | **128** |

붙는 거리가 8픽셀인데 128픽셀이 어긋났습니다. → **카드가 그려진 뒤
자기를 재서 알려줍니다** (`board_card_view.dart`의 `onMeasured`,
`addPostFrameCallback`으로 다 그려진 뒤에 잽니다). 판은 이 값을
`_measuredHeights`에 모아두는데, **setState를 안 부릅니다** — 이 값은
스냅 계산에만 쓰이고 화면 생김새를 안 바꾸므로, 다시 그리라고 하면
되돌이가 생길 수 있습니다.

**② 거리 제한이 없어서 먼 카드가 옆 카드를 이겼습니다.** 3000픽셀
떨어진 카드가 바로 옆 카드보다 숫자상 더 가까우면 그쪽이 이겼습니다.
의뢰인이 말한 문제가 정확히 이것이었습니다. → **400픽셀(카드 두 줄쯤)
넘게 떨어진 카드는 후보에서 뺍니다** (`boardSnapNeighborRange`).

**③ 그래서 카드가 늘수록 아무 데서나 붙었습니다.** 임의의 자리 400곳
중 붙는 비율을 재보니 카드 12장에서 28%→18%, 40장에서 57%→38%로
떨어졌습니다.

### 그다음 기본값을 뒤집었습니다

붙는 자리를 고친 뒤에도 **"평소에 늘 스냅이 걸리는 게 방해"** 라는
의견을 받았습니다. 카드를 대충 늘어놓는 시간이 훨씬 많고, 줄을 딱 맞추고
싶을 때만 가끔 스냅을 켜는 편이 손에 맞았습니다.

`_snapEnabled`의 부정 하나만 뗐습니다.
`!HardwareKeyboard.instance.isAltPressed` → `HardwareKeyboard.instance.isAltPressed`.

기존 스냅 테스트들이 `tester.drag`만 쓰고 있어서 기본값이 뒤집히자
Alt를 안 눌러 두 개가 빨갛게 됐습니다. `withAltPressed` 도우미
(`sendKeyDownEvent`/`sendKeyUpEvent`)로 감쌌고, **"평소에는 가까이
있어도 안 붙는다"** 테스트를 새로 추가해 기본값이 정말 자유로운지
못박았습니다. 이게 없으면 나중에 누가 또 뒤집어도 아무도 못 잡습니다.

### 격자는 카드끼리 안 맞을 때만 씁니다

**이 순서가 중요합니다.** 반대로 하면 격자를 켜둔 동안 카드끼리는
절대 안 맞게 됩니다. 격자에 먼저 붙어버려서 카드 가장자리까지 갈 일이
없기 때문입니다. 테스트로 못박았습니다.

### 안내선

스냅은 눈에 안 보이는 기능이라, 선을 하나 그어서 무엇에 맞춰졌는지
보여줍니다. **격자에 붙었을 때는 안 그립니다** — 아무것도 없는 자리에
선이 뜨면 "저 선은 뭐지?" 하게 됩니다. **`IgnorePointer`로 감쌌습니다**
— 안 감싸면 안내선이 클릭을 가로채서 빈 곳 끌기가 죽습니다. 이 프로젝트
에서 반복해서 나온 함정이라 테스트도 붙였습니다.

### 크기 조절은 오른쪽 모서리만 붙습니다

크기 조절은 가로세로 비율을 고정합니다. 아래 모서리를 따로 붙이면
비율이 깨지므로 오른쪽만 봅니다. 아래를 맞추고 싶을 때는 **6번(정렬·
분배 툴바)** 몫입니다.

### 돌려주는 값이 바뀌었습니다

`moveCard` / `resizeCard`가 목록만이 아니라 **어디에 붙었는지**도 함께
돌려줍니다(`BoardCardsUpdate`). 화면이 안내선을 그리려면 그 정보가
필요합니다. 기존 테스트 14곳은 `.cards`를 붙이는 기계적인 수정입니다.

### 어떻게 확인했나

`flutter analyze` 문제 없음 · `flutter test` **460건 통과**(432 → 460)
· `flutter build windows` 성공 · 의뢰인이 실제 앱에서 확인

**중간에 두 가지를 실수했다가 고쳤습니다.**
- 중심선 스냅 테스트: 두 카드의 폭을 같게 잡았더니 왼쪽·가운데·오른쪽이
  동시에 맞아버려서 구분이 안 됐습니다. 폭을 다르게 했습니다.
- 아래쪽 끝 맞춤 테스트: 기준 카드의 가운데선과 똑같이 3픽셀 떨어져
  있어서 어느 쪽에 붙은 건지 구분이 안 됐습니다. 값을 다시 잡았습니다.

### 알고 있는 한계
- 격자를 켠 것은 기억하지 않습니다. 판을 나갔다 오면 꺼집니다.
- 여러 장을 한꺼번에 붙일 수 없습니다 → 5번(마퀴 다중선택)
- 폰에는 Alt가 없습니다. 터치에서 스냅을 켜는 방법은 아직 없습니다.
- **`board_screen.dart`가 436줄로 300줄 기준을 넘었습니다.**
  5번(마퀴 다중선택)을 시작하기 전에 조작 부분을 정리하기로 했습니다.
