# reference-archive-app — 프로젝트 메모리

이 파일은 Claude Code가 이 폴더에서 세션을 시작할 때 자동으로 읽는 "외부 기억 장치"입니다.
새 대화를 시작하기 전에 이 파일부터 읽고, 지금까지 뭘 했는지 파악한 뒤 이어서 작업하세요.

**역할 분담**
- 이 `CLAUDE.md` — 프로젝트 구조, 기술 결정, 워크플로우 같은 **절차적/구조적** 내용.
  구조가 바뀔 때만 갱신합니다.
- `update.md` — **PR 단위 변경 이력**. 무엇을 왜 어떻게 고쳤는지, 테스트 방법과 한계.
  PR이 병합될 때마다 여기에 추가합니다.

## 작업자에게 (가장 중요)

**의뢰인은 코딩을 이제 막 시작한 비개발자입니다.** 앞으로 이 앱을 직접 출시하고, 오류를
수정하고, 보안 점검까지 할 수 있게 되는 것이 목표입니다. 따라서 이 프로젝트에서는
"동작하는 코드"만큼 **"읽고 고칠 수 있는 코드"** 가 중요합니다.

- **모든 파일 상단에 그 파일이 무슨 역할인지 한국어 주석**을 답니다.
- **모든 함수 위에 그 함수가 무엇을 하는지 한국어 한 줄 주석**을 답니다.
  "왜 이렇게 했는지"가 자명하지 않은 곳(우회법, 플랫폼 제약 대응 등)은 이유도 적습니다.
- **영리하지만 읽기 어려운 코드보다, 길더라도 읽기 쉬운 코드**를 택합니다.
  한 줄에 여러 동작을 압축하지 말고 단계별로 풀어 씁니다.
- PR 본문에는 "무엇을 고쳤다"뿐 아니라 **의뢰인이 나중에 같은 부분을 고칠 때 어디를
  봐야 하는지**(파일명/함수명)를 적어줍니다.
- 새로운 개념(Provider, async/await, Stream 등)을 처음 도입할 때는 PR 본문에
  **그게 뭔지 2~3줄로 설명**해줍니다. 의뢰인이 그 PR에서 그 개념을 배우게 됩니다.
- 파일 하나가 300줄을 넘으면 나누는 것을 검토합니다.

## 저장소 정보
- GitHub: https://github.com/luseuss/reference-archive-app (소유자 계정: `luseuss`, public)
- 로컬 clone: 컴퓨터마다 다릅니다 — 노트북 `D:\reference-archive-app`, 데스크톱 `E:\reference-archive-app`.
  경로를 문서에 박아두지 말고 지금 열려 있는 폴더를 기준으로 삼으세요.
- **이전 웹앱 저장소: https://github.com/luseuss/reference-archive — 건드리지 않습니다.**
  새 앱이 완성될 때까지 의뢰인이 계속 쓸 현역 앱입니다.
  기능의 정확한 동작이나 엣지케이스가 궁금하면 그쪽 `CLAUDE.md` / `update.md`(PR #1~#32)를
  먼저 확인하세요. 특히 무드보드 스냅/그룹/정렬 동작, 유튜브 임베드 제약, 메모 리치텍스트 처리.

## 새 컴퓨터에서 처음 시작할 때

의뢰인이 **노트북과 데스크톱을 오가며** 작업합니다. 새 컴퓨터에서 이어받을 때 필요한 것들입니다.
(2026-08-27 데스크톱으로 옮김)

### 1. 저장소 받기

```
git clone https://github.com/luseuss/reference-archive-app.git
```

이 파일과 `update.md`가 함께 따라오므로, 새 컴퓨터의 첫 세션에서도 지금까지의 맥락을
그대로 읽을 수 있습니다.

### 2. 설치해야 하는 것

| 무엇 | 왜 | 확인 |
|---|---|---|
| **Flutter SDK** (stable, 3.47 이상) | 앱을 만들려면 | `flutter --version` |
| **Visual Studio 2022** (Build Tools 또는 Community) | Windows 앱으로 만들려면. 설치할 때 **"C++를 사용한 데스크톱 개발"** 워크로드를 반드시 고를 것. Community를 이미 쓰고 있다면 그 워크로드만 추가하면 됩니다 | `flutter doctor` |
| **NuGet** | 유튜브 재생에 쓰는 웹뷰 부품이 필요로 합니다 | 아래 설명 참고 |
| **개발자 모드** (Windows 설정) | 부품을 이어 붙일 때 심볼릭 링크가 필요합니다 | 아래 설명 참고 |
| **GitHub CLI** (`gh`) | PR을 여는 데 씁니다 | `gh --version` |

`flutter doctor`를 돌리면 빠진 것을 알려줍니다. **Android 항목이 빨간 것은 정상입니다**
(아래 5번 참고).

### 3. NuGet — 이걸 빠뜨리면 Windows 빌드가 실패합니다

`flutter_inappwebview`(유튜브 재생·미리보기용)가 Windows에서 NuGet을 필요로 합니다.
없으면 빌드가 `NUGET-NOTFOUND`로 죽습니다. 오류 문구만 봐서는 원인을 알기 어렵습니다.

```
winget install Microsoft.NuGet
```

**설치한 뒤 반드시 `flutter clean`을 한 번 돌리세요.** 빌드 도구가 "NuGet 없음"을
기억해두기 때문에, 설치만 하고 다시 빌드하면 똑같이 실패합니다.

### 4. 개발자 모드 — 이것도 빠뜨리면 빌드가 실패합니다

Windows 설정의 **개발자 모드**를 켜야 합니다. 꺼져 있으면 `flutter pub get`이
부품을 이어 붙이는 단계에서 이렇게 멈춥니다.

```
Building with plugins requires symlink support.
```

Flutter는 Windows에서 부품(플러그인)을 연결할 때 **심볼릭 링크**(폴더 바로가기 같은 것)를
씁니다. Windows는 보안상 이걸 아무나 못 만들게 막아두기 때문에, 개발자 모드로 풀어줘야 합니다.

```
start ms-settings:developers
```

이 명령이 설정 창을 바로 엽니다. **개발자 모드** 스위치를 켜고 경고창에 **예**를 누르면 됩니다.
재부팅은 필요 없습니다.

NuGet과 마찬가지로 **오류 문구만 봐서는 원인을 짐작하기 어려운** 부류라 적어둡니다.
(2026-08-27 데스크톱을 새로 세팅하면서 실제로 여기서 막혔습니다)

### 5. 일부러 설치하지 않은 것

- **Android SDK** (약 10GB). 의뢰인이 "Flutter + Windows 먼저"를 선택했습니다.
  폰에서 실제로 볼 준비가 됐을 때 설치합니다. 그전까지 **Android 빌드는 검증 불가**입니다.

### 6. 코드 생성기(build_runner)는 평소엔 안 돌려도 됩니다

`lib/data/app_database.g.dart`는 **저장소에 커밋되어 있습니다.** 그래서 clone 직후
바로 빌드됩니다. `dart run build_runner build`는 **표 정의(`lib/data/tables.dart`)를
고쳤을 때만** 돌리면 됩니다. (한 번에 5~7분 걸립니다)

### 7. 모아둔 레퍼런스는 git에 없습니다 (중요)

사진과 데이터베이스는 저장소가 아니라 **그 컴퓨터 안**에 있습니다.

```
%APPDATA%\com.luseuss\reference_archive_app
   ├─ reference_archive.sqlite   ← 레퍼런스·폴더·태그·무드보드
   ├─ images\                     ← 사진과 유튜브 썸네일
   └─ shared_preferences.json     ← 밝기 모드, 사용자 이름
```

기기 간 동기화는 아직 없습니다(6단계 이후 별도 논의). 다른 컴퓨터에서 **지금까지 모아둔
레퍼런스를 그대로 보려면 이 폴더를 통째로 복사**해 같은 경로에 넣으세요.

**복사하기 전에 앱을 끄세요.** 켜져 있는 동안 sqlite 파일을 복사하면 깨질 수 있습니다.

기능만 시험해볼 거라면 복사하지 않아도 됩니다. 빈 상태로 시작해서 사진 몇 장 넣으면 됩니다.

### 8. git 신원

전역 설정이 없는 컴퓨터라면 저장소별로 정해줘야 커밋이 됩니다.

```
git config user.name luseuss
git config user.email luseuss@users.noreply.github.com
```

`gh auth login`은 **의뢰인이 직접** 합니다. 계정 로그인은 작업자가 대신하지 않습니다.

## 프로젝트가 뭔지
"레퍼런스 아카이브" — 이미지·디자인 레퍼런스와 유튜브 영상을 모아서 정리하고,
무드보드로 배치해보는 **개인용 로컬 우선 아카이브 앱**.

기존 HTML/JS 웹앱(`app.html` 단일 파일 PWA)을 Flutter로 재구축하는 프로젝트입니다.
재구축 이유는 두 가지: (1) 브라우저에서 여는 "웹사이트"가 아니라 독립 실행되는 진짜 앱을
원함 (2) **폰에서도 쓰고 싶음** — 이게 Flutter를 고른 결정적 이유입니다.

### 무드보드는 PureRef를 대체하는 것이 목표입니다 (2026-08-27 확정)

의뢰인이 실제로 하는 작업은 **AE·포토샵을 띄워놓고 그 옆에 레퍼런스를 참고하며**
만드는 일입니다. 그래서 무드보드는 "예쁘게 배치해보는 판"이 아니라
**작업 중에 옆에 띄워두는 도구**여야 합니다.

이 기준이 없으면 판단이 흔들립니다. 무드보드 기능을 넣을지 말지 고민될 때는
**"PureRef를 안 켜도 되게 만드는가"** 로 판단하세요.

여기서 나오는 결론 두 가지:

- **판에 경계가 없어야 합니다**(무한 캔버스). 정해진 크기 안에서 확대·축소하는
  것은 "아트보드"이지 PureRef가 아닙니다. 레퍼런스가 쌓이면 판을 계속 새로
  만들게 되어 "한 판에 다 쏟아붓고 훑어보기"가 안 됩니다.
- **항상 위(always-on-top)가 사실상 필수입니다.** 다른 프로그램에 가려지면
  작업 중에 참고할 수가 없어서, 결국 PureRef를 다시 켜게 됩니다.

두 가지 모두 아래 개발 단계에 정식 항목으로 들어가 있습니다.

## 기술 스택
| 항목 | 결정 |
|---|---|
| 프레임워크 | Flutter |
| 언어 | Dart |
| 타겟 | Windows, macOS, Linux, iOS, Android (코드 하나로) |
| 로컬 DB | drift (사유는 아래) |
| 1단계 데이터 | 로컬 전용 (서버 없음) |
| 향후 | 동기화 서버 도입 예정 — 아래 설계 원칙 필수 준수 |

### 검토했으나 채택하지 않은 것
- **Python (Flask/FastAPI, pywebview)** — 데스크톱에 갇히거나 HTML/JS를 파이썬 껍데기에
  넣는 형태가 되어 복잡도만 늘어남.
- **Tauri / Electron** — 기존 코드 재사용률은 높지만 모바일 지원이 안 되거나 약함.
- **C# (WPF/WinUI)** — Windows 룩앤필은 최고지만 모바일이 사실상 불가능.
- **sqflite** (drift 대신) — 아래 "왜 drift인가" 참고.

### 왜 drift인가 (sqflite 대신)
1. **한 코드로 Windows/Android 둘 다** — sqflite는 데스크톱에서
   `sqflite_common_ffi` 같은 패키지를 따로 붙여야 하고 초기화 코드가 플랫폼마다 갈립니다.
   drift는 그 차이를 안으로 감춰줍니다.
2. **SQL 오타를 컴파일 시점에 잡아줌** — sqflite는 SQL이 그냥 문자열이라 오타가 나면
   앱을 실행해서 그 화면까지 가봐야 터집니다. drift는 코드를 생성하면서 미리 잡아주므로
   **비개발자가 디버깅하기 훨씬 쉽습니다.** 이게 이 프로젝트에서 가장 큰 이유입니다.
3. **Repository 패턴(원칙 3)과 잘 맞음** — 테이블 정의와 쿼리가 Dart 클래스로 나오기 때문에
   나중에 서버용 구현체로 갈아끼울 때 인터페이스 모양을 맞추기 쉽습니다.

대신 drift는 **코드 생성**을 씁니다. `lib/data/tables.dart`나 `lib/data/app_database.dart`,
`build.yaml`을 고친 뒤에는 아래를 실행해야 생성된 파일(`lib/data/app_database.g.dart`)이
갱신됩니다. 안 하면 "정의는 고쳤는데 앱은 그대로"인 상태가 됩니다.

```
dart run build_runner build
```

(인터넷에서 흔히 보이는 `--delete-conflicting-outputs` 옵션은 지금 쓰는 build_runner
버전에서 제거됐습니다. 붙이면 무시된다는 경고만 뜹니다.)

### Windows 빌드 관련해서 알아둘 것 (PR #9부터)

유튜브 재생용 웹뷰(`flutter_inappwebview`)가 들어오면서 챙길 것이 생겼습니다.

- **`nuget`이 필요합니다.** 빌드 중에 WebView2 SDK를 받아오는 데 씁니다.
  없으면 `error MSB3073: NUGET-NOTFOUND`로 실패합니다.

  ```
  winget install Microsoft.NuGet
  ```

  **설치 후 새 터미널을 열어야** PATH가 잡힙니다. 이미 한 번 실패했다면
  `build` 폴더에 "없음"이 캐시돼 있으므로 `flutter clean`도 해야 합니다.
- **개발자 모드가 켜져 있어야 합니다.** 꺼져 있으면 빌드가 아니라 그 앞의
  `flutter pub get`이 `Building with plugins requires symlink support.`로 멈춥니다.
  `start ms-settings:developers`로 켭니다. (위 "새 컴퓨터에서 처음 시작할 때" 4번)
- **다시 빌드하기 전에 앱을 완전히 닫아야 합니다.** 실행 중인 앱이
  `WebView2Loader.dll`을 붙잡고 있어서 `error MSB3027`로 실패합니다.
  터미널에서 `q`가 가장 깔끔합니다.
  (아래 `.bat` 파일들은 이걸 알아서 처리합니다)

### 앱 켜고 끄기 (의뢰인용)

터미널에 명령을 치는 것이 번거로워서 더블클릭으로 되게 만들어뒀습니다.

| 파일 | 언제 쓰나 |
|---|---|
| **`앱 만들기.bat`** | 코드가 바뀐 뒤 한 번. 앱을 만들고 **바탕화면에 바로가기**를 놓습니다. |
| **`개발용 실행.bat`** | 고친 것을 바로 확인할 때. 터미널이 함께 뜨고 `q`로 끕니다. |
| 바탕화면 바로가기 | **평소 쓸 때.** 그냥 프로그램처럼 열고 창을 닫아 끕니다. |

둘 다 **실행 중인 앱을 먼저 닫고** 시작합니다. 위에 적은 `MSB3027`을 자동으로 피하려는 것입니다.

바로가기를 만드는 부분은 `tools/make_shortcut.ps1`에 따로 뒀습니다.
배치 파일 안에 PowerShell을 욱여넣으면 따옴표가 겹쳐 알아보기 힘들어집니다.

### 데이터베이스 관련해서 반드시 지킬 것

- **`schemaVersion`을 올렸으면 `migration`에 할 일을 반드시 적을 것.**
  안 적으면 새로 설치한 사람은 멀쩡한데 **기존 사용자의 앱만 안 켜집니다.**
  놓치기 아주 쉬운 실수입니다. (`lib/data/app_database.dart`)
- **마이그레이션에는 반드시 테스트를 붙일 것.**
  개발하는 사람은 대개 데이터베이스를 새로 만드는 쪽이라, 마이그레이션이 잘못돼도
  **자기 컴퓨터에서는 아무 문제가 없습니다.** 눈으로는 절대 못 잡습니다.

  `test/data/migration_v1_to_v2_test.dart`와 `migration_v2_to_v3_test.dart`가
  본보기입니다. 옛 구조의 진짜 sqlite 파일을 만들어놓고 지금 앱으로 열어봅니다.
  최소한 이 다섯은 확인하세요.
  **앱이 켜지는지 / 기존 데이터가 남는지 / 새 칸이 알맞게 채워지는지 /
  두 번 열어도 중복이 안 생기는지 / 옛 버전에서 여러 단계를 건너뛰어도 되는지.**

  마지막 것이 특히 놓치기 쉽습니다. 개발할 땐 늘 **바로 전 버전**에서만
  올려보게 되는데, 실제 사용자는 한참 지나 1에서 곧장 3, 4로 건너뜁니다.
  그래서 `migration`은 반드시 **부등호**(`if (from < 3)`)로 적어야 하고,
  건너뛰는 경우를 테스트로 한 번 밟아봐야 합니다.

  (SQLite에서 `references`는 예약어입니다. 테스트에서 직접 SQL을 쓸 때는
  `"references"`로 감싸야 합니다. drift는 알아서 감싸줘서 평소엔 안 드러납니다)
- **새 칸을 추가할 때는 `nullable`로.** 이미 저장된 줄에는 값이 없기 때문입니다.
  값을 채우는 것은 마이그레이션이 하고, 구조상으로는 비어 있을 수 있어야 합니다.
- **`build.yaml`의 `store_date_time_values_as_text`는 건드리지 말 것.**
  저장 형식 자체를 바꾸는 설정이라, 이미 저장된 데이터가 있는 상태에서 바꾸면
  기존 시각 값을 못 읽습니다.
- **시각은 언제나 UTC.** drift는 기본적으로 시각을 현지 시각으로 돌려주기 때문에
  위 설정과 저장소의 `.toUtc()` 두 겹으로 막고 있습니다. 새 저장소 구현체를 만들 때도
  똑같이 해야 합니다.

## 반드시 지킬 설계 원칙 (동기화 준비된 로컬 우선)

나중에 서버를 얹을 때 데이터 마이그레이션이나 재작성 없이 넘어가기 위한 원칙입니다.
Notion, Linear, Obsidian 등이 쓰는 local-first 아키텍처 패턴과 같은 접근입니다.
**예외 없이 모든 모델에 적용합니다.**

1. **모든 엔티티에 UUID v4** — 레퍼런스, 메모, 폴더, 카테고리, 태그, 프로젝트, 씬, 그룹,
   카드 배치 전부. 배열 인덱스나 자동증가 정수로 식별하면 나중에 두 기기의 데이터를 합칠 때
   "서로 다른 기기에서 만든 3번 항목"이 충돌합니다.
2. **모든 엔티티에 `createdAt` / `updatedAt`** — **UTC로 저장**, 표시할 때만 현지 시각 변환.
   나중에 "어느 기기의 버전이 최신인가"를 판단하는 유일한 근거입니다.
3. **데이터 접근은 Repository 패턴으로 추상화** — UI가 로컬 저장소를 직접 읽고 쓰지 않습니다.
   1단계에선 `LocalXxxRepository` 하나만 만들고, 나중에 서버를 붙일 때
   `SyncedXxxRepository`로 **갈아끼우기만** 하면 화면 코드는 한 줄도 안 건드려도 됩니다.
4. **이미지/첨부는 UUID 기반 파일명** (`{uuid}.jpg`) — DB에는 **절대경로를 저장하지 말고**
   파일명(또는 상대경로)만 저장하고 실제 경로는 런타임에 조합합니다.
   기기마다 앱 데이터 폴더 위치가 다르고, 나중에 클라우드 스토리지로 옮길 때 매핑이 깨집니다.
5. **가벼운 변경 추적** — 거창한 변경 로그는 불필요. `updatedAt`만 정확히 갱신하면
   "마지막 동기화 이후 바뀐 항목"을 걸러낼 수 있습니다. 삭제는 즉시 지우지 말고
   `deletedAt`을 찍는 **소프트 삭제** — 안 그러면 "이 기기에서 지운 건지, 저 기기에서 새로
   만든 건지" 구분이 안 됩니다.

## 프로젝트 구조
```
lib/
  main.dart                  앱 시작점
  models/                    데이터 모델 (Reference, Scene, Placement...)
  repositories/              데이터 접근 추상화 (원칙 3)
    reference_repository.dart        인터페이스
    local_reference_repository.dart  로컬 구현
  services/                  이미지 처리, 유사도 계산 등
  screens/                   화면 단위
  widgets/                   재사용 UI 조각
  theme/                     색·모양 기본값 (아래 참고)
  utils/                     공용 헬퍼
```

### 화면 구조 (의뢰인이 목업으로 정해준 것)

의뢰인이 목업 이미지로 앞으로의 화면 구조를 정해줬습니다. **왼쪽 사이드바 + 오른쪽 본문**
2단이며, 지금까지의 "위쪽 막대 + 격자"에서 크게 바뀝니다.

```
┌──────────────┬────────────────────────────────────┐
│ ① 사용자      │ ④ 제목 · 검색창 · 추가 버튼 · 로고   │
│ ─ 무드보드 ─  ├────────────────────────────────────┤
│ ② 파트 목록   │ ⑤ 폴더 / 카테고리 / 프로젝트 선택    │
│              ├────────────────────────────────────┤
│ ③ 설정·로그인 │ ⑥ 레퍼런스 격자                     │
└──────────────┴────────────────────────────────────┘
```

**무드보드는 목업에 없던 자리**입니다. 4단계를 붙이면서 ①과 ② 사이에 한 칸
띄워 넣었습니다(PR #17). 파트는 "레퍼런스를 어떻게 나눠 볼까"이고 무드보드는
"레퍼런스로 무엇을 할까"라, 성격이 달라서 파트 목록에 섞지 않았습니다.

**거르는 단계가 셋으로 나뉩니다.**

| | 무엇으로 거르나 | 자리 |
|---|---|---|
| **파트** | 큰 갈래 (디자인/파티클…) | 사이드바 ② |
| **폴더·카테고리·프로젝트** | 그 안에서 더 좁히기 | 본문 위 ⑤ |
| **검색어** | 글자로 찾기 | 위쪽 ④ |

**의뢰인이 정한 것**
- **파트는 사용자가 직접 추가·삭제**합니다. (기존 웹앱은 3개 고정이었지만 바꿨습니다)
  폴더·태그와 같은 성격이므로 `TaxonomyKind`에 더해 분류 관리 화면을 그대로 재사용합니다.
  ✅ PR #16에서 구현. **필터 줄에는 일부러 안 넣었습니다** — 같은 것을 두 군데서
  고르면 헷갈립니다. (`filterableTaxonomyKinds`)
- 기존에 넣어둔 레퍼런스는 **기본 파트**로 들어갑니다. ✅ PR #16 마이그레이션에서 처리.
- **로그인 시스템은 아직 만들지 않습니다.** ①에는 사용자 이름만 보여줍니다.
- **설정(③)에 들어갈 것**: 밝은/어두운 모드, 만든 사람, 앱 버전. 그 이상은 지금 넣지 않습니다.
- **추가 버튼은 ④ 검색창 옆**에 둡니다. (지금의 떠 있는 버튼에서 옮김)

### 디자인은 기존 웹앱을 따릅니다

색과 모양은 새로 정하지 않고 **기존 웹앱 `app.html`의 CSS 값을 그대로** 씁니다.
의뢰인이 이미 그 모양에 익숙하고, 새 앱이 완성될 때까지 두 앱을 함께 쓰기 때문입니다.

- `lib/theme/app_palette.dart` — 색 값. 원본 CSS 변수를 밝은/어두운 모드 둘 다 옮겨 적었습니다.
- `lib/theme/app_metrics.dart` — 모서리 둥글기·여백·격자 크기.
- `lib/theme/app_text.dart` — 글자 크기·굵기. **색은 안 들어있습니다** (밝은/어두운 모드가
  달라서). 쓰는 곳에서 `copyWith(color: ...)`로 붙입니다.
- `lib/theme/app_theme.dart` — 위 셋으로 테마를 조립합니다.

**`ColorScheme.fromSeed`(대표색 하나로 자동 생성)를 쓰지 않습니다.** 편하지만 맞춰야 할
원본이 있을 때는 오히려 방해가 됩니다. 실제로 자동 생성 색은 배경이 푸른기 도는 회색이
되고 테두리 색이 아예 없어서, 원본의 따뜻한 느낌이 안 났습니다. (PR #11)

**화면 코드에서 색을 직접 정하지 마세요.** `AppPalette.of(context)`로 가져다 씁니다.
화면마다 색을 적어두면 나중에 앱 색을 바꿀 때 그 줄들만 안 바뀌어 튑니다.

## 개발 단계
각 단계가 끝날 때마다 **실제로 돌아가는 앱**이 나와야 합니다.

- **1단계 — 뼈대와 저장** ✅ 완료 (PR #1~#3)
  프로젝트 생성, 데이터 모델 + Repository, 로컬 저장(drift), 레퍼런스 추가/목록/삭제,
  이미지 자동 리사이즈(긴 변 1600px, JPEG q0.85 — 기존 앱과 동일), 다크모드
- **2단계 — 정리 체계** ✅ 완료 (PR #4~#6, #8)
  폴더/카테고리/태그/프로젝트 CRUD, 검색(디바운스), 즐겨찾기,
  핀 고정(정렬 무관 항상 상단), 정렬 옵션, 일괄 선택(폴더 이동 / 태그 추가 / 삭제)
- **3단계 — 유튜브** ✅ 완료 (PR #9~#10)
  링크 저장, 제목·썸네일 자동 수집, 앱 안에서 재생, 호버 미리보기(데스크톱만)
  기존 웹앱의 임베드 우회법(PR #26~#31)은 그대로 옮기지 않고 처음부터 Flutter 방식으로 구현
- **4단계 — 무드보드** ✅ 완료 (PR #17~#30, 1~8번 전부)
  자유 배치 캔버스, 무한 캔버스, 스냅, 마퀴 다중선택/그룹화,
  정렬·분배 툴바, 이미지 내보내기, 떠 있는 창

  **지금까지 중 가장 큰 작업이라 한 PR에 다 넣으면 검토가 불가능합니다.**
  아래 순서로 쪼갭니다.

  1. ~~씬 만들기 + 카드를 캔버스에 올리고 **드래그로 옮기기**~~ ✅ **PR #17**
  2. ~~크기 조절 + 줌·팬~~ ✅ **PR #18**
  3. ~~**무한 캔버스**~~ ✅ **PR #21** (+ 사방 무한 보완 **PR #22**)
  4. ~~**스냅** — 카드끼리 + 격자~~ ✅ **PR #23**
  5. ~~**마퀴 다중선택**~~ ✅ **PR #25**. **그룹화(저장되는 묶음)는 아직입니다** —
     선택은 판을 나갔다 오면 풀립니다. 여러 장을 계속 묶어두고 싶으면
     다음 조각에서 `groupId` 칸을 더하는 별도 작업이 필요합니다(저장
     구조 v5 마이그레이션). 5번 안에서 일부러 미뤘습니다.
  6. ~~**정렬·분배 툴바**~~ ✅ **PR #26**
  7. ~~**이미지 내보내기**~~ ✅ **PR #28**
  8. ~~**떠 있는 창** — 무드보드를 항상 위에~~ ✅ **PR #30**

  **3번(무한 캔버스)을 왜 앞으로 당겼나 (2026-08-27):**
  원래 스냅이 3번이었는데, 스냅 설계를 짜다가 **"판 가장자리에 붙기"가
  무한 캔버스로 가면 통째로 버려진다**는 것이 드러났습니다. 순서를 바꾸는
  쪽이 쌉니다. 4·5·6번도 전부 판 크기를 전제로 쌓이므로 미룰수록 비쌉니다.

  **3번(무한 캔버스)은 끝났습니다 — PR #21에서 이렇게 됐습니다:**
  - 판 크기는 이제 **최소치**입니다(`minCanvasWidth`/`minCanvasHeight`).
    카드가 퍼지면 `boardCanvasSize`가 따라 늘어납니다.
  - **저장 구조는 안 바뀌었습니다.** 마이그레이션 없음.
  - `clampToBoard` → 없앰. 카드는 어디로든 갈 수 있습니다(PR #22).
    `clampBoardOffset` → `clampCanvasOffset`(가운데로 안 잡아끔),
    `fitBoardScale` → `fitAllScale`(카드가 놓인 범위에 맞춤).
  - ⛶ 버튼이 **"카드 전부 보기"** 가 됐습니다. 판에 끝이 없어도 카드를
    잃어버리지 않는 유일한 이유입니다. **없애지 마세요.**
  - 계산 파일을 둘로 나눴습니다 — `board_layout.dart`(카드가 어디 놓이나),
    `board_view.dart`(어느 배율로 어디를 보나).
  **8번(떠 있는 창)은 이렇게 되어 있습니다 (PR #30):**
  - **이번엔 "항상 위" 토글 하나만 넣었습니다.** 창 테두리 없애기
    (frameless)·반투명·클릭 통과는 2026-08-27에 시험해서 되는 것까지는
    확인했지만(아래 표), 이번 PR에는 일부러 안 넣었습니다 — frameless는
    창을 닫는 버튼을 따로 만들어야 하는 등 손이 더 가서, 의뢰인이
    "항상 위 토글만"을 골랐습니다. 필요해지면 다시 꺼내 씁니다.

    | 기능 | 메서드 | 이번에 씀 |
    |---|---|---|
    | 항상 위 | `setAlwaysOnTop()` | ✅ |
    | 창 테두리 없애기 | `setAsFrameless()` / `setTitleBarStyle()` | 아직 |
    | 반투명 | `setOpacity()` | 아직 |
    | 클릭 통과 | `setIgnoreMouseEvents()` | 아직 |

    Windows/macOS/Linux만 지원합니다(모바일은 항상 위라는 개념이 없어서
    맞습니다). **나중에 `setAsFrameless()`를 켤 때는 창을 닫는 방법을
    반드시 함께 만드세요.** 제목 표시줄이 사라지면 앱에 갇힙니다.
  - **`lib/screens/board_window_controller.dart`**(새 파일)가 상태와
    동작을 담습니다. `BoardExportController`와 같은 `ChangeNotifier`
    패턴입니다.
  - **상태를 기억합니다.** 한 번 켜두면 앱을 다시 켜도 그대로 켜져
    있습니다. `shared_preferences`에 자체 키(`boardAlwaysOnTop`)로
    저장합니다 — `AppSettings`(밝기 모드·이름)에는 안 넣었습니다.
    설정 화면에는 이 값을 안 넣기로 했고(CLAUDE.md 위쪽 "설정에 들어갈
    것" 참고), 무드보드 화면 버튼으로만 켜고 끄는 창 자체의 성질이라
    따로 뒀습니다.
  - **창의 "항상 위" 상태는 운영체제가 기억해주지 않습니다.** 앱을 새로
    켤 때마다 꺼진 채로 시작합니다. 그래서 무드보드 화면을 열 때마다
    `BoardWindowController.load()`가 저장해둔 값을 읽어서 **다시
    적용**합니다(`windowManager.setAlwaysOnTop(...)`를 다시 부릅니다).
  - **데스크톱(Windows/macOS/Linux)에서만 버튼이 보입니다.** 폰·태블릿엔
    "다른 앱 위에 떠 있기"라는 개념 자체가 없어서, 여느 플랫폼 차이
    처리처럼(`home_screen.dart`의 `supportsHoverPreview`와 같은 방식)
    버튼을 통째로 숨깁니다(`supportsAlwaysOnTopWindow`).
  - **테스트에서는 이 값이 항상 거짓입니다** (`debugDefaultTargetPlatformOverride`로
    바꾸지 않는 한). 그래서 `BoardWindowController.load()`/`toggle()`이
    실제 `window_manager` 플러그인 통로를 부르지 않고 조용히 넘어가고,
    위젯 테스트가 오류 없이 돕니다. `board_window_controller_test.dart`가
    이 플랫폼 판정 자체를 확인하고, `board_screen_test.dart`는 "테스트
    환경에서는 버튼이 안 보이는지"만 확인합니다 — **데스크톱으로
    바꿔서 버튼을 실제로 눌러보는 테스트는 못 씁니다**(눌렀을 때 진짜
    플러그인을 부르려고 해서 테스트 환경에서 오류가 납니다). 실제로
    켜지는지는 `flutter run -d windows`로 눈으로 봐야 합니다.

  **다음 조각을 시작하기 전에 알아둘 것 (PR #17~#21에서 정해진 것들):**
  - 확대·축소·이동은 `lib/widgets/board_viewport.dart`, 카드 배치는
    `lib/widgets/board_canvas.dart`가 맡습니다. **스크롤 위젯을 쓰지 마세요.**
    처음에 그렇게 만들었다가 **끌기가 스크롤에 져서 카드가 아예 안
    잡혔습니다**(제스처 아레나). 지금은 판을 옮기는 바닥을 카드보다
    **아래층에** 깔아서 겨룰 일이 없게 만들어뒀습니다.
  - 그래서 `board_canvas.dart`는 **판 바탕을 안 그립니다.** 바탕을 깔면
    빈 곳의 클릭을 가로채서 판이 안 움직입니다.
  - **카드에는 반드시 `ValueKey(card.id)`를 붙여둘 것.** 카드를 잡으면
    목록 맨 뒤로 옮겨지는데(맨 위에 그리려고), Key가 없으면 Flutter가
    순서로만 짝지어서 **엉뚱한 카드가 끌려갑니다.** PR #18에서 실제로
    겪었습니다(자세한 내용은 `update.md`의 PR #18).
  - 계산은 `lib/utils/board_layout.dart`(자리·배율)와
    `lib/utils/board_card_actions.dart`(옮기기·크기 바꾸기 규칙)에
    **순수한 함수**로 모여 있습니다. 새 계산도 여기 두면 앱을 안 띄우고
    테스트할 수 있습니다.
  - **끄는 도중의 상태 변화가 얽힌 문제는 `tester.drag`로 못 잡습니다.**
    `tester.drag`는 누르고-옮기고-떼기를 화면을 다시 그리지 않고 한 번에
    합니다. `tester.startGesture` + `moveBy` 사이에 `pump`를 넣으세요.
    (`test/screens/board_screen_test.dart`의 '여러 장이 놓여 있어도...' 참고)

  **스냅은 이렇게 되어 있습니다 (PR #23):**
  - **카드끼리 붙는 것은 항상 켜져 있습니다.** 다른 카드의 왼쪽/가운데/
    오른쪽(가로), 위/가운데/아래(세로)에 맞춥니다. 자기 자신은 뺍니다.
  - **격자(20px)는 위쪽 막대 버튼으로 켜고 끕니다.** 켜도 **카드끼리 맞는
    것이 하나도 없을 때만** 쓰입니다. 순서를 반대로 하면 격자를 켜둔 동안
    카드끼리는 절대 안 맞게 됩니다. 격자에 먼저 붙어버리기 때문입니다.
  - **기본은 자유롭게 움직이고, Alt를 누르고 끌 때만 붙습니다**
    (2026-08-28에 뒤집음). 처음에는 반대로(평소 붙고 Alt로 끄기) 만들었는데,
    의뢰인이 써보니 "붙는 게 부자연스럽다"고 했습니다. 카드를 대충 늘어놓는
    시간이 훨씬 많고, 줄을 딱 맞추고 싶을 때만 가끔 스냅을 켜는 편이 손에
    맞았습니다. `HardwareKeyboard.instance.isAltPressed`로 봅니다 — 키
    입력 위젯을 따로 만들 필요가 없습니다. (`board_interaction_controller.dart`의
    `snapEnabled` — 조작 상태를 이 파일로 뺀 뒤 옮겨졌습니다)
  - **카드가 자기 실제 높이를 알려줍니다** (`onMeasured`). 이게 없으면
    세로 스냅이 **눈에 보이지도 않는 자리**에 붙습니다. 카드 높이는 보통
    저장돼 있지 않고 그림 비율이 정하는데, 4:3으로 어림잡으면 3:2 사진에서
    18픽셀, 세로 사진에서 128픽셀이나 어긋납니다. 붙는 거리가 8픽셀인데
    말입니다. **처음에 이걸 빠뜨려서 "붙는 게 부자연스럽다"는 말을
    들었습니다.** (`board_layout.dart`의 `boardCardHeight`)
  - **거리 제한(400)이 있습니다.** 없으면 3000픽셀 떨어진 카드가 바로 옆
    카드를 이깁니다 — 숫자만 보면 더 가깝기 때문입니다. 카드 40장일 때
    아무 자리에서나 **57%** 확률로 붙던 것이 38%로 떨어집니다.
    (`app_metrics.dart`의 `boardSnapNeighborRange`)
  - 붙는 거리는 **8**(판 좌표)입니다. 확대해서 보면 화면에서는 그보다 크게
    느껴집니다. 기존 웹앱도 확대가 있는 채로 같은 값을 썼습니다.
  - 붙으면 **안내선**이 잠깐 보입니다. **격자에 붙었을 때는 안 그립니다** —
    아무것도 없는 자리에 선이 뜨면 "저 선은 뭐지?" 하게 됩니다.
  - 안내선은 **`IgnorePointer`로 감싸야 합니다.** 안 감싸면 안내선이 클릭을
    가로채서 빈 곳 끌기가 죽습니다. (`board_guides.dart`)
  - **크기 조절은 네 모서리 어디서든 됩니다** (PR #27부터). 붙는(스냅) 것은
    잡은 손잡이가 있는 쪽 모서리(왼쪽 손잡이면 왼쪽, 오른쪽 손잡이면
    오른쪽)만 봅니다. 세로가 비율을 따라오기 때문에 위·아래 모서리를
    따로 붙이면 비율이 깨집니다. 위아래를 맞추고 싶을 때는 6번(정렬·분배
    툴바)을 쓰게 됩니다. 아래 "네 모서리 손잡이" 항목 참고.
  - 격자를 켠 것은 **기억하지 않습니다.** 판을 나갔다 오면 꺼집니다.
    기존 웹앱과 같습니다.

  **마퀴 다중선택은 이렇게 되어 있습니다 (PR #25, 조작법은 PR #34에서 바뀜):**
  - **빈 곳 끌기가 마퀴입니다.** 판 이동(마우스 휠 버튼으로 끌기)과 같은
    손잡이를 공유하고, `_onEmptyDragStart`에서 그 순간 **눌린 버튼이
    무엇이었는지**로 한 번만 가릅니다(`_emptyPointerButtons`). 끄는
    도중에 다른 버튼을 누르거나 떼도 이미 정해진 쪽으로 계속 갑니다 —
    안 그러면 마퀴가 판이 됐다 다시 마퀴가 됐다 하며 뒤죽박죽이 됩니다.
    (`board_viewport.dart`)
  - **처음엔 반대였습니다** — "빈 곳 끌기 = 판 이동, Alt+빈 곳 끌기 =
    마퀴". 의뢰인이 써보니 마퀴를 훨씬 자주 쓰는데 그게 곁다리(Alt)
    취급이라 불편했습니다. 그림 편집 프로그램들이 흔히 쓰는 배치로
    바꿨습니다(PR #34): **빈 드래그 = 선택, 마우스 휠 버튼 드래그 = 화면
    이동.** Alt는 여전히 스냅에만 씁니다 — 마퀴/판 이동과는 이제
    아무 관계가 없습니다.
  - **버튼을 가리려면 `RawGestureDetector`가 필요합니다.** 보통의
    `GestureDetector.onPanStart`는 **왼쪽 버튼만** 받습니다(Flutter의
    `PanGestureRecognizer` 기본 동작 — `_defaultButtonAcceptBehavior`가
    `buttons == kPrimaryButton`만 통과시킵니다). 휠 버튼 끌기까지
    받으려면 `PanGestureRecognizer(allowedButtonsFilter: ...)`를 직접
    만들어야 하고, `GestureDetector`는 그 값을 밖으로 안 열어주므로
    `RawGestureDetector`를 씁니다.
  - **선택·클릭 판정은 GestureDetector가 아니라 `Listener`로 봅니다.**
    처음에는 `onTapDown`/`onTap`을 카드·빈 곳의 기존 `GestureDetector`에
    같이 뒀는데, 그러면 **끌기 인식기가 탭 인식기와 경쟁하게 됩니다.**
    살짝만 끄는 경우(10픽셀 안팎) 탭이 이겨버려서 끌기 자체가 시작되지
    않는 회귀가 실제로 났습니다(제스처 아레나 문제 — `CLAUDE.md`가 PR
    #17부터 경고해온 바로 그것이 또 나왔습니다). `Listener`는 아레나에
    안 끼고 날것 신호만 보므로, 끌기 인식은 전혀 안 건드립니다.
    (`board_canvas.dart`의 카드 `Listener.onPointerDown`,
    `board_viewport.dart`의 빈 곳 `Listener.onPointerDown/Move/Up`)
  - **여러 장을 함께 끌 때는 기준 카드 하나만 스냅됩니다.** 각자 따로
    스냅되면 골라둔 카드들끼리 흩어집니다. 기준 카드의 보정값을 나머지
    전부에 그대로 더합니다. (`board_card_actions.dart`의 `moveCards`)
  - **그룹은 없습니다.** 선택은 저장하지 않고, 판을 나갔다 오면 풀립니다.
    저장되는 묶음(그룹화)은 5번에서 일부러 미뤘습니다(위 참고).
  - 카드를 몇 번째냐로 찾지 말라는 규칙(PR #18)이 테스트를 쓸 때 또
    나왔습니다. 화면 좌표를 구할 때 `find.byType(BoardCanvas)`가 아니라
    `find.byType(BoardViewport)`를 기준으로 삼아야 합니다 — `BoardCanvas`는
    카드 둘레 여백(`canvasBreathingRoom`)까지 포함한 큰 상자라 그 모서리가
    화면 밖으로 한참 벗어나 있습니다. (`test/screens/board_screen_test.dart`)

  **정렬·분배 툴바는 이렇게 되어 있습니다 (PR #26):**
  - **선택 6개 이상 아니고 2장 이상일 때만 눌립니다.** 정렬은 "여럿을
    나란히 맞추는" 동작이라 한 장으로는 뜻이 없습니다. 버튼을 눌러도
    아무 일이 안 일어나는 것보다, 처음부터 못 누르게(disabled) 하는
    편이 헷갈리지 않습니다. (`board_selection_bar.dart`)
  - **기준은 "선택 전체를 감싸는 범위"입니다.** 카드 하나를 골라 그것에
    맞추는 게 아니라, 선택된 카드들을 전부 감싸는 네모를 구해서 그
    왼쪽·가운데·오른쪽(또는 위·가운데·아래)에 맞춥니다. 카드를 하나
    더 고르거나 빼면 기준도 그만큼 넓어지거나 좁아져서 예측하기
    쉽습니다. (`board_align.dart`의 `alignSelectedCards`)
  - **크기 맞추기는 "맨 위 카드"를 기준으로 삼습니다.** Set의 삽입
    순서(옛 웹앱이 "마지막에 고른 카드"를 정하던 방식)는 Dart에서
    믿을 수 있게 재현하기 애매해서, 대신 **zOrder가 가장 큰 카드**로
    바꿨습니다. 카드는 누르기만 해도(끌지 않아도) 맨 위로 올라오므로
    (board_canvas.dart 설명 참고), **원하는 카드를 마지막에 한 번 더
    누르면**(선택은 유지된 채) 그 카드가 기준이 됩니다.

    **이 규칙을 테스트로 확인하다가 실제로 헷갈렸습니다** — A를 클릭,
    B를 Shift+클릭해서 둘 다 골라도 A가 먼저 눌렸으므로(Shift 없이)
    A가 맨 위가 되어 A가 기준이 됩니다. B를 기준으로 쓰려면 **B를 다시
    한 번 Shift 없이 눌러야** 합니다(이미 여러 장 선택된 상태에서 그중
    하나를 누르면 선택이 유지되는 규칙 덕분에 안전합니다).
  - **자리(x, y)는 안 건드립니다.** 크기 맞추기와 자리 맞추기는 다른
    일입니다. 한 번에 둘 다 하면 "왜 자리도 바뀌었지" 하고 놀랍니다.
  - 여러 장 저장은 5단계에서 만든 `saveCards`를 그대로 씁니다.
    새 저장 방식을 안 만들었습니다.
  - **저장 구조는 안 바뀌었습니다. 마이그레이션 없음.**

  **네 모서리 손잡이는 이렇게 되어 있습니다 (PR #27):**
  - 예전엔 오른쪽 아래 손잡이 하나뿐이었습니다. 이제 네 모서리
    (`BoardResizeCorner`: topLeft/topRight/bottomLeft/bottomRight) 전부에
    손잡이가 있고, **잡은 손잡이의 반대쪽 모서리가 고정된 채** 크기가
    바뀝니다. 오른쪽 아래를 끌면(예전과 같음) 왼쪽 위가 그대로 있고,
    왼쪽 위를 끌면 반대로 오른쪽 아래가 그대로 있습니다.
  - **가로세로 비율은 여전히 고정됩니다.** 이 앱은 "그림을 있는 그대로
    보려고" 만든 판이라, 어느 손잡이를 잡든 찌그러지면 안 됩니다.
    (`board_card_actions.dart`의 `resizeCard` 설명 참고)
  - **크기 맞추기(정렬·분배 툴바) 버튼은 안 바뀝니다.** 그 버튼은 여전히
    **왼쪽 위 모서리를 고정한 채** 크기만 기준 카드에 맞춥니다. 의뢰인이
    "왼쪽 위 고정, 지금과 같음"을 골랐습니다 — 버튼을 누르는 동작에는
    "어느 쪽에서 끄는지"가 없어서 네 모서리 규칙을 그대로 적용할 방법이
    없기 때문입니다.
  - **반대쪽 모서리를 구하려면 "손잡이를 잡던 순간의 자리"가 필요합니다.**
    `resizeCard`가 `startPosition`(자리)과 `startSize`(크기)를 함께
    받는 이유입니다. `board_interaction_controller.dart`의
    `onResizeStart`가 손잡이를 잡는 순간 카드의 x, y를 스냅샷으로 찍어둡니다.
  - **스냅은 잡은 쪽 모서리만 봅니다.** 왼쪽 손잡이를 끌면 왼쪽 모서리만
    다른 카드에 붙고, 오른쪽 모서리는 안 봅니다(`board_snap.dart`의
    `snapResizingCard`에 `onLeftEdge`를 추가했습니다). 세로(위·아래)는
    여전히 안 봅니다 — 비율 고정 때문입니다.
  - **내리기(×) 버튼을 제목 띠 안으로 옮겼습니다.** 예전엔 오른쪽 위에
    따로 떠 있었는데, 이제 오른쪽 위도 손잡이 자리라 겹칩니다. 제목 띠
    (카드 아래쪽)에 제목과 나란히 넣었습니다. (`board_card_view.dart`의
    `_buildTitleBar`)
  - **"골라짐" 표시(초록 체크)도 옮겼습니다.** 왼쪽 위 구석에 있었는데
    이제 그 자리도 손잡이입니다. 손잡이가 없는 **위쪽 가운데**로 옮겼습니다.
  - **손잡이마다 이름표(Key)가 있습니다** (`resize-handle-topLeft` 등).
    아이콘이 넷 다 똑같아서(`Icons.open_in_full`), 테스트에서 "어느
    손잡이를 끌었는지" 구분하려면 이름표가 필요합니다. 아이콘을 넷 다
    다르게 하지 않은 이유는, 이 프로젝트에서 최근(PR #26) 일부 아이콘이
    폰트에 없어서 안 보이는 문제를 겪었기 때문입니다 — 검증된 아이콘
    하나만 쓰는 편이 안전합니다.
  - 카드가 `minBoardCardWidth`(80)까지 작아져도 제목 띠 안의 글자·버튼이
    넘치지 않게 손잡이 자리만큼만 좌우를 비워둡니다. 넉넉하게 비워두면
    (예: 양쪽에 10픽셀씩 더) 가장 작은 카드에서 글자 상자가 손잡이보다
    커져 화면이 깨집니다 — 실제로 겪은 뒤 줄였습니다.

  **판이 사방으로 무한합니다 (PR #22에서 벽을 없앰):**
  처음에는 왼쪽 위 (0, 0)에 벽이 있었습니다. 음수 자리에서는 클릭이 안
  닿기 때문이었는데, 의뢰인이 써보니 **자주 부딪혔습니다.**

  고친 방법은 좌표를 옮기는 것이 **아닙니다.** 카드의 x, y는 음수여도
  그대로 저장하고, **그리는 상자를 카드를 따라 움직이게** 했습니다
  (`boardCanvasRect`). 상자 안에서는 모든 카드가 0 이상이라 클릭이 닿습니다.

  화면이 안 흔들리는 이유는 이 식이 상쇄되기 때문입니다.

      화면 = 이동 + 원점×배율 + (카드 − 원점)×배율 = 이동 + 카드×배율

  그래서 **카드 좌표를 다시 저장할 필요가 없습니다.** 끈 카드 하나만
  저장하면 됩니다. `board_viewport.dart`가 원점을 더하고
  `board_canvas.dart`가 빼는데, **둘 중 하나만 고치면 카드가 어긋납니다.**

  **이미지 내보내기는 이렇게 되어 있습니다 (PR #28):**
  - **화면에 보이는 부분이 아니라 카드 전체가 한 장에 들어갑니다.** 판에
    끝이 없어서 화면 밖 카드가 있을 수 있는데, 그것까지 전부 찍혀야
    "용도는 팀 공유"(CLAUDE.md 위쪽 참고)에 쓸 수 있습니다.
  - **화면 밖(사용자 눈에 안 보이는 아주 먼 곳)에 카드를 다시 한 번
    늘어놓고, 그것을 통째로 사진으로 뜹니다.** `RepaintBoundary`(Flutter
    기본 기능 — "이 안의 그림을 통째로 이미지로 달라")를 씁니다. 지금
    화면에 떠 있는 `BoardViewport`/`BoardCanvas`는 확대·축소·스크롤이
    섞여 있어서 그대로 쓸 수 없어, 내보내기 전용으로 아주 단순하게
    다시 그립니다. (`lib/services/board_exporter.dart`의
    `exportBoardImage`)
  - **카드는 `BoardCardView`를 그대로 재사용하되, 마우스를 올린 상태로
    두지 않습니다.** 그러면 제목 띠·손잡이·내리기 버튼 없이 저절로
    **그림만** 나옵니다 — 새 조건문을 안 만들어도 되는 이유입니다.
    사진첩처럼 깔끔한 모습이 되도록 의뢰인이 확인해준 방향입니다.
  - **사진을 찍기 전에 두 가지를 미리 해둡니다.**
    1. 카드가 쓸 사진 파일들을 `precacheImage`로 미리 읽어둡니다. 안
       그러면 아직 안 읽힌 사진이 하얗게 빈 채로 찍힙니다.
    2. 카드마다 실제로 그려질 높이를 미리 구합니다. 사용자가 크기를
       조절한 카드는 `card.height`에 정확한 값이 있지만, 안 바꾼
       카드는 그림의 원본 비율이 높이를 정하므로 사진 파일을 직접
       읽어 가로세로 비율을 구합니다(`ui.instantiateImageCodec`).
       평소 화면처럼 "일단 그려보고 재서 알아내는" 방식은 쓸 수
       없습니다 — 전체 상자 크기를 찍기 **전에** 미리 알아야
       하기 때문입니다.
  - **`Overlay.insert`로 화면 트리에 잠깐 끼워 넣습니다.** 화면에서 아주
    멀리 떨어진 자리(-10만 픽셀)에 그려서 눈에는 안 보이지만, Flutter는
    여전히 그 위젯을 그립니다(`IgnorePointer`로 감싸 실수로 클릭이
    닿는 것도 막습니다). 다 찍으면 반드시 치웁니다(`entry.remove()`,
    `try/finally`로 감쌉니다) — 안 치우면 안 보이는 위젯이 계속 화면
    트리에 남아있게 됩니다.
  - **저장은 `file_picker`의 저장 대화상자로 합니다.** 이미 이 프로젝트가
    가져오기(`importFromFilePicker`)에 쓰던 패키지라 새로 추가한 것이
    없습니다. Windows에서는 `FilePicker.saveFile(..., bytes: ...)`에
    사진 데이터를 같이 넘기면 file_picker가 고른 자리에 파일까지
    직접 씁니다(경로만 돌려주고 저장은 안 해주는 플랫폼도 있어서,
    쓰는 쪽 코드에서 따로 파일을 써야 하는 경우도 있다는 점은
    기억해두세요 — 다른 플랫폼을 붙일 때 다시 확인하세요).
  - **이 기능은 순수 함수로 못 뗍니다.** 실제로 Flutter가 그림을 그려봐야
    하는 일이라 화면 없이 테스트할 수 없습니다. 그래서 위젯 테스트는
    "버튼이 카드 없을 때 안 눌리는지"만 확인하고, **사진이 실제로 잘
    나오는지·저장이 되는지는 앱을 켜서 눈으로 확인**했습니다
    (`update.md` 참고). 저장 대화상자를 실제로 여는 것도 위젯 테스트
    안에서는 확인하지 않습니다 — 진짜 운영체제 창을 띄우려고 해서
    테스트가 끝없이 멈추기 때문입니다.
  - **저장 구조는 안 바뀌었습니다. 마이그레이션 없음.**

  **4단계에서 남겨둔 것 (순서와 상관없이 언젠가):**
  - **확대·이동한 상태가 기억되지 않습니다.** 판을 나갔다 오면 다시
    전체 보기입니다. 기억하려면 저장 구조를 또 바꿔야 해서 미뤘습니다.
  - **창 크기를 바꿔도 화면이 다시 안 맞춰집니다.** 처음 한 번만
    맞추기로 했기 때문입니다(그렇게 안 하면 카드를 끌 때 화면이 카드를
    따라가서 제자리처럼 보입니다). ⛶를 누르면 됩니다.
    (`lib/widgets/board_viewport.dart`의 `_ensureFitted`)
  - **폰에서는 카드 크기를 못 바꿉니다.** 크기 조절 손잡이와 내리기 버튼이
    **마우스를 올렸을 때만** 나타나는데 터치에는 호버가 없습니다.
    플랫폼 차이표에 적은 대로 **롱프레스**로 여는 방식이 필요합니다.
    (`lib/widgets/board_card_view.dart`의 `isRaised`)
  - 카드가 아주 많아지면 느려질 수 있습니다. 화면 밖 이미지 언로드는
    **느려지는 것을 확인한 뒤에** 합니다. 미리 최적화하지 마세요.
- **5단계 — 메모(스티키노트)** ✅ 완료 (PR #31)
  리치텍스트, 글자색/형광펜/배경색, 서식, 링크, 목록, 정렬

  **5단계(메모)는 이렇게 되어 있습니다:**
  - **`flutter_quill` 패키지**로 편집기를 만들었습니다. 요구된 기능
    (색·형광펜·굵게/기울임/밑줄·링크·목록·정렬)을 패키지가 기본
    제공하는 툴바로 보여줍니다. 저장 형식은 Delta(JSON) 문자열이고,
    `memo` 칼럼 타입은 그대로 TEXT입니다 — 안에 들어가는 내용의 뜻만
    "순수 글자"에서 "서식이 붙은 JSON"으로 바뀌었습니다.
    **정렬 버튼만은 예외입니다** — `QuillSimpleToolbar`의 기본값이
    `showAlignmentButtons: false`라서 아무 설정 없이 쓰면 정렬 버튼
    자체가 안 나타납니다. `lib/widgets/rich_memo_editor.dart`에서
    `QuillSimpleToolbarConfig(showAlignmentButtons: true)`로 켜줘야
    합니다. **새 flutter_quill 기능을 추가할 때는 "기본값이 꺼져
    있을 수 있다"를 항상 의심하세요** — 패키지 문서만 보고 "당연히
    될 것"이라 넘겨짚으면 이런 게 조용히 빠집니다.
  - **저장 구조 v4**로 기존 순수 텍스트 메모를 최소 Delta로 감쌌습니다
    (`lib/data/app_database.dart`의 `_upgradeToVersion4`). 표 구조
    자체는 안 바뀝니다(`addColumn` 없음) — 있는 `memo` 값을 다시
    써주는 것이 전부입니다.
  - **Delta ↔ 순수 텍스트 변환은 `lib/utils/rich_text_memo.dart`**에
    순수 함수로 모여 있습니다. 편집기(`lib/widgets/rich_memo_editor.dart`의
    `RichMemoEditor`)와 목록 카드 미리보기(`lib/widgets/reference_card.dart`)
    둘 다 이 파일의 `documentFromMemo`/`memoFromDocument`/
    `plainTextFromMemo`를 씁니다.
  - **`flutter_quill`의 툴바는 Flutter 로컬라이제이션 설정이 없으면
    바로 죽습니다 — 계획에서 예상 못 한 부분이라 여기 남겨둡니다.**
    `QuillSimpleToolbar`가 내부적으로 `FlutterQuillLocalizations`를
    찾는데, 이 앱의 `MaterialApp`(`lib/main.dart`)에는 원래 로컬
    라이제이션 델리게이트 자체가 없었습니다(지금까지 필요한 적이
    없었습니다). 그래서 `flutter_localizations`를 `dependencies`로
    옮기고(SDK 패키지라 `dev_dependencies`가 아닙니다),
    `MaterialApp`에 `localizationsDelegates`
    (`FlutterQuillLocalizations.delegate` + Flutter 표준 델리게이트 셋)와
    `supportedLocales`(`ko`, `en`)를 추가해야 했습니다. **새 패키지가
    자기만의 로컬라이제이션을 요구하면, `MaterialApp`에 델리게이트가
    없는지부터 의심하세요** — 에러 메시지만 봐서는 원인을 짐작하기
    어려운 부류입니다(NuGet·개발자 모드와 같은 사정).
  - **검색은 손대지 않았습니다.** Delta JSON 안에도 실제 글자가 그대로
    부분 문자열로 남아있어서 대체로 계속 찾아집니다. 문제가 실제로
    나타나면 그때 고칩니다.
  - **무드보드용 가벼운 주석은 이번에 포함하지 않았습니다.** 레퍼런스
    메모와는 저장 구조가 다른 별도 기능이라, "무드보드 추가 제안"
    표에 그대로 남겨뒀습니다.
  - **의뢰인이 실제 앱에서 서식·색·목록·링크·정렬 버튼을 직접 눌러보는
    확인은 아직입니다.** 자동화 테스트로는 "버튼 하나하나가 실제로
    먹는지"를 잡을 수 없어서(위젯 테스트가 못 잡는 부류 — 아래 계획
    문서 참고), 병합 전에 의뢰인이 직접 켜서 확인해야 합니다.
- **6단계 — 유사도 엔진** ✅ **완료** (PR #32 — 엔진 + 정렬 옵션, PR #35 — 상세
  화면 섹션) 태그 Jaccard + 카테고리/폴더/프로젝트 보너스 + dHash 퍼셉추얼
  해시. 목록 정렬 메뉴의 "유사한 것끼리"와, 레퍼런스 상세 화면 아래쪽의
  "비슷한 레퍼런스" 묶음 둘 다 있습니다.

  **"비슷한 레퍼런스" 상세 화면 섹션은 이렇게 되어 있습니다 (PR #35):**
  - **문턱값 0.12, 최대 6장** — 기존 웹앱(`reference-archive` 저장소)의
    `PREVIEW_SIMILAR_THRESHOLD`/`renderPreviewSimilar`와 같은 값입니다.
    이보다 낮은 점수는 "우연히 태그 하나 겹친" 수준이라 추천으로 보여줄
    만큼 의미 있지 않다고 봅니다. (`lib/widgets/similar_references_section.dart`)
  - **화면을 열 때 한 번만 계산합니다.** 편집하는 동안(태그를 고를 때마다)
    다시 계산하지 않습니다 — 목록이 계속 바뀌면 산만하고, "저장을 누르기
    전까지는 반영 안 함"이라는 이 화면의 원칙과도 어긋납니다.
  - **비슷한 레퍼런스를 누르면 그 레퍼런스의 상세 화면을 위에 쌓습니다**
    (push, 대신하지 않음). 유튜브 재생 버튼과 같은 방식입니다 — 뒤로
    가면 원래 보던 레퍼런스로 그대로 돌아오고, 고치던 내용도 안 사라집니다.
  - **레퍼런스 전부를 읽어와 계산합니다.** "비슷한가"는 SQL로 표현할 수
    없어서(모든 레퍼런스를 서로 견줘봐야 함), 후보 전부를 손에 들고 있어야
    합니다. `ReferenceLookup`(레퍼런스 전부 + 그림 경로를 한 번에 구하는
    도구, 무드보드 판·레퍼런스 고르는 창과 같은 것)을 그대로 재사용했습니다.
    개인 아카이브 규모에서는 부담이 없다고 보고 일부러 단순하게 뒀습니다
    — 나중에 레퍼런스가 수천 장이 되어 느려지면 그때 최적화하세요.

  **6단계(엔진 + 정렬)는 이렇게 되어 있습니다:**
  - **점수 계산 가중치와 dHash 방식은 새로 정하지 않고 예전 웹앱에서
    그대로 가져왔습니다** (`reference-archive` 저장소의 PR #12). 태그
    자카드 유사도 × 0.35, 같은 카테고리/폴더/겹치는 프로젝트 각각 +0.1,
    pHash 해밍거리 기반 점수 × 0.35 — 숫자를 새로 정하려면 "몇 번 써보고
    감으로 맞춰야" 하는데, 예전 앱에서 이미 검증된 값이 있으니 그걸
    믿고 옮겼습니다. (`lib/utils/similarity.dart`의 `jaccard`,
    `hammingDistance`, `similarityScore`, `similarItems`,
    `sortBySimilarity` — 전부 순수 함수라 화면 없이 테스트했습니다)
  - **사진이 얼마나 닮았는지는 dHash(퍼셉추얼 해시)로 봅니다.** 사진을
    가로세로 비율을 무시하고 강제로 9×8 픽셀 흑백으로 뭉갠 뒤, 옆
    픽셀끼리 밝기를 비교해서(**왼쪽이 더 밝으면 1, 아니면 0**) 64비트
    숫자 하나로 요약합니다. 완전히 같은 사진이 아니어도 **비슷하게
    생긴 사진은 비슷한 숫자가 나옵니다** — 그 숫자끼리 몇 비트가
    다른지(해밍거리)로 "얼마나 닮았는지"를 잽니다.
    (`lib/services/image_hash.dart`의 `dHashFromBytes`, `left > right ? '1' : '0'`)

    **dHash 테스트 기대값을 고치며 배운 것**: 계획에 적힌 테스트가 "왼쪽/
    오른쪽 반씩 다른 색으로 칠한 두 그림은 해밍거리가 30 넘게 나야 한다"고
    가정했는데, 실제로 만들어보니 최대 8비트밖에 다르지 않았습니다.
    구현이 틀린 게 아니라 **테스트가 잘못 가정한 것**이었습니다 — dHash는
    "옆 픽셀과 비교해 밝아지는지 어두워지는지"만 기록하는데, 세로 경계선
    하나로 반을 가른 밋밋한 그림은 **한 행마다 그 경계선을 지나는 한
    칸에서만 비교 결과가 바뀝니다.** 8행이니 정보를 담은 비트는 딱
    8개뿐이고, 그래서 이론상 나올 수 있는 최대 해밍거리도 정확히
    8입니다 — 30을 기대한 것부터가 셀 수 있는 정보량을 넘어선
    요구였습니다. **나중에 dHash 테스트용 그림을 만들 때는 "단색 덩어리로
    채운 그림은 그레이디언트 정보가 거의 없다"는 것을 기억하세요** —
    실제 사진처럼 결이 있는 그림이어야 해밍거리가 크게 벌어집니다.
  - **`pHash` 칼럼은 이미 있었습니다.** schemaVersion 1부터 있던 칸이라
    **저장 구조 변경도 마이그레이션도 없습니다.** 이번 작업은 그 칸을
    "채우는 방법"만 두 가지 만들었습니다.
    1. **새로 들여오는 레퍼런스**는 저장하는 순간 이미 메모리에 있는
       이미지 바이트로 바로 계산합니다(`lib/services/reference_importer.dart`의
       `saveYoutube()`/`_saveImageBytes()`). 파일을 다시 읽지 않습니다.
    2. **예전부터 있던 레퍼런스**(pHash가 비어있는 것들)는 앱을 켤 때
       화면 뒤에서 조용히 훑어 채웁니다(`lib/services/phash_backfill.dart`의
       `backfillMissingPHashes`, `lib/screens/home_screen.dart`의
       `initState()`에서 결과를 기다리지 않고 던져둡니다). 파일이 없거나
       깨져서 실패하면 그냥 건너뛰고 다음에 앱을 켤 때 다시 시도합니다 —
       개인 아카이브 규모라 이 정도로 충분합니다.
  - **정렬 메뉴에 "유사한 것끼리"가 생겼습니다**
    (`lib/models/reference_query.dart`의 `ReferenceSortOrder.similar`).
    화면 쪽은 손댈 게 없었습니다 — 정렬 메뉴가 `ReferenceSortOrder.values`를
    그대로 읽어 그리기 때문입니다(`lib/widgets/reference_filter_bar.dart`).
    **완전한 클러스터링(끼리끼리 여러 무리로 묶기)이 아닙니다.**
    `sortBySimilarity`(`lib/utils/similarity.dart`)는 목록에서 **가장
    최근에 추가된 항목 하나를 기준점**으로 고정하고, 나머지 전부를 그
    기준점과의 유사도 내림차순으로 한 줄 세웁니다(기존 웹앱과 같은
    단순한 방식). 기준점 자체가 맨 앞에 옵니다.
  - **핀 고정은 유사도 정렬에서도 항상 맨 위입니다.** SQL은 "이 레퍼런스와
    저 레퍼런스가 얼마나 비슷한가"를 표현할 수 없어서, 이 정렬만은
    데이터베이스가 아니라 **Dart 쪽에서** 다시 정렬합니다
    (`lib/repositories/local_reference_repository.dart`의 `search()`).
    일단 `createdAt DESC`로 가져온 다음, 핀 고정된 것과 아닌 것을
    나눠서 **핀 고정 안 된 부분만** `sortBySimilarity`로 다시 섞습니다.
    핀 고정된 부분은 그대로 두기 때문에 "핀 고정은 정렬과 무관하게 항상
    맨 위"라는 기존 규칙이 깨지지 않습니다.
  - **의뢰인이 실제 앱에서 확인하는 것은 아직입니다.** 정렬 메뉴에
    "유사한 것끼리"가 실제로 나타나는지, 태그가 겹치는 레퍼런스나
    비슷하게 생긴 사진들이 실제로 가까이 모이는지는 위젯 테스트로
    확인할 수 있는 부류가 아니라서(5단계와 같은 사정), 병합 전에
    의뢰인이 직접 켜서 눈으로 봐야 합니다.
- **그 이후(별도 논의)** 동기화 서버, 공유 스페이스/협업

### 단계 밖 작업: 레퍼런스 편집을 대화상자로 (별도 PR — "reference-detail-dialog")

의뢰인이 옛 웹앱 스크린샷("레퍼런스 추가" 대화상자)을 보여주며, 지금
레퍼런스를 편집할 때 화면 전체를 덮는 것이 아니라 저 스크린샷처럼
**가운데에 뜨는 작은 대화상자**로 나오게 해달라고 요청해 만든 기능입니다.

- **화면 너비로 갈립니다.** 넓을 때(데스크톱, 사이드바가 늘 펼쳐지는
  기준인 `sidebarBreakpoint`=900 이상)는 대화상자로, 좁을 때(폰)는
  지금까지처럼 화면 전체로 엽니다. 고정폭 대화상자를 좁은 화면에 그대로
  띄우면 여백만 남고 오히려 쓰기 불편해지기 때문입니다(CLAUDE.md
  플랫폼 차이표와 같은 판단 기준).
- **`lib/screens/reference_detail_screen.dart`에 `showReferenceDetailDialog()`
  함수가 새로 생겼습니다.** 화면을 열던 곳(`home_screen.dart`의
  `_openDetail`)이 이제 `Navigator.push`를 직접 부르지 않고 이 함수를
  부릅니다 — 너비를 재서 대화상자(`showDialog`)로 열지 새 화면
  (`Navigator.push`)으로 열지는 이 함수가 알아서 정합니다.
- **`ReferenceDetailScreen`에 `isDialog`(기본값 거짓) 값이 하나
  늘었습니다.** 참이면 `Scaffold`+`AppBar` 대신 `AlertDialog`(고정폭
  560, 높이는 화면의 80%)로 그립니다. 안의 내용(제목·메모·분류·비슷한
  레퍼런스)은 그대로라 두 모습 다 같은 `_buildForm()`을 씁니다.
- **"비슷한 레퍼런스"를 눌러 들어가는 화면은 일부러 그대로 뒀습니다**
  (`_openSimilar`는 여전히 `Navigator.push`만 씁니다). 이미 대화상자
  하나가 떠 있는 위에 또 고정폭 대화상자를 겹치는 것보다, "더 깊이
  들어가는 것"은 화면 전체로 보여주는 편이 자연스럽다고 판단했습니다.
- 직접 `ReferenceDetailScreen`을 만드는 곳(테스트 등)은 `isDialog`를
  안 넘기면 예전과 똑같이 화면 전체 모습 그대로입니다 — 기존 테스트가
  코드 수정 없이 그대로 통과합니다.
- **`test/screens/home_reference_detail_dialog_test.dart`**(새 파일)가
  두 갈래(넓은 화면=대화상자, 좁은 화면=화면 전체)를 각각 확인합니다.

### 밀린 정리거리

기능을 붙이다 보면 파일이 커집니다. 지금 알고 있는 것:

- ~~이미지 가져오기 부분 분리~~ ✅ **PR #14에서 완료**
  (`lib/services/reference_importer.dart`)
- **`lib/screens/home_screen.dart`가 1280 → 1049줄로 줄었지만 여전히
  300줄 기준 초과** (별도 PR — "home-screen-cleanup"). 여러 장 고르기와
  호버 미리보기를 board_interaction_controller.dart와 같은
  `ChangeNotifier` 패턴으로 뺐습니다.
  - **`lib/screens/home_selection_controller.dart`**(새 파일) — "여러 장
    고르기" 모드의 상태(`isSelecting`, `selectedIds`)와 동작(토글/전체선택/
    폴더 이동/태그 추가/삭제, 확인 대화상자 포함)이 여기 있습니다.
    대화상자를 띄우는 동작은 `board_export_controller.dart`의 `export()`와
    같은 모양으로 결과(`BulkActionOutcome`)만 돌려주고, 스낵바 문구는
    화면이 고릅니다.
  - **`lib/screens/home_hover_preview_controller.dart`**(새 파일) — 유튜브
    카드 호버 미리보기의 상태(`previewingItemId`, `previewUrl`)와
    동작(타이머 대기, 임시 서버 켜고 끄기)이 여기 있습니다.
  - `home_screen.dart`는 두 컨트롤러를 `ListenableBuilder`
    (`Listenable.merge`)로 감싸 다시 그리기만 합니다.
  - 순수 리팩터라 기존 위젯 테스트(특히 `home_bulk_select_test.dart`의
    여러 장 고르기 시나리오 12개, `home_hover_preview_test.dart`)가 그대로
    통과하는 것으로 검증했습니다(회귀 없음).

  **다음에 여러 장 고르기나 호버 미리보기를 고칠 때는 이 두 컨트롤러를
  보세요.**

  **`home_screen.dart`가 1049 → 938줄로 한 번 더 줄었습니다** (별도 PR —
  "home-screen-grid-cleanup"). 격자와 드롭 영역을 뺐습니다.
  - **`lib/widgets/reference_grid.dart`**(새 파일) — 레퍼런스를 메이슨리
    격자로 늘어놓는 `_buildGrid()`를 통째로 옮겼습니다. 상태 없이 값과
    콜백만 받는 `StatelessWidget`입니다.
  - **`lib/widgets/home_drop_area.dart`**(새 파일) — 화면 전체를 "끌어다
    놓을 수 있는 영역"으로 감싸던 `_buildDropArea()`를 옮겼습니다. **이때
    `_isDragging` 상태 자체도 함께 옮겼습니다** — 그 상태를 쓰는 곳이 이
    위젯 하나뿐이라, `home_screen.dart`가 아예 몰라도 되게
    `StatefulWidget`으로 통째로 뺐습니다(hover_lift.dart와 같은 방식).
  - 두 파일 다 `lib/widgets/`에 있지만 `lib/screens/home_hover_preview_controller.dart`의
    `supportsHoverPreview`를 그대로 가져다 씁니다 — `board_toolbar_actions.dart`가
    `board_interaction_controller.dart`를 가져다 쓰는 것과 같은 기존
    선례를 따랐습니다(`lib/widgets/`가 `lib/screens/`의 것을 가져다
    쓰는 것 자체는 이 프로젝트에서 이미 있던 방식입니다).
  - 순수 리팩터라 기존 위젯 테스트가 코드 수정 없이 그대로 통과하는
    것으로 검증했습니다(회귀 없음).

  **`home_screen.dart`가 938 → 861줄로 한 번 더 줄었습니다** (별도 PR —
  "home-screen-appbar-empty-cleanup"). 고르기 모드 위쪽 막대와 "레퍼런스
  없음" 안내를 뺐습니다.
  - **`lib/widgets/home_selection_app_bar.dart`**(새 파일) — 고르는
    중일 때 나오는 위쪽 막대(`_buildSelectionAppBar`)를 옮겼습니다.
    상태 없이 값(`selectedCount`/`totalCount`)과 콜백만 받습니다.
  - **`lib/widgets/reference_empty_state.dart`**(새 파일) — 레퍼런스가
    하나도 안 보일 때의 안내(`_buildEmptyState`)를 옮겼습니다. 상태
    없이 `isFiltered` 하나와 콜백 하나만 받습니다.
  - 순수 리팩터라 기존 위젯 테스트가 코드 수정 없이 그대로 통과하는
    것으로 검증했습니다(회귀 없음).

  **`home_screen.dart`에 남은 것(필터바·사이드바 조립, 검색·가져오기·
  탐색 로직)은 여기서 멈춥니다.** `board_screen.dart`가 321줄에서
  멈춘 것과 같은 이유입니다 — 남은 코드는 "화면을 읽어오고 조립하는"
  일 자체라, 더 잘게 쪼개면 오히려 따라가기 어려워집니다. 우선순위도
  낮습니다.
- ~~`lib/screens/board_screen.dart`가 436줄이라 300줄 기준 초과~~ ✅ **완료**
  (5번을 시작하기 전에 처리). "조작" 부분(끌기·크기 조절·스냅 상태와
  동작 전부)을 `lib/screens/board_interaction_controller.dart`
  (`ChangeNotifier`)로 뺐습니다. `board_screen.dart` 436 → **265줄**,
  컨트롤러 **287줄**. 순수 리팩터라 기존 위젯 테스트 460건이 그대로
  통과하는 것으로 검증했습니다(회귀 없음).

  카드 목록(`cards`)도 함께 옮겼습니다 — 끌기·크기조절뿐 아니라 레퍼런스
  담기·내리기도 같은 목록을 건드리기 때문입니다. 화면에는 대화상자를 여는
  `_addCards`만 남았고, 저장하고 목록에 넣는 부분은 컨트롤러의
  `addCards()`가 합니다.

  **다음에 무드보드 조작을 고칠 때는 이 컨트롤러를 보세요.**
  `board_screen.dart`는 이제 읽어오기·화면 조립만 합니다.
- **`board_screen.dart`는 정리했습니다** (8번을 시작하기 전, 별도 PR —
  "board-screen-export-refactor"). 397줄이던 것을 **321줄**로 줄였습니다.
  `_isExporting` 상태와 `_exportBoardImage()`를
  `lib/screens/board_export_controller.dart`(새 파일,
  `BoardInteractionController`와 같은 `ChangeNotifier` 패턴)로 뺐고,
  위쪽 AppBar 버튼 세 개(격자·내보내기·담기)를
  `lib/widgets/board_toolbar_actions.dart`(새 파일)로 뺐습니다.
  순수 리팩터라 기존 위젯 테스트 499건이 그대로 통과하는 것으로
  검증했습니다(회귀 없음).

  **321줄이라 300줄 기준을 여전히 살짝 넘습니다.** 남은 것은 화면을
  읽어오고 조립하는 코드 자체라, 더 잘게 쪼개면 오히려 따라가기
  어려워질 것 같아 여기서 멈췄습니다. 굳이 더 뺄 곳을 찾자면
  `_loadBoard`/`_addCards`를 "읽기 전용 화면 진입점" 클래스로 한 번 더
  뺄 수는 있지만, 지금은 우선순위가 낮습니다.
- **`board_viewport.dart`가 531 → 540줄로 오히려 조금 늘었지만
  내용은 정리됐습니다** (별도 PR — "board-viewport-cleanup"). 마퀴·클릭
  판정 부분을 뺐는데도 줄 수가 준 게 아니라 는 이유: 이 파일은
  `ChangeNotifier` 패턴이 아니라 **작은 값 보관 클래스 둘**로 뺐고,
  그 클래스들의 문서 주석이 새 파일에 새로 붙어서 총량은 늘었습니다 —
  다만 `board_viewport.dart` 자기 자신은 마퀴 관련 필드·메서드가 짧은
  위임 코드로 줄었습니다.
  - **`lib/widgets/board_viewport_gestures.dart`**(새 파일) —
    `EmptyPointerGesture`(빈 곳을 눌렀다 뗄 때까지 클릭인지 끌기인지,
    어느 버튼이었는지 가리는 값)와 `MarqueeState`(마퀴 네모가 화면
    어디에 있는지)를 담습니다.
  - **`ChangeNotifier`로 만들지 않았습니다.** 다른 컨트롤러들과 달리
    이 둘은 `board_viewport.dart` 한 파일 안에서만 쓰는, 화면과 무관한
    순수 상태 보관함입니다. 그 파일의 State가 이미 pan·zoom 때문에
    `setState`를 쓰고 있어서, 여기서 또 다른 "다시 그려라" 체계를
    두면 오히려 헷갈립니다 — **언제 다시 그릴지는 여전히
    `board_viewport.dart`가 `setState`로 정합니다.**
  - **판 좌표 변환(`_scaleFor`/`_offsetFor`/`_toCanvasPoint`)은
    옮기지 않고 `board_viewport.dart`에 그대로 뒀습니다.** `MarqueeState`는
    화면 좌표만 알고, 판 좌표로 바꾸는 일은 여전히 `_onEmptyDragUpdate`가
    합니다 — 미리 남겨둔 주의사항(배율·이동 값을 공유하는 문제)대로
    처리했습니다.
  - 순수 리팩터라 기존 위젯 테스트(마퀴 다중선택·판 이동 시나리오
    포함)가 그대로 통과하는 것으로 검증했습니다(회귀 없음).
- **`board_interaction_controller.dart`(567줄)는 아직 손대지 않았습니다.**
  뺄 후보는 이전 메모(정렬·선택 관련 메서드) 그대로입니다.

  **`board_interaction_controller.dart`가 567 → 546줄로 줄었습니다**
  (별도 PR — "board-interaction-cleanup"). 선택·마퀴 관련 상태를 뺐습니다.
  - **`lib/utils/board_card_selection.dart`**(새 파일) — `BoardCardSelection`
    클래스가 "지금 무엇이 골라져 있는지"(`ids`)와 마퀴 계산에 필요한
    내부 상태(`_marqueeBase`/`_marqueeAdditive`/`_marqueeMoved`), 동작
    (`toggle`/`selectOnly`/`clear`/`beginMarquee`/`applyMarqueeHits`/
    `endMarquee`)을 담습니다.
  - **왜 `ChangeNotifier`가 아닌가**: `board_viewport_gestures.dart`의
    `MarqueeState`와 같은 이유입니다. 이 값은 `board_interaction_controller`가
    이미 갖고 있는 `notifyListeners()` 체계 하나에 얹혀서 화면에
    알려지므로, 여기서 또 다른 알림 체계를 두면 오히려 헷갈립니다.
  - **Dart는 클래스 하나의 몸통을 여러 파일로 쪼갤 수 없습니다**
    (C#의 partial class 같은 기능이 없습니다). 그래서 "정렬·분배
    메서드"(`alignSelected`/`matchSizeSelected`)는 이번에도 옮기지
    못했습니다 — `_cards`/`_selection`/`_measuredHeights`/`_saveCards`를
    함께 쓰는데, `BoardInteractionController` 클래스 자체를 쪼갤 방법이
    없어서 그대로 뒀습니다. 대신 값 하나(선택 상태)만 별도 객체로 빼고,
    그 객체를 다루는 코드만 위임하는 얇은 메서드로 바꿨습니다.
  - 순수 리팩터라 기존 위젯 테스트(마퀴 다중선택 시나리오 포함)가
    그대로 통과하는 것으로 검증했습니다(회귀 없음).
- **다른 무드보드 파일들은 아직 여유가 있습니다** (PR #26 기준):
  `board_card_view.dart` 398줄, `board_selection_bar.dart` 174줄,
  `board_card_actions.dart` 299줄, `board_align.dart` 153줄,
  `board_canvas.dart` 232줄, `board_snap.dart` 181줄,
  `board_view.dart` 164줄, `board_layout.dart` 152줄.
- **`lib/widgets/reference_card.dart`가 635 → 378줄로 줄었지만 여전히
  300줄 기준 초과** (별도 PR — "reference-card-cleanup"). 그림 부분과
  호버 효과를 뺐습니다.
  - **`lib/widgets/reference_card_thumbnail.dart`**(새 파일) — 카드의
    그림 부분(자리표시자, 유튜브 재생 버튼, 호버 미리보기, 고르기
    체크박스)을 통째로 담습니다. 상태 없이 값만 받아 그리는
    `StatelessWidget`이라 별도 컨트롤러 없이 그대로 뺄 수 있었습니다.
  - **`lib/widgets/hover_lift.dart`**(새 파일) — "마우스를 올리면 살짝
    떠오른다" 효과만 하는 범용 도우미 위젯(`_HoverLift`)을 공개
    클래스(`HoverLift`)로 바꿔 옮겼습니다. 레퍼런스 카드에만 매인 게
    아니라 다른 카드 종류가 생겨도 그대로 쓸 수 있습니다.
  - 카드 본체 조립과 아래쪽 글자 부분(제목·폴더·카테고리·태그·메모·
    날짜, `_buildBody`)은 `reference_card.dart`에 그대로 남았습니다.
  - 순수 리팩터라 기존 위젯 테스트가 그대로 통과하는 것으로
    검증했습니다(회귀 없음).

  **`reference_card.dart`가 378 → 187줄로 300줄 기준 안으로 들어왔습니다**
  (별도 PR — "reference-card-body-cleanup"). `_buildBody`(글자 부분)를
  뺐습니다.
  - **`lib/widgets/reference_card_body.dart`**(새 파일) — 제목·폴더·
    카테고리·태그·메모·날짜·삭제 버튼을 통째로 담습니다.
    `reference_card_thumbnail.dart`와 같은 이유로, 상태 없이 값
    (`item`/`taxonomyNames`/`isSelectionMode`)과 콜백(`onDelete`)만
    받는 `StatelessWidget`이라 그대로 뺄 수 있었습니다.
  - `reference_card.dart`에는 이제 카드 본체(테두리·그림자·클릭) 조립만
    남았습니다.
  - 순수 리팩터라 기존 위젯 테스트가 코드 수정 없이 그대로 통과하는
    것으로 검증했습니다(회귀 없음).
- **`lib/screens/reference_detail_screen.dart`가 524 → 495줄로 줄었지만
  여전히 300줄 기준 초과** (별도 PR — "reference-detail-cleanup"). 분류
  항목(파트·폴더·카테고리·태그·프로젝트) 편집 부분을
  `home_selection_controller.dart`와 같은 `ChangeNotifier` 패턴으로 뺐습니다.
  - **`lib/screens/reference_taxonomy_edit_controller.dart`**(새 파일) —
    "지금 무엇을 골랐는지" 상태 다섯 개(`partId`/`folderId`/`categoryId`/
    `tagIds`/`projectIds`)와 `load`/`reloadFor`/`setXxx`/`handleCreated`
    동작이 여기 있습니다. **이미지 경로를 불러오거나 "비슷한 레퍼런스"를
    계산하는 일은 분류 항목과 상관없는 별개의 관심사라 그대로
    `reference_detail_screen.dart`에 남겨뒀습니다** — `_loadOptions()`가
    이 컨트롤러의 `load()`를 부른 뒤 이어서 이미지 경로·유사도를
    계산합니다.
  - **"+ 버튼으로 새 항목을 만들면 바로 골라진다"는 다섯 곳 전부 똑같은
    모양이라 `handleCreated(repository, kind, created)` 하나로
    합쳤습니다.** 파트·폴더·카테고리는 바꿔치기, 태그·프로젝트는 목록
    맨 뒤에 더합니다 — `switch (kind)`로 종류만 가릅니다.
  - `reference_detail_screen.dart`는 이 컨트롤러를 `ListenableBuilder`로
    감싸 다시 그리기만 합니다.
  - 순수 리팩터라 기존 위젯 테스트(태그 붙이기/떼기, 폴더 고르기, **+
    칩으로 새 항목을 만들면 바로 골라지는지**, 비슷한 레퍼런스 관련
    전부)가 그대로 통과하는 것으로 검증했습니다(회귀 없음).

  **다음에 분류 항목 고르는 부분을 고칠 때는 이 컨트롤러를 보세요.**

  **`reference_detail_screen.dart`가 495 → 431줄로 한 번 더 줄었습니다**
  (별도 PR — "reference-detail-preview-cleanup"). 화면 위쪽 미리보기를
  뺐습니다.
  - **`lib/widgets/reference_detail_preview.dart`**(새 파일) — 이미지·
    유튜브 재생 버튼이 있는 미리보기(`_buildPreview()`)를 통째로
    옮겼습니다. `reference_card_thumbnail.dart`와 같은 이유로, 상태
    없이 값(`imagePath`/`isYoutube`)과 콜백(`onPlay`)만 받는
    `StatelessWidget`이라 그대로 뺄 수 있었습니다.
  - 순수 리팩터라 기존 위젯 테스트가 코드 수정 없이 그대로 통과하는
    것으로 검증했습니다(회귀 없음).

  **`reference_detail_screen.dart`가 431 → 362줄로 한 번 더 줄었습니다**
  (별도 PR — "reference-detail-taxonomy-fields-cleanup"). 분류 항목
  칸 다섯 개를 늘어놓는 반복 부분을 뺐습니다.
  - **`lib/widgets/reference_detail_taxonomy_fields.dart`**(새 파일) —
    파트·폴더·카테고리·태그·프로젝트 다섯 칸이 전부
    `ReferenceTaxonomyEditController`를 그대로 받아쓰는 똑같은 모양이라,
    그 반복 자체를 `controller`/`repository` 두 값만 받는
    `StatelessWidget` 하나로 묶었습니다.
  - 순수 리팩터라 기존 위젯 테스트가 코드 수정 없이 그대로 통과하는
    것으로 검증했습니다(회귀 없음).

  `reference_detail_screen.dart`에 남은 것(제목·메모·저장 로직)은
  분류 항목·미리보기와 성격이 달라서 그대로 뒀습니다.
- ~~파트를 지울 때 그 안의 레퍼런스가 미아가 됩니다~~ ✅ **PR #33에서 완료**
  (PR #16에서 빠뜨렸던 것). 이제 파트를 지우면 그 안의 레퍼런스가 **기본
  파트로 옮겨집니다.** 그 결과로 **기본 파트 자체는 지울 수 없습니다**
  (`local_taxonomy_repository.dart`의 `delete()` 맨 위, `defaultPartId`면
  조용히 반환). 이 규칙을 지키며 새 화면을 만들 때는 이 함수를 거치는지
  확인하세요 — 데이터베이스를 직접 만지는 다른 경로를 만들면 이 보호가
  적용되지 않습니다.

### 단계 밖 작업: 웹에서 이미지 바로 가져오기 ✅ 완료 (PR #7)

인수인계 문서가 "별도 검토"로 미뤄뒀던 항목(7장 표의 "북마클릿 웹 캡처" 줄)입니다.
데스크톱에서 세 경로를 한 번에 만들었습니다.

- 앱 창에 **끌어다 놓기**
- **붙여넣기** (Ctrl+V / macOS Cmd+V) — 클립보드 이미지
- **이미지 주소 붙여넣기** — 클립보드가 글자면 주소인지 보고 내려받음

**왜 셋을 함께 만들었나**: 브라우저에서 이미지를 끌면 파일이 아니라 **주소만**
넘어오는 경우가 가장 흔합니다. 끌어다 놓기만 만들면 어떤 경우엔 되고 어떤
경우엔 안 되는 기능이 됩니다. `lib/services/dropped_item_reader.dart`가
종류를 가려내고, 주소면 내려받아서 무엇이 들어오든 이미지 데이터가 나오게 합니다.

**남은 것: 모바일 공유 시트** — 폰 브라우저에서 "공유 → 레퍼런스 아카이브".
완전히 다른 코드 경로라 별도 작업입니다. 아직 착수 시점 미정.

## 플랫폼 차이로 재설계가 필요한 기능
기존 웹앱 일부는 데스크톱 브라우저 전용이라 그대로 못 옮깁니다.
**원칙: 플랫폼에 없는 기능은 억지로 흉내내지 말고, 그 플랫폼에 맞게 바꾸거나 깔끔하게 숨깁니다.**

| 기존 기능 | 문제 | 방향 |
|---|---|---|
| 호버 시 유튜브 자동재생 | 모바일엔 호버가 없음 | ✅ PR #10 — 데스크톱만 호버 미리보기 |
| 플로팅 PiP 무드보드 창 | 브라우저 Document PiP API 전용 | ✅ **되는 것 확인**(2026-08-27, `window_manager`). 데스크톱만. 4단계 8번 |
| 내 컴퓨터 폰트 불러오기 | 데스크톱 전용 개념 | 데스크톱만 제공, 모바일은 번들 폰트 |
| 북마클릿 웹 캡처 | 브라우저 전용 | 모바일 "공유 시트로 받기", 데스크톱은 별도 검토 |
| 우클릭 컨텍스트 메뉴 | 모바일엔 우클릭 없음 | 모바일은 롱프레스로 동일 메뉴 |
| PWA 오프라인 캐싱 | 설치형 앱이라 개념 자체가 불필요 | 삭제 |

## 무드보드 추가 제안 (2026-08-27 의뢰인 정리)

PureRef 대체를 기준으로 의뢰인이 정리해준 목록입니다. **아직 로드맵에 넣지
않았지만 버리지도 않은 것들**입니다. 4단계를 끝낸 뒤 여기서 골라 갑니다.

**이미 결론이 난 세 가지는 위 개발 단계로 옮겼습니다** — 무한 캔버스(4단계
3번), 항상 위(4단계 8번), 판 내보내기(4단계 7번). 나머지는 아래에 남깁니다.

### 우선순위 높음

| 제안 | 왜 필요한가 | 비용 |
|---|---|---|
| **불투명도 조절** | 이미지를 겹쳐 비교하거나 배경에 반투명하게 깔아두고 참고. 캐릭터 포즈 겹쳐보기 등 | `BoardCards`에 칸 추가 = **저장 구조 v4 + 마이그레이션**. 그 자체로 PR 하나 |
| **자유 회전** | 기울어진 구도, 회전된 소품 사진을 그대로 돌려서 배치 | v4 마이그레이션. **스냅과 충돌**합니다 — 돌아간 카드의 "왼쪽 가장자리"가 무엇인지부터 정해야 하므로 **스냅 다음에** 오는 것이 맞습니다 |
| **판 배경색** | 어두운 톤 이미지를 모을 때 배경이 밝으면 비교가 안 됨 | 작습니다. 지금 `board_viewport.dart`가 배경을 한 줄로 그립니다. 판마다 다르게 하려면 v4 |
| **무드보드를 진짜 별도 창(팝업)으로 띄우기** | PR #30의 "항상 위"는 앱 창 하나가 위로 뜨는 것입니다. 의뢰인이 원한 것은 **메인 창과 무드보드 창을 아예 분리**하는 것 — 메인 창에서 다른 작업(레퍼런스 정리 등)을 계속하면서 무드보드만 따로 띄워두는 그림입니다. | **스파이크로 가능성만 확인함(2026-08-31, 버리는 코드)**: `desktop_multi_window` 패키지로 진짜 두 번째 OS 창을 띄울 수 있고, 그 창이 같은 sqlite 파일에 **별도 연결**을 열어도 문제없이 데이터를 읽는 것까지 확인했습니다. 새 러너(native) 설정 없이 빌드도 됩니다. 정식으로 만들 때 정할 것: 두 번째 창이 특정 무드보드 하나만 보여줄지 아무 판이나 고를 수 있게 할지, 메인 창을 닫으면 같이 닫힐지, 두 창을 동시에 켜놨을 때 한쪽에서 옮긴 카드가 다른 쪽에 실시간으로 보여야 하는지(아니면 각자 새로고침해야 보이는 정도로 둘지). **아키텍처 단계 브레인스토밍부터 다시 시작**(bounded가 아니라 새 서브시스템입니다). |

### 중간

- **판 안에 텍스트·화살표 주석** — 5단계 스티키노트와 겹치지만 더 가벼운 형태.
  "이 부분 색감 참고" 같은 짧은 메모를 이미지 **위에** 바로 다는 것.
  5단계를 할 때 "무드보드용 가벼운 주석"을 따로 둘지 함께 정하세요.
- **판 사이로 카드 옮기기** — 같은 레퍼런스를 다른 판에도 놓고 싶은 경우.
  지금은 목록에서 다시 찾아 담아야 합니다. 4단계 5번(다중선택) 뒤가 자연스럽습니다.

### 나중에

- **판 잠금** — 배치가 끝난 판을 실수로 못 건드리게. PureRef에 있는 기능.
- **화면 밖 이미지 언로드** — 무한 캔버스에서 이미지가 많아지면 필요해집니다.
  **3번(무한 캔버스)을 만들 때 구조만 염두에 두세요.** 지금 최적화하지는
  마세요 — 느려지는 것을 확인한 뒤에 고치는 편이 낫습니다.

### 검토했으나 채택 보류 (기록용)

**웹 전체에서 비슷한 레퍼런스 자동 추천(핀터레스트식)** — 안 합니다.

- 로컬 우선·서버 없음 원칙과 충돌합니다.
- 리버스 이미지 검색은 유료 API가 필요합니다.
- **클립보드로 붙여넣은 이미지는 원본 사이트 정보가 없어서** 전제가 성립하지
  않는 경우가 많습니다.

대신 두 가지로 대체합니다.

- **"이 이미지로 구글 이미지 검색 열기"** 버튼 정도로 가볍게. 브라우저를 열어줄 뿐입니다.
- **6단계 유사도 엔진을 "내 아카이브 안에서 비슷한 것 찾기"로.** 이쪽이 프로젝트
  철학에 맞습니다. **우선순위를 앞당기는 것도 검토할 만합니다.**

## 지금 결정하지 않는 것
서버/백엔드 스택, 실시간 vs 주기적 동기화, 인증/계정, 공유 스페이스 구체 설계,
앱스토어 출시 절차/코드사인/심사, 유료화·구독. 전부 나중에 별도 논의합니다.

## 작업 워크플로우 (매번 이렇게 진행)
1. `main`에서 새 브랜치 생성 (**절대 main에 직접 커밋하지 않음**)
2. 기능 구현
3. **테스트** — `flutter test`(단위/위젯)와 `flutter run`(실제 실행) 둘 다 활용.
   기존 웹앱처럼 브라우저 자동화에 의존하지 않아도 되므로 **위젯 테스트를 적극 작성**할 것.
   최소한 데스크톱에서 실행해 눈으로 확인합니다.

   **웹뷰가 얽힌 문제는 `flutter test`로 못 잡습니다.** 일반 테스트에서는 웹뷰
   부품이 아예 안 켜집니다(`InAppWebViewPlatform.instance == null`). 그럴 때는
   진짜 앱을 띄우는 통합 테스트를 씁니다. (PR #10에서 크래시를 쫓으며 만들었음)

   ```
   flutter test integration_test -d windows
   ```

   느리고 창이 실제로 떴다 사라지므로 **평소에는 안 돌립니다.** 웹뷰가 얽힌
   문제를 쫓을 때만 씁니다.
4. 상세한 커밋 메시지로 커밋 → push
5. `gh pr create`로 Summary + Test plan + **"나중에 여기를 고치려면 어디를 보면 되는지"**
   포함해 PR 오픈
6. **의뢰인의 명시적 "병합해줘" 전까지 병합하지 않고 대기**
7. 병합 후 로컬 `main` 동기화 (`git checkout main && git pull origin main`)
8. **`update.md`에 이번 PR 내용 추가 → 커밋 → push**
   (`CLAUDE.md`는 구조/워크플로우가 바뀔 때만 갱신)
9. 다음 단계로 이동
